# Review Report: accessibility-lens

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The diff of `/increment`, `.spark/accessibility-lens/plan.md` |
| **Status** | `passed` |
| **Round** | 2 |
| **Date** | 2026-09-15 |

<!-- Handoff: read this block first, the numbered sections below by exception. Whoever
     writes to this report — including `/increment` in fix-mode, which is not this
     report's owner — updates it in the same edit that closes or re-rules a finding:
     overwrite in place, never append. The block holds one current state, never a
     per-round log; a stale block is a defect, not a cosmetic issue. -->

**Handoff**
- **Status:** `passed` (round 2). All seven round-1 findings re-verified against source and confirmed genuinely fixed; three new Minors, all introduced *by* the fix pass, found and fixed by the Reviewer. Gate closed — `/demo-day` may start.
- **Verdict:** §6, rewritten for round 2. Round 1's verdict is superseded, not softened: its condition ("fix F1, F2 and F3 and this passes") was met on the merits, checked at the source, not on the fix summary's word.
- **Re-derived from source this round, not cited** (condition (a) — verifying a fix to the very fact): both `agents/facilitator.md` quotes and `templates/constitution.md:35`; the `5 sites` arithmetic; the fence (`git diff --name-only main` over the four protected paths → empty, Must-AC, condition (b)); `claude plugin validate .` (green); the 5/5 phase↔section mapping after F4's heading rewrite; AC-2.5's three bullets after F5's deletion, plus a repo-wide sweep for orphaned references to the deleted bullet (none outside this report's own F5 row).
- **New this round:** F9, F10, F11 — Minor, artifact-internal, all `fixed r2` by the Reviewer in place (`evidence.md` ×2, `plan.md` ×1). Each is collateral of a round-1 fix, not a pre-existing defect.
- **Open:** `0`. No Blocker, no Major, no unwaived Minor.
- **Binding ruling:** §6 Verdict and the gate checklist below — the only binding location.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Round 2 (re-review of `/increment` fix-mode). Same diff surface as round 1, re-confirmed: `git diff --name-only main` → `README.md`, `lenses/README.md`, `lenses/seo.md`, `lenses/ux.md` (+19/−5) plus untracked `lenses/accessibility.md` and `.spark/accessibility-lens/*`; nothing else. Re-read in full for this round: `lenses/accessibility.md`, `lenses/README.md` §"Adding a lens", `README.md:294-325`, `evidence.md` T7 (incl. the F1 extension), `plan.md` §Deviations, and the four source locations F1 quotes (`agents/facilitator.md:45-70`, `templates/constitution.md:25-40`) — read as context, not edited.
Scope of re-verification: each of F1–F7 checked against its source rather than its fix summary, plus the four regression surfaces a fix pass could plausibly have broken (fence, `plugin validate`, lens-internal consistency, collateral staleness). Not re-derived: round-1's own AC-2.1/2.2/2.3/2.4 and NFR-1/2/3/5 verdicts, untouched by the fixes and left exactly as they were.
Not reviewed: `.spark/.guard/**` (Open Question 1, unchanged); the positive-firing half (no venue on this repo, correctly `not-verified-live`).
Tooling: no `aspark-graph` query (the graph does not index Markdown — structural absence already recorded in `evidence.md`). `claude plugin validate .` re-run by me post-fix: `✔ Validation passed with warnings` (one pre-existing `autoUpdate` warning).

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | `lenses/accessibility.md:1-5` — `triggers` present, `applies-to` absent, `phases` 5-token incl. `act`; who-owns-what 5 rows; "never invents scope" at `:29`; Specify section beats the template baseline with a numeric target |
| T2 | ✅ | Counting domain stated; zero recorded *with* its reason. Independently re-derived (see AC-1.2) |
| T3 | ✅ r2 | Substance correct (cites, does not restate). Both deltas now in §Deviations with the T1-vs-T3 `4.5:1` conflict *reasoned*, not asserted (F7 fixed); `4.5:1` verified present only at `:45`, `44px` verified absent, `ux` cited by path at `:57-58` |
| T4 | ✅ r2 | Headings now `### n. <Topic> *(<Phase> — <Agent>)*`, matching `security.md`'s topic-first form (F4); the unauthorized 4th QA bullet is gone and AC-2.5's three are byte-intact (F5); 5/5 mapping re-derived after the rewrite |
| T5 | ✅ | `lenses/README.md:49`, `:92`, `:106` — all three edits, same format as `security`/`i18n`/`data` |
| T6 | ✅ | Exactly 1 line each (`seo.md:54`, `ux.md:59`); `seo.md:76` / `ux.md:71-72` untouched, as ruled |
| T7 | ✅ r2 | Activation layer now audited alongside dispatch, quotes re-verified verbatim at source, verdict correctly restated as five sites (F1); `peer-review`'s row downgraded from clean pass to grouped-with-F1 (F6). Two citation ranges were off by one line — F9, reviewer-fixed |
| T8 | ✅ r2 | `README.md:302` now states the live proof-state honestly (five sites, both ledgers linked) while `:320-322`'s dated sweep narrative is correctly left alone (F2); R6's promised README disclosure is delivered. The §Deviations entry went stale as a result — F11, reviewer-fixed |
| T9 | ✅ | Fence audit reproduced by me exactly; lens count 10; negative case re-run holds; AC-1.4 split verdict correct; `constitution.md:16` ("8 lenses") verified stale and correctly routed to `/charter` per Q3 |

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Major | `agents/facilitator.md:51-52` and `:67-69`; `templates/constitution.md:27-28` and `:35` | The **activation** vocabulary is a closed enumeration of six characteristics — `:51-52` "`handles-auth`, `is-public`, `handles-payments`, `handles-pii`, `has-database`, `is-multilingual`", `:67-69` maps only `security`/`i18n`/`data`, and `templates/constitution.md:35` ships consumers `<!-- any of: handles-auth · … · is-multilingual -->`. Since the constitution is the only place a lens is switched on and `/charter` is its only writer, `must-be-accessible` is not in the vocabulary any `/charter` run proposes or any consumer's constitution template offers. T7 audited only which lens *paths get passed once active* and never this layer, so NFR-6's refutation is under-scoped: 4+ sites, not 2, and the one that matters most is missing. (`facilitator.md:46-47` does point at `lenses/README.md`'s detection tables, which now carry the row — that is why this is Major, not Blocker: reachable by a diligent Facilitator, not by the literal instruction.) **Fix:** do not repair here (Q2's ruling applies equally); extend `evidence.md`'s T7 entry with these `file:line`s and route them to the same follow-up increment, then re-word the consequence in the README per F2. | fixed r2 |
| F2 | Major | `README.md:302` | The T8 deviation refused to change "the add-a-file guarantee refuted for **one** skill file" in *both* places. That is correct for `:320-322` (see §6) but wrong here: `README.md:294-296` states in its own words that the table beginning at `:298` "is not dated and not frozen — it tracks each part's proof state and is updated whenever that state changes", and the neighbouring row `:303` dates its sweep explicitly while `:302` does not. So `:302` is a live-state claim, now false, in the file whose "always reflects the current state" assertion constitution §4 names as the bar. Compounding: plan R6's mitigation was "recorded at T7 **and surfaced in `README.md` at T8** — never left implicit", and the deviation dropped that surfacing without noting it, while `README.md:87` (added by this diff) advertises "keyboard-only + measured-contrast QA" the feature's own evidence says will not dispatch. **Fix:** in `:302` replace "refuted for one skill file" with the current count and its consequence, e.g. "refuted at four sites: two skill files' closed lens lists (`demo-day`, `look-and-feel`) and the `/charter` activation vocabulary (`agents/facilitator.md`, `templates/constitution.md`) — so the `accessibility` lens ships with its Design and QA checks undispatched and its characteristic not yet offered by `/charter`", plus a link to this feature's ledger. Left `open` rather than reviewer-fixed because the correct number depends on F1's disposition. | fixed r2 |
| F3 | Major | `lenses/README.md:117-118` | "Its checks flow automatically: the skills pass active lens paths to their agents by name, so any activated lens is picked up without further wiring." This is the add-a-file promise made to the next lens author, in a file **this diff already edits**, and this feature is the proof it is false for the `design` and `qa` phases (and, per F1, for activation). Pre-existing since `situational-lenses` found `demo-day:84`, but doubled and left unqualified here; constitution §1 "a doc that presents an intention as delivered is a defect". **Fix:** append one qualifying clause naming the known exceptions and pointing at `.spark/accessibility-lens/evidence.md`, until the routed dispatch fix lands. | fixed r2 |
| F4 | Minor | `lenses/accessibility.md:43,51,62,77,87` | All 8 sibling lenses use `### n. <Topic> *(<Phase>)*` — the italic tag is the **owner phase** (`security.md:34-57`, `ux.md:34-58`). This file uniquely uses `### n. <Phase> *(<Agent>)*`, so the italics read "(Designer)", "(QA Tester)". Agents are told to "apply only the rows for its phase" and have 8 files' worth of precedent for finding that phase in the italics; and the task's own bar — a future author can use either file as the template — is not met, since the two named precedents now disagree. T3/T4's DoD ("tagged with its owner phase in italics like `security.md:34-57`") is literally unmet. **Fix:** move the phase into the italic tag, e.g. `### 2. Grounding the design baseline *(Design — Designer)*`. Not reviewer-fixed: renaming five headings also touches the `(§3)` cross-reference at `:93`. | fixed r2 |
| F5 | Minor | `lenses/accessibility.md:95-96` | "The core flow stays usable at 200% zoom/reflow, no horizontal scroll or lost content" is a 4th QA bullet that no AC authorizes (AC-2.5 lists three items; plan T4's DoD enumerates the same three), it overlaps `ux.md:54` ("no horizontal scroll, no content clipped") without the cite-once treatment every other bullet in this file applies, and spec §6 explicitly cuts "`ux`'s … responsive/touch". The file's own rule at `:29` is "it never invents scope". **Fix:** either drop the bullet, or keep it (it is genuinely WCAG 1.4.10 reflow, not merely responsive) with a cite-once clause against `ux.md:54` **and** a recorded AC-2.5 amendment — the PO's call, not the reviewer's. | fixed r2 |
| F6 | Minor | `.spark/accessibility-lens/evidence.md` (T7, "Review — generic placeholder"); source `skills/peer-review/SKILL.md:50-51` | Read directly: "`…/lenses/<name>.md` for any of `seo`, `api`, `cli`, `library`, `security`, `data`, `i18n` that are active" is a closed 7-name membership list that will not name `accessibility`. `situational-lenses` Entry 3's distinction (generic `<name>.md` placeholder vs. literal filenames) is real and I did not overturn it — but the evidence presents Review as a clean ✅ next to `demo-day:84`'s refutation, when the two instructions are grammatically near-identical (generic head clause + parenthetical list) and only the object form differs. **Fix:** restate the Review row as "dispatches by placeholder, but its membership parenthetical is closed — lower-confidence ✅, same reword applies", rather than a binary pass. | fixed r2 |
| F7 | Minor | `.spark/accessibility-lens/plan.md` §Deviations | Two further T3 deltas went unrecorded: (a) T3's DoD "the strings `4.5:1`, `44px` appear 0 times outside that citation" is literally unmet — `4.5:1` is at `lenses/accessibility.md:45` — because T1's DoD *required* a numeric target in the Specify section; the two DoDs conflict and nothing says which won. (b) The DoD said "cites `lenses/ux.md:60`"; the file cites the motion check by path without a line (arguably better — line numbers rot). Neither is a substantive defect; both are plan drift that only this report now records. **Fix:** add both to §Deviations, noting T1 beats T3 on the `4.5:1` conflict. | fixed r2 |
| F8 | Minor | `lenses/accessibility.md:84` | Shipped consumer-facing material cited "the same narrower Review remit **the founding issue** specifies" — the only line in all of `lenses/` referencing an artifact (GitHub issue #6) a plugin consumer cannot see. **Fixed by reviewer:** replaced with "a deliberately narrower Review remit"; no check changed, `claude plugin validate .` re-run green. | fixed r2 |
| F9 | Minor | `.spark/accessibility-lens/evidence.md:100`, `:103` (and the same refs at `:116`, `:120`) | F1's fix quotes `agents/facilitator.md` accurately but *under-ranges* both citations: the text quoted at `:51` runs to `:52` (`handles-pii`, `has-database`, `is-multilingual` sit on 52) and the text quoted at `:67-68` runs to `:69` (`has-database` sits on 69). Round 1 cited `:51-52`/`:67-69` correctly; the fix narrowed them. A reader following the citation to route the fix lands on a partial quote and could conclude the enumeration is shorter than it is. **Fixed by reviewer:** ranges restored to `:51-52` and `:67-69` at all four occurrences; quotes themselves re-verified verbatim and are correct. | fixed r2 |
| F10 | Minor | `.spark/accessibility-lens/evidence.md:62` | F6's fix correctly downgraded the Review row to "lower-confidence ✅ … grouped with F1's four sites", but left it under the group header "**Dispatch correctly generic, verified by reading, no finding:**". The header now contradicts the bullet it introduces — a skim-reader routing the follow-up increment would drop `peer-review:50-51` from the fix set, which is exactly the five-vs-four confusion F6 existed to prevent. **Fixed by reviewer:** header reworded to "Dispatch verified by reading — Specify and Act clean, Review downgraded (F6)"; the two genuinely clean rows are unchanged. | fixed r2 |
| F11 | Minor | `.spark/accessibility-lens/plan.md:116` | Collateral of F2: the §Deviations entry still opened "T8 — did not correct `README.md:302` / `:319-321`'s …" after `:302` *had* been corrected, so the plan's own drift log asserted the opposite of the diff — the one artifact whose job is to be true about what was and wasn't done. (The `:319-321` range was also off by one; the sentence is at `:320-322`.) **Fixed by reviewer:** entry re-scoped to `:320-322`, range corrected, and a two-sentence round-1 amendment added recording that the argument holds for the dated narrative only. The original reasoning is preserved verbatim, not rewritten. | fixed r2 |

## 4. Requirements Traceability

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1 | `lenses/accessibility.md:1-5` | ✅ met — `triggers: [must-be-accessible]`, `applies-to` absent, `phases: [specify, design, act, review, qa]`; matches `security.md:1-5`'s key shape exactly |
| AC-1.2 | `.spark/constitution.md` §2 | ✅ met — re-derived: `grep -in "must-be-accessible" .spark/constitution.md` → no match; active-lens table has one row (`library`); only accessibility mention in the file is §4's "N/A — no UI" |
| AC-1.3 | `lenses/README.md:92`, `:106` (+ `:49`) | ✅ met — both tables, same format as the `security`/`i18n`/`data` rows |
| AC-1.4 | `git diff --name-only main -- skills/ agents/ templates/ .claude-plugin/` → empty (re-run r2, post-fix) | ⚠️ partial — "no agent or skill file edited" confirmed by my own run; "touches only 4 files" `refuted-with-finding`, ruled at the plan gate (Q1). Ruling honored: README carries the 5th-file edit, the dispatch fix is genuinely absent, `constitution.md:16` genuinely untouched |
| AC-2.1 | `lenses/accessibility.md:43-49` | ✅ met |
| AC-2.2 | `:51-60` | ✅ met — read side by side with `agents/designer.md:56-60`: the lens cites §3 by path and restates none of its four signals; `ux` motion check cited once |
| AC-2.3 | `:62-75` | ✅ met — semantic-first, ARIA-as-fallback, live regions, plus the explicit "no lens-specific step in `/increment`" (C6, re-verified at `skills/increment/SKILL.md:26-27`) |
| AC-2.4 | `:77-86` | ✅ met — incl. the explicit statement of what Review does *not* re-check (R3 made legible) |
| AC-2.5 | `:87-94` | ✅ met r2 — exactly three bullets, each mapping 1:1 to the AC's three clauses; the unauthorized 4th is deleted, not spec-amended, and has no orphaned reference left anywhere in `lenses/`, `README.md` or this feature's ledger |
| AC-3.1 / AC-3.2 | `lenses/seo.md:54`, `lenses/ux.md:59` | ✅ met — 1 line each, citing phase unchanged, no other check touched |
| NFR-1 | `lenses/accessibility.md` whole file | ✅ — frontmatter, per-phase owner map, falsifiable checks throughout (F4 is shape, not contract) |
| NFR-2 | `:1-5` vs `:43-97` | ✅ — 5/5 both directions: every `phases` token owns exactly one `###` section, every section's owner is in `phases` |
| NFR-3 | `evidence.md` T9 | ✅ — registration in `lenses/README.md` alone activates nothing; the constitution still doesn't declare it |
| NFR-4 | this repo's profile | ✅ — zero checks/NFRs contributed here, *because* the characteristic is absent |
| NFR-5 | whole diff | ✅ — purely additive; no command, heading, column or ID pattern renamed or removed (`library` lens §1/§2/§4 applied; §3 N/A per constitution §2). Version bump correctly deferred to `/go-live` |
| NFR-6 | `evidence.md` T7 | ❌ r2 — `refuted-with-finding`, now correctly scoped: five sites, activation *and* dispatch, all routed to one follow-up increment. The ❌ is the honest outcome, not a failure of this diff (`CLAUDE.md`: refuted-with-finding is a valid ceremony outcome) |
| NFR-Provability | `evidence.md`, `README.md:302`, `lenses/README.md:117-129` | ✅ r2 — the gap now reaches the two places a reader actually meets it: the live proof-state table and the add-a-lens promise itself, each linking this feature's ledger |

## 5. What Was Checked

- [x] Correctness: every Must AC traced to the file and line that implements it; all five phase rows read against their AC
- [x] Non-functional: constitution §1 (suppression, honesty), §2 (`library` lens applied to the diff), §3 (add-a-file, Markdown-only), §4 (docs-in-step, dogfood), §5, §6 all checked
- [x] Error handling: n/a — no executable code; the analogue (what happens when a check has no carrier) is checked at `:73-75`, `:101-104` and R5
- [x] Security: no secrets, no user input, no execution introduced; `.spark/.guard/` raised as an Open Question
- [x] Tests: no suite exists or is possible (constitution §4); `claude plugin validate .` re-run green by me, T2/T9's negative case independently re-derived
- [x] Readability: the lens reads cleanly and a consumer can follow every citation after F8's fix
- [x] r2 regression sweep: fence empty post-fix and the changed-file set is exactly what fix-mode claimed; `plugin validate` green; phase↔section 5/5 both directions after the heading rewrite; no orphaned reference to the deleted QA bullet; `README.md:302`'s "five sites" arithmetic re-derived (2 skills + facilitator + template + peer-review) and "9th lens" confirmed against `ls lenses/*.md` = 9; `lenses/README.md`'s `../.spark/accessibility-lens/evidence.md` resolves from its own directory

**Open questions (cannot be judged here):**
1. `.spark/.guard/ledger.jsonl` and `trail.jsonl` are untracked, un-ignored and present in the working tree. Constitution §6's test is **presence, not tracking**, and `marketplace.json`'s `source: "./"` copies ignored files — so these ship to every consumer at `/go-live`. Contents look benign (this repo's own public feature names, git SHAs, session UUIDs), and they are unrelated to this feature, but `evidence.md` dismissed them as "out of scope" without checking. For `/go-live`, not for this diff.
2. Whether F1's activation gap should ride the same follow-up increment as Q2's dispatch fix, or block the lens's announcement in `README.md` until it lands, is the user's call.

## 6. Verdict

**Passed.** I re-checked all seven fixes against the source rather than against the fix summary, and every one of them does what it claims. The two that mattered are genuinely closed, not papered over: F1's extension quotes `agents/facilitator.md` and `templates/constitution.md` verbatim — I re-read both files rather than trusting the ledger, because verifying a fix to a fact is exactly when re-derivation is mandatory — and its "five sites" verdict is arithmetically and substantively right (`look-and-feel`, `demo-day`, the Facilitator's characteristic vocabulary, the shipped constitution template, and `peer-review`'s parenthetical), with the Not-a-Blocker reasoning about `facilitator.md:46-47` preserved intact. F2 is the fix I expected to go wrong and it did not: `README.md:302` now carries the *current* count rather than the count that was current when the fix was drafted, links both ledgers, and — the part that takes discipline — leaves `:320-322`'s dated sweep narrative untouched, so the repo still says two different things about "one skill file" in the only two places where saying two different things is correct. F3's qualifying clause names its exceptions precisely (including `peer-review`'s caveat) and its relative link resolves. F4's headings now match `security.md`'s topic-first form and the `(§3)` cross-references still land on the Act section, because the headings were retitled and not renumbered. F5 deleted the unauthorized bullet instead of retro-amending the AC to cover it, which was the right of the two options offered. F6 and F7 hold. What the fix pass did introduce is three Minors of its own, all of the same species — an artifact that went stale or imprecise *because* a neighbouring artifact was corrected: two under-ranged citations in `evidence.md`, a group header that still said "no finding" above a bullet that now carries one, and a §Deviations entry still claiming `README.md:302` was deliberately left alone after F2 changed it. Each is small, each is exactly the kind of drift this project's own constitution §1 calls a defect rather than a cosmetic issue, and each was obvious enough to fix in place, which I did; none changes a verdict, a gate answer or a Must AC, so all three are capped at Minor. NFR-6 remains ❌ `refuted-with-finding` and that is the honest outcome, not a blocker — the lens is complete and correct, the loop's dispatch and activation layers are not, and this feature's job was to prove that in writing and route it, which it has now done at five sites instead of two. The fence held empty across the fix pass, `claude plugin validate .` is green, the changed-file set is exactly what was claimed, and the lens's internal 5/5 phase↔section mapping survived both the heading rewrite and the bullet deletion. Ship it.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings
- [x] No open Major findings — F1, F2, F3 confirmed `fixed r2` against source; no waiver needed or used
- [x] Every Must AC traces to implementing code; no constitution non-negotiable violated (§6's five non-negotiables all hold; §1/§4's quality bars, breached at round 1 by F2/F3, now met)
- [x] All plan deviations documented and accepted — T8's entry corrected and amended (F11), T3's two deltas recorded with the DoD conflict reasoned (F7)
- [x] Test suite runs green — n/a by constitution §4; `claude plugin validate .` re-run green post-fix, fence re-derived empty
- [x] Line budget respected: Ist 117 / Soll ~150 (excluding HTML comments)
- [x] Status set to `passed`
