# QA Report: handbook-maturity

| | |
|---|---|
| **Phase** | Review (hands-on) |
| **Owner** | QA Tester (`/demo-day`) |
| **Input** | **Recorded override (plan §4):** no app, URL or browser exists — the work object is the binary `docs/aSPARK_Enterprise_Architecture_Handbook.docx`. Per the approved plan §4 ceremony-appropriate equivalent, this report rests on **QA's own fresh `textutil -convert txt -stdout` extraction**, independent re-walking of every spec §4 AC against that extraction, hands-on package inspection (zip listing / `testzip` / XML parse), and validation of the recorded AC-2.3 dry run. Builder and reviewer artifacts were never opened; every equality claim was re-established by reproducing hashes myself. |
| **Status** | `passed` |
| **Date** | 2026-08-25 |

<!-- Handoff: read this block first, the numbered sections below by exception. Whoever
     writes to this report updates it in the same edit that closes or re-rules a bug:
     overwrite in place, never append. The block holds one current state, never a
     per-round log; a stale block is a defect, not a cosmetic issue. -->

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`). QA executed 2026-08-25 on working tree of `feat/lean-rounds` vs HEAD `3666787`, testing the post-F1-fix state (review round 1 passed; F1 `fixed r1`; F2 routed/waived). All 11 acceptance criteria and all applicable NFRs verified by the override method; the recorded dry run validated. Next ceremony step: `/demo-day` equivalent — the user's open-in-Word visual integrity check reserved for release — then `/go-live`.
- **Verdict:** Would demo this to a stakeholder right now — on the recorded method: the front matter tells the truth legibly (zero chapters Shipped, sharp and unmissable), every label/marker/table row verified hands-on, the prose freeze independently re-proven (8 removed / 101 added, all manifest-layer), and the package is intact, DEFLATE-compressed and only `word/document.xml` different from HEAD.
- **Open:** `none` — Blockers: none; Majors: none; Minors: none open (three documentation-grade observations E1–E3 in §3, **user-accepted 2026-08-25** — objection window closed with the routing decision; no product defect). User decision same day: proceed to `/go-live`; the open-in-Word visual integrity check stays booked for release per plan §4.
- **Binding ruling:** §2 Acceptance Criteria Verification carries the per-criterion verdicts; §3 holds exploratory findings; the ✅ QA GATE below is the definition of done.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/demo-day` and proceed — don't stop on it.

## 1. Test Environment

- **Work object:** `docs/aSPARK_Enterprise_Architecture_Handbook.docx` — 2,013,907 bytes (exact match to the post-F1 record), SHA-256 `b46cd684cc2a50d6b2f11b20c928049f6c32b10533ef2224dce498f5fa2eebdf`, mtime 2026-08-25 08:14. Branch `feat/lean-rounds`, HEAD `3666787`.
- **Override method (recorded reason):** binary docx has no browser/demo surface; plan §4 books QA's own fresh extraction + independent AC walk + dry-run validation instead of the browser gate. Applied exactly; stated here rather than skipped silently.
- **Extraction channel:** `textutil -convert txt -stdout docs/aSPARK_Enterprise_Architecture_Handbook.docx > /tmp/hm-qa/qa-extract.txt` — 1325 lines, SHA-256 `8f2c4e6bb2b26e2cc299c09526e63f0ce01d65f96a98f1288535f145695aa415` (matches the round-1-reviewed extraction hash cited in review.md). All line numbers below cite this file.
- **Independent baseline:** `git cat-file -p HEAD:docs/…docx` → SHA `73b1f446…` (= evidence's build-start hash, byte-for-byte); its own fresh extraction → 1232 lines, SHA `11902927…` (= T2's recorded baseline-extraction hash). My delta audit therefore runs builder-independent on both sides.
- **Package integrity (hands-on):** `zipfile` inspection — 46 parts, `testzip()` None, **every entry DEFLATE** (F1 fix confirmed at container level), all XML/.rels parts well-formed via ElementTree, part set AND order identical to HEAD's package, exactly one part byte-different from HEAD: `word/document.xml` (759,017 B). Overview table is a real `w:tbl` element; all 17 inserted label paragraphs carry a bold first run in raw OOXML (visibility has formatting backing, not just extraction presence).
- **Browser / viewport(s):** N/A — no UI exists; design review marked N/A in spec §8; the rendered-document visual check is deliberately reserved for the release open-in-Word step (plan §4).
- **Test data / accounts used:** none required; public repo state plus remote tag listing (`git ls-remote --tags https://github.com/a-lottes/aSPARK-graph`) for baseline verification.
- **Graph tool surface:** runner + `graph.json` present, but plan §2 and review §1 already recorded these exact paths as not indexed (`files_checked: 0` / `unknown_files`); the tool's own rules forbid resting any result on it here. Scoping done by hand from the extraction map; nothing in this report cites graph output.
- **Console/network equivalents:** N/A by method — a static binary document has no console and makes no requests. Recorded explicitly; §4 below.

## 2. Acceptance Criteria Verification

Every row performed by me, on my own extraction (`/tmp/hm-qa/qa-extract.txt`, SHA above). No verdict rests on a builder/reviewer artifact or a tool answer.

| Spec ID | Steps performed | Expected | Observed | Result |
|---|---|---|---|---|
| AC-1.1 | Own extraction; scripted walk requiring every chapter heading to be immediately followed by a stage-label paragraph; XML check that each label paragraph's first run is bold | Each of the 17 chapters' first page shows a visible stage label drawn only from {Shipped, Partial, Planned} | 17/17 headings (l. 71, 127, 188, 272, 409, 460, 547, 598, 672, 747, 833, 907, 952, 1003, 1059, 1118, 1160) each immediately followed by `Delivery stage: <value>`; values ∈ {Partial, Planned}; all 17 bold-led in raw OOXML | ✅ pass |
| AC-1.2 | Counted `^Delivery stage:` lines document-wide (17); located legend occurrences; extracted the value set after every label | Exactly one label per chapter; single legend defining all three values in one place; no fourth value anywhere as a chapter stage | 17 labels total = one per chapter; legend exactly once (l. 70) defining Shipped/Partial/Planned and sitting directly before ch. 1; value set = {Partial, Planned} ⊆ taxonomy; case-insensitive `delivery stage` hits = 19 lines, all accounted (17 labels + header cell l. 16 + legend l. 70) | ✅ pass |
| AC-1.3 | Printed each of the 18 inserted markers together with its preceding line; asserted each marker text is in my own diff whitelist; compared kept paragraphs byte-wise against my HEAD-side extraction | Every inventoried target-state item carries an adjacent inline marker naming it outside the shipped baseline | 18/18 markers directly follow their claims — incl. M9 (D2) at §3.2.2 (claim l. 227 → marker l. 228), M12 at §10.6 (808→809), M15/D1 at ch. 13 (956→957); all texts complete sentences, zero corrupted mid-sentence; kept ch. 8 (now l. 658) and ch. 11 (now l. 889) `Status.` paragraphs present verbatim in HEAD extraction too — byte-unchanged | ✅ pass |
| AC-1.4 | Region-scoped greps inside the four Planned chapters | Each Planned chapter names the roadmap stage/product expected to deliver it | ch. 9 names `aSPARK-policy` (3 lines / 4 occurrences, l. 676/702×2/738); ch. 13 via D1/M15 names "Stages 3–4 (aSPARK-policy … see Chapter 16 Roadmap)" (l. 957); ch. 14 names `aSPARK-ci` ×2; ch. 16 matches `Stage(s) N` ×7 | ✅ pass |
| AC-1.5 | Scripted read of ch. 3's product-family table cells (name / responsibility / maturity triples); document-wide scan for old taxonomy values as standalone cell lines | Maturity column uses the new taxonomy and distinguishes the two shipped products from the five nonexistent ones | Core `Shipped`, graph `Shipped`, policy/insights/ci/memory/Enterprise `Planned`; each of the 7 product rows appears exactly once; standalone `Foundation`/`Future`/`Distribution` cells: zero document-wide; lone standalone `Platform` (l. 713) is pre-existing ch. 9 hierarchy-table cell ("aSPARK Default"), not a maturity value | ✅ pass |
| AC-2.1 | Parsed the front-matter table from my extraction: header triple + data-row triples; asserted position before ch. 1 heading | One overview table before chapter 1 with exactly 17 rows showing stage + marker pointer per chapter | Header row (Chapter / Delivery stage / Target-state markers) at l. 15–17; exactly 17 data rows (chapters 1–17 in order, l. 18–68), every row carrying a stage ∈ taxonomy and a pointer (`Status. markers in this chapter` ×13 or `whole chapter awaits delivery — see ch. 16 Roadmap` ×4); table entirely before ch. 1 heading (l. 71); real `w:tbl` in OOXML | ✅ pass |
| AC-2.2 | Mechanical cross-check: parsed the 17 table stages and the 17 in-chapter labels from my own extraction and compared pairwise | No stage value in the table disagrees with the label inside the corresponding chapter | 17/17 agreements, zero mismatches (scripted comparison, empty mismatch list) | ✅ pass |
| AC-2.3 | Validated the recorded dry run (evidence.md "AC-2.3 dry run"): reconstructed the claimed material from my byte-identical extraction (l. 1–69), checked it is front-matter-only, checked both answers against shipped reality, checked reader freshness and timing record | A reader unfamiliar with the handbook answers both probes correctly from title page + table alone within 2 minutes | Material l. 1–69 = title page + "Contents" heading + overview table + trailing blank ONLY — front-matter-only claim TRUE (no chapter prose, no in-chapter markers). Answer (a) "Core and graph": correct — exactly the products named in the title-page baseline pairs, both releases independently re-verified by me today (Core tag v0.7.0 → commit `753a793` locally; graph's highest remote tag v0.7.0 via ls-remote). Answer (b) "No chapter": mechanically correct — zero Shipped labels among 17 (13 Partial / 4 Planned), i.e. the expected sharp result confirmed. Reader = user (fresh context, not the builder). Timing recorded qualitatively ("well under 2 minutes"), not measured — see E1 | ✅ pass |
| AC-3.1 | Read title page from my extraction (l. 2–8); grepped document-wide for the old mixed revision line | Three separately identifiable statements: incremented revision identifier; current released version pair per existing product; designation of content beyond as target architecture | l. 5 `Handbook revision: Version 1.2 · August 2026`; l. 6 `Shipped baseline: aSPARK Core v0.7.0 · aSPARK-graph v0.7.0`; l. 7 `Scope designation: Content beyond the shipped baseline stated above is target architecture.` — three distinct statements; `Version 1.1` count document-wide = 0 | ✅ pass |
| AC-3.2 | Checked where a reader can discover the judging baseline without opening chapters | Baseline discoverable from title page or overview table alone | Pairs sit on the title page (l. 6); the legend (l. 70) judges every stage against "the shipped baseline named on the title page" — discoverable from page 1 without opening any chapter | ✅ pass |
| AC-3.3 | Verified the documented spot-check exists and binds: evidence.md T3 (sources actually read, confirmed-pairs table, ~19-row per-item inventory table, zero contradictions filed); compared title-page pairs to T3's confirmed values byte-wise; independently re-verified both releases myself | Version pairs updated only after a documented build-phase spot-check walking chapter prose against current releases; no baseline claimed without a corresponding release | T3 spot-check fully recorded with sources and per-item outcomes; title-page pairs byte-equal to T3's confirmed values; my own checks confirm both releases exist today (Core v0.7.0 → `753a793` locally; graph v0.7.0 highest remote tag) | ✅ pass |
| NFR-1 | Value-set check across all labels and table stages; searched for any second definitional passage of the taxonomy | Only the three taxonomy values appear as chapter stages; each defined exactly once | Stage values everywhere ∈ {Shipped, Partial, Planned}; definitional passage exactly once (legend l. 70; `Shipped —`/`Partial —`/`Planned —` definitions occur nowhere else) | ✅ pass |
| NFR-2 | Release verification: local `git rev-parse v0.7.0^{commit}` (Core) and `git ls-remote --tags` (graph); cross-checked title page against these | At merge, title page states each existing product's current released version; updated only after the documented pre-bump spot-check; no baseline without a release | Both pairs correspond to real releases verified by me on 2026-08-25; bump gated by the recorded T3 spot-check (AC-3.3); no other baseline asserted anywhere in the additions (delta whitelist) | ✅ pass |
| NFR-3 | `git status --porcelain`; `git diff --stat`/full ROADMAP diff; repo-tree scan for leaked scripts | Diff confined to docs/ content + ROADMAP bookkeeping + .spark trail; no executable code/script/build step added | Working-tree changes exactly `M ROADMAP.md`, `M docs/…docx`, `?? .spark/handbook-maturity/`; ROADMAP diff = precisely the two recorded edits (Shipped row l. 40; #18 follow-up entry l. 52–56); harness stayed in /tmp — nothing committed to the repo tree | ✅ pass |
| NFR-4 | My own extraction IS the test: presence of all labels, legend and full table in plain text; dry-run recording present in evidence.md | Every stage label, the legend and the full overview table survive plain-text extraction; change exercised by a documented dry run | All 17 labels, legend, complete 17-row table present and correct in my extraction (AC-1.x/2.x rows above); dry-run transcript recorded in `.spark/handbook-maturity/evidence.md` and validated under AC-2.3 | ✅ pass |
| NFR-5/6/7 | Spec marks these N/A (static document — no runtime, no secrets/surface, nothing to observe) | n/a | Confirmed N/A with reason per spec §5; nothing observed contradicting it (content-only diff to an already-public tracked file) | ✅ n/a |

## 3. Exploratory Findings

Beyond the ACs: duplicate-insertion probes, blank-line/extraction-artifact scans, TOC interaction, marker corruption hunt, ROADMAP truthfulness, release-existence probes, and an independent redo of the prose-freeze delta audit.

**Delta audit (redo, builder-independent):** my own diff of HEAD-side vs working-tree extractions shows exactly **8 removed** lines (the v1.1 mixed revision/baseline line + the 7 old Maturity values — whitelist-exact) and **101 added** lines, every one classified into the manifest layer: 17 labels + 1 legend + 31 `Status.`-lines (18 markers + 13 table pointer cells) + 37 further table cells (3 header + 17 chapter names + 17 stage cells + 4 roadmap pointers = the corrected "51 data cells + 3 header") + 3 title statements + 7 relabel values + 1 spacer. Zero prose words changed beyond the manifest. Matches T7's and review's counts, now reproduced from scratch on my own extractions.

**Positive probes (no findings):**
- Duplicate scan over all non-blank lines: duplicates are exclusively legitimate repeats (table chapter-name/stage/pointer cells ×13–17, pre-existing YAML sample keys). Legend ×1, header cells ×1, each chapter label ×1, each product row ×1.
- Blank-run profile new `{1:75, 2:1, 3:1}` vs HEAD `{1:74, 2:1, 3:1}` — exactly the one inserted spacer; no doubled-blank or empty-run artifacts introduced.
- All 33 `Status.`-led paragraphs read as complete sentences; 20 bold-led (18 new markers + 2 kept originals), the 13 non-bold being table pointer cells — shape-consistent throughout.
- ROADMAP.md truthfulness: Shipped row wording matches what I verified in the document point for point; links point at issue #18 (real, open), not lean-rounds' PR #23; the "Close the handbook honesty exception" follow-up entry present and correctly scoped as post-release.
- F2's stale-version citations observed present in my extraction (l. 240, 454, 901) and untouched — consistent with the recorded waive/route; not re-adjudicated here.

| # | Severity | Steps to reproduce | Expected vs. observed | Status |
|---|---|---|---|---|
| E1 | Minor | Open `.spark/handbook-maturity/evidence.md`, section "AC-2.3 dry run — transcript". Plan T7's DoD lists "(material shown, answers, elapsed time, verdict)". | Expected: a measurable elapsed time. Observed: qualitative only ("well under 2 minutes of reading"); material shown, answers and verdict are recorded. Substance unaffected — QA validated the material and both answers independently (AC-2.3 row). | accepted |
| E2 | Minor | `textutil -convert txt -stdout docs/aSPARK_Enterprise_Architecture_Handbook.docx \| sed -n '11,20p'` — the overview table renders directly beneath the bare "Contents" heading (extraction l. 11 → table l. 15), because the Word TOC field contributes no extractable entries and the manifest anchored the insert before ch. 1. | Expected (spec): table before chapter 1 — met. Observed risk: a skimming reader may initially take the maturity table for the table of contents. Cosmetic/placement only; adjudicate visually at the release open-in-Word check. | accepted |
| E3 | Minor | Documentation-count color in two prior artifacts, measured against my shared-hash extraction: (a) evidence.md "Ch. 9 names aSPARK-policy ×3" — actual 4 occurrences on 3 lines (l. 676 ×1, l. 702 ×2, l. 738 ×1); (b) review.md F2 cites stale-prose lines as l. 241/455/902 — actual l. 240/454/901 (+1 each). | Expected: counts/cites reproducible as written. Observed: off by counting-method or ±1; conclusions (deliverer named; stale prose exists) unaffected. Not a re-adjudication of any closed finding — recorded so future readers don't chase phantom line numbers. | accepted |

## 4. Console & Network

N/A by method — the work object is a static binary document: there is no runtime, hence no console, and the document issues no network requests. The only network activity in this QA run was test tooling (`git ls-remote --tags` against github.com to verify the graph release), which is instrumentation, not app traffic. Nothing to report on either channel; recorded rather than left silent.

## 5. Verdict

Would I demo this to a stakeholder right now? Yes — on the recorded plan §4 method. Every Must criterion passed on steps I performed myself: the ambition/delivery split is legible from the front matter alone (title page separates a real revision identity, v1.2 · August 2026, from two baselines I confirmed exist as releases; the 17-row overview table agrees with the in-chapter labels 17/17; the sharp "zero chapters Shipped" result falls out of the document mechanically), all 18 inline markers sit next to the actual claims they qualify, chapter 3's column now distinguishes the two shipped products from five that do not exist, and the dry run's recorded answers validate against my own extraction. The prose freeze survived a third independent reproduction, and the container is healthy: 46 parts in HEAD order, all DEFLATE, only `word/document.xml` changed. Remaining human acts are unchanged and out of my scope: the open-in-Word visual integrity check at release, and the already-routed follow-ups (website sync C9/F2; constitution amendment).

---

## ✅ QA GATE

*All boxes checked → `/go-live` may start. Any box open → back to `/increment`, then re-run `/demo-day`.*

- [x] Every Must-story acceptance criterion verified in the real browser and passed *(override per plan §4, recorded in header/Input: verified by QA's own fresh extraction + package inspection + dry-run validation — US-1/US-2/US-3 ACs all pass)*
- [x] Every browser-observable NFR verified and passed *(NFR-1–NFR-4 verified by the same channel; NFR-5/6/7 N/A per spec §5; no lens carries browser-observable checks for this feature)*
- [x] No open Blocker or Major bugs (Minor bugs listed and accepted by the user) *(none found; E1–E3 are documentation-grade notes listed in §3 with status accepted — visible to the user in this report for objection)*
- [x] Browser console free of errors on the tested flows *(N/A by method — no console exists for a static document; §4)*
- [x] Tested on all agreed viewports *(none agreed — no UI; design review N/A per spec §8; rendered-document visual check reserved for the release open-in-Word step per plan §4)*
- [x] Status set to `passed`
