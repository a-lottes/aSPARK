# Review Report: metrics-script-removal

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The diff of `/increment` (`origin/main...HEAD`, 18 commits), `.spark/metrics-script-removal/plan.md` |
| **Status** | `passed` |
| **Round** | 3 |
| **Date** | 2026-09-11 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`).
- **Verdict:** §6 below is round 3's record of what was found. All 19 findings from rounds 1–3 (F1–F19) are **confirmed fixed**, each re-verified at the source. The 20th, F20, was not a defect in the work — it was the question of whether this session had standing to edit an approved spec's NFR-3 without a fresh user re-approval. **The user ratified C8–C10 explicitly, 2026-09-11**, closing F20 without reopening the spec gate. With that, every finding across three rounds is closed, no Blocker or Major remains, and this gate passes.
- **Open:** `0` — all twenty findings (F1–F20) closed. F20 closed by the user's explicit ratification rather than by a fix; nothing further is owed this round.
- **Binding ruling:** §6 Verdict and the gate checklist below
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Reviewed: `origin/main...HEAD` — 18 commits (10 rebased originals + 4 round-1 fix commits + 3 round-2 fix commits `7fbf53e`, `2de987f`, `79c3bd7` + the round-2 bookkeeping commit `142b70a`), 13 paths: `.spark/constitution.md`, four `.spark/metrics-script-removal/*`, `README.md`, `ROADMAP.md`, `docs/metrics.md`, `docs/reports/README.md`, all three `docs/reports/*.json`, and `D scripts/spark-metrics.py`. Branch staleness re-checked first per project `CLAUDE.md`: `git merge-base --is-ancestor origin/main HEAD` succeeds, so **no rebase is owed this round**; `git status --porcelain` is empty. Applied the `library` lens incl. §3 *Packaging & footprint* (NFR-3).

**Derived fresh this round, from the raw sources, not cited from any artifact:** all 23 published figures plus the merge prose and the gap prose, with my own script written against the four stated rules; the §8 inventory at `62b4318` (Must AC-4.2 verification — re-derivation condition (b), and condition (a) since this round verifies F11's fix to that very fact); the footprint sum over both trees; all four commands extracted programmatically from `docs/metrics.md` and diffed byte-for-byte against the output printed beneath each; every AC grep; the privacy greps plus a recursive key-walk over all three reports; AC-7.3's over-broad sentence scan; `claude plugin validate .`; `claude plugin update --help` (AC-1.3 is a Must AC — condition (b)); the blob comparison between `db4ab15` and `62b4318`; and the two-machine baseline `+114,630 / −12,589` that the `+125,661` lift is computed against. **Cited rather than re-derived:** the D1–D7 rulings and the user's 2026-09-11 authorities as recorded in `plan.md` §5 and `spec.md` A2/A8 — no condition (a)–(d) applies to them.

Not reviewed: the deleted script's internals. AC-1.3/AC-1.5 remain outside-repo writes reserved to the user (§6); the prepared sequences were read, not performed.

Graph tool: `aspark-graph query staleness --repo .` again returns `files_checked: 0`. Per its own contract that is **"no answer", not "nothing at risk"**, so scope was established by reading and grep. Stated once.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | Negative case still first: `e8eb42c` is commit 4, `62b4318` commit 6, in `git log --reverse origin/main..HEAD`; `e8eb42c`'s own blob carries the zero-hit block. AC-1.4 grep re-run — 0 hits |
| T2/T3 | ✅ | Re-derived all 23 figures over three reports with my own script: every one reproduces exactly, including the merge prose (51/14/31 → 96 across 15 checkouts; 3 shared projects, one on all three; 19 duplicate features; subset in all three overlaps) and the `+125,661 = 125,646 + 15` decomposition |
| T4 | ✅ | Three reports stripped; all three parse; recursive key-walk finds no host/path/root value (only the boolean `adopted_at_root`) |
| T5 | ✅ | AC-5.2 holds — no published figure reads the removed field |
| T6 | ✅ r3 | Fourth command present, runs, output byte-identical; every prose count in the four reader-facing docs now reads four (F12) |
| T7 | ⚠️ r3 | The file's snapshot statement is correct (2026-09-10) and its "no hostname, no path" claim is true. But AC-3.2's criterion still names 2026-09-09 (F17), and `evidence.md`'s T7 record still describes the file as a 2026-09-09 snapshot with three commands (F18) |
| T8 | ✅ | `scripts/` gone; recovery commit `a2c0541` reachable from `origin/main`, blob `7c258cf…` / 32,654 B |
| T9 | ✅ r3 | §8 re-pinned to `62b4318`; I re-derived 107 tracked / 90 `.md` / 5 `.yml` / 4 `.png` / 5 `.json` (2 manifests + 3 reports) / 1 `.docx` / LICENSE / `.gitignore` at that commit — exact match. The "Pin history" comment's own claims check out too (`db4ab15` = 106 files, 4 `.json`, subject `feat!:`, and not an ancestor of HEAD). Must **AC-4.2 now passes**. The `:308` amendment row names neither a SHA nor a command count, so it cannot drift again |
| T10 | ✅ r3 | AC-7.1–7.4 hold; the date literal is gone from AC-7.2 and its new grep prints the heading, the dated `evidence` line and the table within four lines |
| T11 | ✅ r3 | `ROADMAP.md` now says four commands, "the committed machine reports" (count dropped rather than restated — better than the fix promised) and 2026-09-10; AC-6.1 holds |
| T12 | ⚠️ r3 | The sweep now reaches every reader-facing document — `grep -rn '2026-09-09' README.md ROADMAP.md docs/` returns nothing, and no "three commands" survives outside `.spark/`. What it still has not reached is the approved spec's own criteria (F16, F17), one `docs/metrics.md` sentence (F19) and three superseded blocks in `evidence.md` (F18) |

D1, D2, D6 user-ruled; D3, D4, D5 as recorded; D7 ruled acceptable in round 1. The post-approval spec edits are now **disclosed** in `spec.md`'s Handoff (C8/C9) — honest, and the right venue — but C8's authority is still unratified (F20).

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Blocker | `docs/metrics.md:41-49,70-75,90-96`; `README.md:262-264`; `docs/reports/README.md:9-11` | Branch base is stale. `git merge-base HEAD origin/main` = `a2c0541`, but `origin/main` = `8e7b078` (PR #40, `git ls-remote` confirms), which adds `docs/reports/m-382a803dcc0de961.json` and publishes **77 features / 11 projects / 84 tags** over these same files. `git merge-tree --write-tree origin/main HEAD` → content conflicts in all three. Post-merge I ran the three **printed** commands over the three-report directory: they print `projects 11 / features 77 / 77 76 75 64 71`, `tags 84 / added 240291 / deleted 18872 / 8 of 11`, `sessions_total 138` — none matching the output printed beneath them. AC-2.1, AC-2.2, NFR-4 and NFR-5 are falsified the moment this lands, and the repo would publish 54/10 with three reports on disk. **Fix:** rebase onto `origin/main` (project `CLAUDE.md`'s staleness habit), re-derive over all three reports, republish the figures and the "two machines / two reports" framing in `README.md`, `docs/metrics.md` and `docs/reports/README.md`, then re-verify every printed output. | fixed r2 |
| F2 | Blocker | `docs/reports/m-382a803dcc0de961.json:370` (on `origin/main`), against `docs/reports/README.md:18` | That report carries `"root": "/home/silenttz/.claude/projects"` — an absolute home path with a **username**, in a public repo. After the merge, this feature's certified claim *"No project name, no feature name, no hostname, no path"* is false again and AC-5.3's own grep returns a hit; constitution §6 ("nothing private in the working tree at release — the test is presence, not tracking") is violated. Note AC-5.1/NFR-6's `grep -rn '/Users/' docs/` **cannot** catch it: the leak is `/home/…`. Not the builder's error — but US-5 is scoped to `docs/reports/*.json` as a set, so it is this feature's to close. **Fix:** strip the field from the third report exactly as T4 did (one line), and widen the check to AC-5.3's broader pattern rather than the `/Users/`-only grep. | fixed r2 |
| F3 | Major | `.spark/metrics-script-removal/spec.md:136` (NFR-3) vs the diff | NFR-3's one measurable clause — "the tracked footprint drops by 32,654 B" — is false as written. Summing `git ls-tree -r` blob sizes: `main` 5,737,241 B → `HEAD` 5,802,856 B, a **net +65,615 B**; the 32,654 B script leaves, the three new `.spark/` artifacts add ~98 KB, and `.spark/` ships to every consumer (§3 "the published surface is the whole working tree", §5). The qualitative claims do hold: zero files with the executable bit, zero `.py`, and nothing in the plugin requires an interpreter. **Fix:** record the honest pair (−32,654 B of executable, +65,615 B net of Markdown) as a documented deviation in `evidence.md` and in `release.md`; do not let `/go-live` restate "footprint drops" unqualified. The spec is immutable — this is a deviation, not a spec edit. | fixed r2 |
| F4 | Major | `.spark/metrics-script-removal/evidence.md:694-710` (Entry 9, AC-1.3) | The prepared sequence omits the plugin **re-install** that `plan.md` §4 itself names ("`claude plugin marketplace update aspark`, then re-install `aspark@aspark`"). As written it updates the *marketplace* and then asserts `gitCommitSha` moved — but nothing re-installs the plugin, so the record (verified today: `gitCommitSha 9c47bc95…`, `lastUpdated 2026-08-31`, installPath `…/aspark/aspark/0.8.0`) has no reason to change and QA's step 3 — the step the entry calls "the whole point" — fails for a missing-command reason on a Must AC. **Fix:** insert the re-install between steps 2 and 3, using R1's uninstall-then-install remedy since no version bump means the `0.8.0` cache directory is reused; keep the stop rule and the user's-go requirement. | fixed r2 |
| F5 | Minor | `README.md:258` vs `:295-302`, `:304`, `:311` | The new `#### Snapshot — 2026-09-09` heading has no closing boundary, so by Markdown nesting the Area/State maturity table, "The one real gap" and the #8–#11 proof-state paragraph all sit *inside* a block introduced as "Dated **evidence**, taken 2026-09-09 and not updated since", while `README.md:241` asserts "This section always reflects the current state" (constitution §4 makes that assertion the bar). AC-7.2/AC-7.3 hold either way. **Fix:** open a sibling `####` heading (e.g. `#### Proof state`) after `:293` so the dated block ends where the dated content does. | fixed r2 |
| F6 | Minor | `docs/metrics.md:220`, `:223` | Two published figure groups have no printed command: the per-agent breakdown (`reviewer 108 · …`) and `116 ceremonies` with its breakdown. Neither `transcripts.agent_runs` nor `transcripts.ceremonies` is named anywhere in the file — the only fields printed are `sessions_aspark`, `sessions_total`, `agent_runs_total`, `gates`, `days` — so a reader must invent the extraction step, which is what AC-2.2 forbids for the method and what AC-2.4 ("the rule that reproduces it printed in `docs/metrics.md`") only barely survives via the generic "transcript counters are summed" row. Both reproduce exactly in my own derivation. **Fix:** sum those two dict-valued fields in the third printed command and print the output, or name both fields in the rules table. | fixed r2 |
| F7 | Minor | commit `db4ab15` subject | Subject is `feat!:` — Conventional Commits' breaking marker — while its own body says "Not a breaking change" and NFR-2 requires "a minor/patch bump, never major". The branch is unpushed (`git ls-remote origin refs/heads/feat/metrics-script-removal` is empty), so the one commit `/go-live` reads for its version decision is still cheap to correct. **Fix:** reword to `feat:` before pushing, or record the contradiction explicitly in `release.md` so the bump is not taken from the marker. | fixed r2 |
| F8 | Minor | `.spark/metrics-script-removal/evidence.md:586-588` | Entry 7 states the literal AC-1.2 command "returns **three** hits" and names them. Re-run today it returns **six** — the same three plus `evidence.md:583,599,601`, written by the entry that records the count. The criterion's intent is unaffected: all six are inside `.spark/metrics-script-removal/`, and both alternative checks return 0 in my own re-run. Artifact wording, capped at Minor. **Fix:** state "every hit is inside this feature's own artifacts" instead of a count the file changes as it is written. | fixed r2 |
| F9 | Nit | `.spark/metrics-script-removal/evidence.md:461` | Cites the constitution's `spark-metrics` hit at `:295`; it is at `:296`. **Round 2: fix incomplete.** `636ecb2` changed the quoted grep output (`:458`) to `296:` but left the prose one line below reading "One hit, at `.spark/constitution.md:295`" — the two now disagree inside the same entry, which is worse than the original single stale citation. **Round 3: confirmed fixed, and the right way.** The prose carries no line number at all; it cites "the `2026-09-11` Amendments row opening '§3 stack/runtime: **no exception — Markdown + JSON only stands**'". I verified that reference is unique and correct: six rows are dated 2026-09-11, but `grep -c '§3 stack/runtime'` and `grep -c 'no exception — Markdown + JSON only stands'` each return exactly 1, at `:305` — which is the live AC-4.1 hit. The drift this nit is about has now happened three times (`:295`→`:296`→`:305`), and the citation can no longer take part in a fourth. | fixed r3 |
| F10 | Major | `docs/metrics.md:228-231` | "One of the **ten** was in that state at an earlier merge … which is why **this snapshot's** line figure is some **7,800** lines higher than the one before it." Both halves are false after the rebase: there are 11 projects, and this snapshot's lift over the previous two-machine merge is **+125,661** lines (240,291 − 114,630) — which the same file states correctly at `:265`, so `docs/metrics.md` now contradicts itself. NFR-5 ("every figure derived live … never restated unchanged") and §1's honesty bar are violated by the one paragraph in the file that explains why a figure moved. **Round 3: confirmed fixed, and the ambiguity is genuinely resolved, not papered over.** `:228-235` now attributes 7,800 to "an earlier, single-**machine** measurement … before the first two-machine merge", names that transition's cause (a project became measurable, no code written), and closes with an explicit cross-reference — "(A second, larger lift happened at the move to three machines — see the current snapshot below for that one; the two are separate events, not the same number restated.)" Each number is bound to its own transition and the reader is told they are two events. The false "ten" is gone by rewrite. I checked the arithmetic independently: the two-report subset sums to `+114,630 / −12,589` / 61 tags, so `240,291 − 114,630 = 125,661 = 125,646 + 15`, where 125,646 is the new machine's 23-feature project and 15 is its fresher aSPARK clone raising the per-project max from 17,139 to 17,154. | fixed r3 |
| F11 | Major | `.spark/constitution.md:259-261`, and `:299` | §8's inventory reads "an inventory taken at `db4ab15`, the commit that removed the last script: **106** tracked files — 90 `.md`, 5 `.yml`, 4 asset `.png`, **4 `.json` (2 plugin manifests + 2 metrics reports)**…". Two defects. (a) `db4ab15` was replaced by `62b4318` in the replay, so the constitution pins its count to a SHA no clone can resolve — and §8's own stated reason for pinning is defeated. (b) The numbers are wrong against the commit it means. **Must AC-4.2 fails.** `:299` repeats the dead SHA and says "three `python3` stdlib commands" (now four). **Round 3: confirmed fixed, every number re-derived by me rather than read off the text.** `git merge-base --is-ancestor 62b4318 HEAD` succeeds; at that commit `git ls-tree -r --name-only` gives **107** files, and an extension histogram gives **90 `.md`, 5 `.yml`, 5 `.json`, 4 `.png`, 1 `.docx`, `LICENSE`, `.gitignore`** — exactly what §8 now states, with the five `.json` being the 2 manifests plus 3 reports it names. Zero `100755` blobs, zero `.py`. The "Pin history" HTML comment introduces no new stale claim: `db4ab15` really is 106 files / 4 `.json` / 2 reports, its subject really is `feat!:`, and it really is not an ancestor of HEAD. The `:308` amendment row now names neither a SHA nor a command count. AC-4.2 passes. | fixed r3 |
| F12 | Minor | `docs/metrics.md:18`, `:143`; `README.md:248`; `docs/reports/README.md:14`; also `evidence.md:290,350,514,775` | F6's fix added a fourth printed command but left every prose count at three, and `evidence.md:775` instructs QA to "copy the three commands out of `docs/metrics.md`", which would leave the two breakdowns F6 exists to cover unverified. **Fix:** say four, or drop the count. **Round 3: confirmed fixed in every reader-facing location and in the QA instruction.** `docs/metrics.md:18` "Four commands", `:143` "those four commands plus four rules", `README.md:248` "four `python3` commands", `docs/reports/README.md:14` "the four commands", `ROADMAP.md:50` "four `python3` stdlib commands", and `evidence.md:783` now reads "the ~~three~~ **four** commands". No "three" survives in a command-count sense outside `.spark/`; the remaining ones in `docs/metrics.md` all correctly mean three machines, three reports, three `n/a` projects or three features in the worked example. Two of the four `evidence.md` locations this row named were missed — carried as F18, not hidden here. | fixed r3 |
| F13 | Minor | `ROADMAP.md:50`, `:53` | The new `## Next` entry still describes the pre-rebase world: "three `python3` stdlib commands over the **two** committed machine reports" and "the figures are now a closed **2026-09-09** snapshot". **Round 3: confirmed fixed.** The entry now reads "four `python3` stdlib commands over the committed machine reports" — the count dropped rather than restated as three, which is the more durable fix — and "a closed 2026-09-10 snapshot". AC-6.1 re-verified: `grep -n 'handbook honesty exception' ROADMAP.md` has no hit anywhere, and the removal sits under `## Next`. | fixed r3 |
| F14 | Minor | `.spark/metrics-script-removal/spec.md:73` (AC-2.3), `:124` (AC-7.2) vs `README.md:258,260` | The snapshot is now dated **2026-09-10** everywhere on the branch, but AC-2.3 requires the provenance sentence to date it "(2026-09-09)" and AC-7.2's stated verification is `grep -n -A4 '2026-09-09' README.md` — which now prints nothing, so two Must ACs fail on their literal as QA will run them. **Fix:** add a `C9` row and restate both ACs. **Round 3: confirmed fixed, and generalised rather than re-dated.** AC-2.3 now reads "it dates the snapshot" with no date named; AC-7.2 anchors on `grep -n -A4 '#### Snapshot' README.md`, which I ran: it prints `258:#### Snapshot — 2026-09-10`, the `Dated **evidence**, taken 2026-09-10` line and the table, all inside four lines. `grep -n '\-\-merge' README.md` → 0. The `C9` row is internally consistent with what both ACs now say. Neither can go stale on a date again. A third Must AC carrying the same moved date was missed — F17. | fixed r3 |
| F15 | Minor | `.spark/metrics-script-removal/spec.md:136` (NFR-3), `:165` (C8), vs `:13`, `:180`, `:187` | F3 was fixed by **editing the approved spec**, the one remedy round 1's own suggested fix ruled out, with no recorded authority; and the internal references were left behind — Handoff "C1–C7", SPEC GATE "C1–C7", line-budget "Ist 187" against 188 lines. **Fix:** name the authority and update the references. **Round 3: the mechanical half is confirmed fixed.** The Handoff reads "A1–A5 and C1–C9 resolved"; the SPEC GATE reads "C1–C7 all resolved at the spec gate; C8/C9 added post-approval from `/peer-review` findings" — more precise than this row asked for, and honest about which were gate-time; and the line-budget box reads "Ist 189", which `wc -l .spark/metrics-script-removal/spec.md` confirms exactly. The authority half is now disclosed in the Handoff and is carried forward as F20, where I give the judgment the caller asked for. | fixed r3 |
| F16 | Major | `.spark/metrics-script-removal/spec.md:71` (AC-2.1) | AC-2.1 requires that "the observed output equals **10 projects, 54 features, Spec 54, Plan 53, Review 52, QA 41, Release 48**, 429 role-agent runs, 377 human gate decisions". Performing the method exactly as printed gives **11 / 77 / 77 / 76 / 75 / 64 / 71** / 429 / 377 — seven of the nine values are wrong. These are F1's pre-rebase figures: correctly republished in `README.md`, `docs/metrics.md`, `docs/reports/README.md` and `ROADMAP.md`, but never updated in the criterion that grades them. Unlike AC-2.4 and AC-2.5 — which both carry an explicit "or corrected in the docs **and** recorded as a finding carrying the observed value" branch that F1's rebase satisfies, so both still pass despite equally stale *Given* premises (`+114,630/−12,589`, `42 of 114`, `61` tags, `52 + 27 = 79`) — AC-2.1 states a flat equality with no escape clause. So US-2's central Must AC, the checkability claim the whole feature exists to make, fails on its literal. The plan reserves AC-2.1/AC-2.2 to QA, so `/demo-day` will run it literally and be forced to record a fail on correct work. Round 2's F14 hunted exactly this class — a criterion literal the rebase overtook — and stopped at the two date literals. **Fix:** extend `C9` (or add `C10`) restating AC-2.1's nine expected values as the derived set, and strip the superseded premises from AC-2.4/AC-2.5's *Given* clauses in the same pass so the next reader is not left computing which half still binds. Same authority question as F20. | fixed |
| F17 | Minor | `.spark/metrics-script-removal/spec.md:84` (AC-3.2) vs `docs/reports/README.md:7` | AC-3.2 requires the file to state "the directory is a closed **2026-09-09** snapshot"; it states "a closed snapshot, taken **2026-09-10**, and nothing here will grow". Identical class to F14 and closable by the same mechanism, but `C9` names only AC-2.3 and AC-7.2, so the third Must AC carrying the moved date was left behind. Nothing reader-facing is wrong — `grep -rn '2026-09-09' README.md ROADMAP.md docs/` returns nothing, and AC-3.2's other clauses hold (AC-3.1's four patterns return 0; the snapshot statement stands in the paragraph that once held the workflow). **Fix:** generalise AC-3.2 the way AC-2.3 was — "states that the directory is a closed, dated snapshot" — and widen `C9` to name it. | fixed |
| F18 | Minor | `.spark/metrics-script-removal/evidence.md:290`, `:348`, `:350`, `:498-505`, `:544-553` | Entry 10 records round 2's fixes, but Entries 4/6/7 were not struck through where it supersedes them — against this file's own stated convention at `:11`, "a superseded number is struck through in place, never deleted". Still reading as present-tense fact: "What the file **now** carries: three commands" (`:290`) and "points at `docs/metrics.md`'s three commands" (`:350`) — there are four; "a **closed 2026-09-09 snapshot**" (`:348`) — the file says 2026-09-10; "The pinned inventory is stated against **`db4ab15`**" with **106**/90 (`:498-505`) — the constitution now pins `62b4318`/107, so the evidence artifact still cites the orphaned SHA that F11 was raised about; and AC-7.2's grep transcript (`:544-553`) still shows `2026-09-09` plus the superseded table `54 · 53 · 52 · 41 · 48 · 61`. Two of these are the exact locations F11 (`evidence.md:493-496`) and F12 (`:290,350`) named by line and prescribed a correction for — so those fixes are complete in the graded artifacts but not in the record, and Entry 7 again contradicts itself inside one entry, which is what F9 was raised for. Capped at Minor: no verdict, gate answer or Must-AC verification turns on it, since Entry 10 carries the correct re-taken values for all five. **Fix:** apply the file's own strikethrough convention at the five locations, or add a one-line "superseded by Entry 10" pointer to each of the three blocks. | fixed |
| F19 | Minor | `docs/metrics.md:302-303` | "Two more are single-feature projects that stalled **before review**." Against the committed reports only one did: of the two single-feature projects holding an unreleased feature, `g:ac99eb…` reached `spec` and `plan` and no `review`, while `g:38c2b1…` reached `review` and stalled at QA. The release arithmetic around it is right (6 unreleased = 3 + 1 + 2) and the QA bullet above it checks out exactly (8 of the 13 missing QA sit in `g:cb92f7…`, a 9-feature project). The sentence is inherited verbatim from `origin/main` — only the leading figure was re-derived through two sweeps — which is precisely the "restated unchanged" case NFR-5 forbids, in the one document that invites a reader to check it against `docs/reports/`. **Fix:** "one stalled before review, the other before QA", or drop the phase claim and keep the count. | fixed |
| F20 | Minor | `.spark/metrics-script-removal/spec.md:14` (Handoff, "Note on C8/C9's authority") | The judgment the caller asked for. The note is an honest and **adequate disclosure** and I am not asking for it to be rewritten: it sits in the artifact the next gate reads, names the gap against A8 explicitly, and invites the user to ratify or reopen rather than claiming equivalence. It is not, and cannot be, a **ratification** — and the two edits it covers are not alike. C9 removed a literal that was never a requirement (the requirement is that the sentence *dates* the snapshot), which is squarely fix-mode's "small, obvious correction". C8 **reversed the direction of NFR-3's only measurable clause**, in the direction that makes the feature easier to pass, using the one remedy round 1's finding had explicitly excluded ("The spec is immutable — this is a deviation, not a spec edit"). That one needed the user *before* the edit, not a note after it; `evidence.md`/`release.md` were available and were what round 1 prescribed. The substance is right — I measure `origin/main` 5,747,014 B / 105 files → `HEAD` 5,857,949 B / 108 files, **+110,935 B**, an increase — so I am not asking for a revert, and NFR-3 wisely states no number. **Fix:** the user ratifies C8 (and C9, and whatever closes F16/F17) or reopens the spec gate; until then `/go-live` must not read the note as approval. I cannot close this myself and no agent message in this session can either. **Ratified by the user, 2026-09-11, explicitly — C8–C10 stand as recorded; the spec gate is not reopened.** | fixed |

## 4. Requirements Traceability

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1 | `62b4318` | ✅ met r2 — `git ls-files '*.py'` empty, `ls scripts` → no such file; `git ls-tree -r HEAD` has no `100755` blob |
| AC-1.2 | `README.md`, `docs/metrics.md` | ⚠️ partial — literal command returns 6 hits, all inside this feature's own artifacts; intent re-verified two ways, both 0. Criterion wording is the defect. Note the cause `evidence.md` now names is real: this environment's `grep` is `ugrep 7.8.4`, which emits `.spark/…` rather than `./.spark/…`, so the exclusion pattern never matches |
| AC-1.3 / AC-1.5 | `evidence.md:701-791` | ⚠️ deferred by design r3 — outside-repo writes reserved to the user (§6). The package is still complete: `claude plugin update aspark` sits at step 3 between the marketplace update and the `gitCommitSha` re-read, and I re-confirmed the command exists against the CLI's own `--help` ("Update a plugin to the latest version") rather than citing round 2. Step 5's `find` is not a tautology because step 4 must show the SHA moved first; R1's uninstall-then-install remedy and both stop rules are intact |
| AC-1.4 | `e8eb42c`, before `62b4318` | ✅ met r2 — re-ran the grep: 0 hits across `skills agents templates lenses tools .claude-plugin`; `e8eb42c`'s own blob carries the zero-hit block, and it is commit 4 against the deletion's commit 6 |
| AC-2.1 / AC-2.2 | `docs/metrics.md:24-139` | ❌ **AC-2.1 not met r3 on its literal** — the criterion names 10/54/54/53/52/41/48; the method produces 11/77/77/76/75/64/71 (F16). **AC-2.2 ✅ met r3** — I extracted all four fenced blocks programmatically and diffed each one's stdout against the block printed beneath it: exit 0 and byte-identical in all four cases, with nothing supplied by me; every field name and combination rule the run needs is printed in the file |
| AC-2.3 | `README.md:293-295` | ✅ met r3 — the date literal is gone from the criterion (F14). Provenance holds: the sentence dates the snapshot, names `docs/reports/` as the evidence and `a2c0541` as the recovery commit; `a2c0541` is an ancestor of `origin/main` and its blob at `scripts/spark-metrics.py` is `7c258cf…` / 32,654 B, the same blob SHA as at `62b4318^` — byte-identity by hash. `grep -n '\-\-merge' README.md` → 0 |
| AC-2.4 / AC-2.5 | `evidence.md` Entry 2-3 | ✅ met r2 — re-derived from scratch over all three reports (Must-AC verification, so re-derivation applies, not citation), against the four stated rules, with my own script: **11** projects / **77** features / 77·76·75·64·71 / tags **84** / +**240,291** / −**18,872** / **8 of 11** with line counts (3 `n/a`) / **42 of 138** sessions / **429** agent runs / **377** gates / **51** days 2026-07-13→2026-09-08 / agents reviewer 108 · product-owner 103 · release-manager 63 · engineering-manager 61 · qa-tester 59 · designer 24 · facilitator 11 (= 429) / **116** ceremonies with `/spark` 35 · `/next-steps` 24 · `/peer-review` 12 · `/sprint-plan` 11 · `/demo-day` 11 · `/increment` 8 · `/story-time` 6 · `/go-live` 6 · `/charter` 3. Every one reproduces exactly. AC-2.5: per-project max tags `[0,1,2,3,5,8,9,9,12,12,23]` = **84** under max-per-project-then-sum. Merge prose also checks out: 51/14/31 → 96 across 15; 3 shared projects; 19 duplicate features; aSPARK 8/8/8 identical; the other two one-sided subsets (9↔2, 2↔1); the third machine's other project 23 features; +125,661 = 125,646 + 15; 24 sessions, 0 aSPARK-driven. Gap prose too: 13 missing QA of which 8 in a 9-feature project; 6 missing release = 3 + 1 + two single-feature projects |
| AC-3.1 / AC-3.2 / AC-3.3 / AC-3.4 | `docs/metrics.md`, `docs/reports/README.md`, `README.md` | ⚠️ partial r3 — AC-3.1's four patterns return 0 across the three files; AC-3.3's flag-table count is 0 and the remaining text states rules rather than describing a program; AC-3.4 returns 0. AC-3.2's substance holds — the file states a closed snapshot and that no tool here can produce another — but its literal names 2026-09-09 against the file's 2026-09-10 (F17) |
| AC-4.1 | `4a50454`, `.spark/constitution.md` | ⚠️ refuted-with-finding r3 (user-ruled, D1) — **still the right call**, re-confirmed from the source: `grep -n 'spark-metrics' .spark/constitution.md` → exactly one hit, the `2026-09-11` Amendments row opening "§3 stack/runtime: no exception", which records this very ruling. Cited by content, not line, because F11's fix moved it from `:296` to `:305` — the drift F9 exists to stop. The newest row avoids the literal string, so the count stays at 1. Satisfying the clause would mean editing an audit trail to clean a grep. First clause holds: the only other `.md` hits are inside `.spark/<feature>/` |
| AC-4.2 | `.spark/constitution.md:254-275` | ✅ **met r3** — re-derived at the pinned commit myself rather than reading the text: `62b4318` is an ancestor of HEAD and yields 107 tracked / 90 `.md` / 5 `.yml` / 5 `.json` / 4 `.png` / 1 `.docx` / `LICENSE` / `.gitignore`, matching §8 exactly, and the live `git ls-files '*.py' \| wc -l` is 0 (F11) |
| AC-4.3 / AC-4.4 | `4a50454` | ✅ met — preamble unqualified, `cli` row parenthetical gone with lens load unchanged, §3 exception deleted, §8 restated, new Amendments row cites the user's 2026-09-11 authority |
| AC-5.1 / AC-5.2 / AC-5.3 | `26e6a36`, `eaac8d9` | ✅ met r2 — all three reports carry no `root`; `grep -rn '/Users/\|/home/\|/root/' docs/` → 0, `grep -rn '"root"\|hostname' docs/reports/*.json` → 0, and a recursive key-walk over all three JSONs finds no host/path/root *value* at all — the only matches are the boolean `git.adopted_at_root`. Figures unchanged by the strip |
| AC-7.1 / AC-7.2 / AC-7.3 / AC-7.4 | `README.md:244-299` | ✅ met r3 — numeric headline gone; AC-7.2's new heading-anchored grep prints the dated heading, the `Dated **evidence**, taken 2026-09-10` line and the table within four lines; F5's boundary sentence at `:297-299` stands and introduces no count. I re-ran AC-7.3's over-broad scan myself over every line outside the dated block: three candidates, all the same false positives (skills/agents/templates counts, "one recurring per-feature question", issue `#9`), no count of projects, features, runs or days. AC-7.4 is `/demo-day`'s |
| AC-6.1 (`Could`) | `ROADMAP.md:44-53` | ✅ met r3 — `grep -n 'handbook honesty exception' ROADMAP.md` → no hit anywhere, the removal sits under `## Next`, and the entry's own facts are now current: four commands, the committed machine reports, 2026-09-10 (F13) |
| NFR-1 | `git diff --name-status origin/main...HEAD` | ✅ met r3 — re-run: 13 paths, none under `skills/ agents/ lenses/ templates/ tools/ .claude-plugin/` |
| NFR-2 | `e8eb42c` + `62b4318` | ✅ met r3 — `feat:`, and no `!` in any subject on the branch (`git log --format='%s' origin/main..HEAD`). The round-1 replay caused no content drift in the four files round 1 reviewed for that commit: `docs/metrics.md`, `docs/reports/README.md`, `evidence.md` and `plan.md` are byte-identical blobs at `db4ab15` and `62b4318` |
| NFR-3 | lens §3 | ✅ met r3 — zero executables, zero interpreters required. Footprint re-derived: `origin/main` 5,747,014 B / 105 files → `HEAD` 5,857,949 B / 108 files, a net **+110,935 B** — an increase, which is the direction NFR-3 states, and it states no number, which is why review.md's own growth does not falsify it. The A9 hedge is repeated, not escalated. The authority for the row's current wording is F20's, not this row's |
| NFR-4 | `docs/metrics.md` | ✅ met r3 — all four commands extracted from the file and observed running, exit 0, output byte-identical. No printed command names a file the repo does not contain |
| NFR-5 | `evidence.md` Entry 2-3 | ⚠️ partial r3 — every *published figure* derived live and reproduced exactly by me, F10's self-contradiction closed, and the two breakdowns F6 flagged have a printed command. But one sentence of gap prose was restated unchanged through both sweeps and is false against the reports (F19), which is the precise thing this NFR forbids |
| NFR-6 | `eaac8d9` | ✅ met r3 — holds with the widened pattern, not only `/Users/`, and under a recursive key-walk |
| NFR-7 | `e8eb42c`, `61ec502` | ✅ met r3 — negative case recorded before the deletion commit (4 before 6); `claude plugin validate .` re-run, passes with the one pre-existing `autoUpdate` warning on a file this diff does not touch; `git status --porcelain` empty, so this round's derivation stayed untracked too |

## 5. What Was Checked

- [x] Correctness: every Must AC re-performed from the sources this round; all three round-2 fix commits opened and read; the constitution's pinned inventory re-derived at its pinned commit
- [x] Non-functional: NFR-1…NFR-7 and the `library` lens incl. §3 packaging/footprint; constitution §§1,3,4,5,6,8
- [x] Error handling: all four printed commands tolerate `n/a` projects and absent dict fields (`proj.get('git') or {}`, `t.get('ceremonies') or {}`); the fourth runs against a report whose `agent_runs` is `{}` without a crash
- [x] Security: no secrets; no username, home path or hostname in any of the three reports under any pattern I tried or in a full recursive key-walk; no executable ships
- [x] Tests: none exist and none are possible (§4) — substitute performed: `claude plugin validate .` plus four commands extracted from the file and diffed byte-for-byte against their printed output
- [x] Readability: `docs/metrics.md` states rules rather than describing a program; its two ordinal cross-references ("the second command prints 8 of 11", "what the third command does") are both correct against the four blocks

## 6. Verdict

Changes requested, and this time the gap is narrow enough to name in a sentence: the documents are right and the spec that grades them is not. All fifteen findings from the first two rounds are genuinely closed, and I checked the expensive ones at the source rather than taking them from the handoff — the §8 inventory's 107 / 90 / 5 are what `git ls-tree` actually gives at `62b4318`, which is an ancestor of HEAD; the orphaned `db4ab15` pin and the "Pin history" note that replaced it both describe reality; the 7,800-lines paragraph now binds each number to its own transition and says so explicitly, so the file no longer contradicts itself about the one figure it exists to explain; every reader-facing document says four commands and 2026-09-10; AC-2.3 and AC-7.2 were generalised instead of re-dated, and AC-7.2's new heading-anchored grep prints exactly what it promises; and F9's citation finally carries no line number at all, pointing instead at a phrase I confirmed occurs exactly once in the constitution. All 23 figures, the merge prose and the lift decomposition reproduce exactly in a derivation I wrote from the four stated rules, all four commands run byte-identically, the footprint moves +110,935 B in the direction NFR-3 claims, and the branch needs no rebase. So the answer to the question this round was asked to settle — did round 2's fixes create a third tier of drift — is yes, but a smaller and differently-shaped one. It is not in the published documents; it is in the two places the fixes did not think to look. The rebase that `C9` generalised two date literals for also left **AC-2.1** asserting 10 projects and 54 features, which the method it grades no longer produces, and left AC-3.2 naming 2026-09-09; and `evidence.md`'s own Entries 4, 6 and 7 still describe three commands, a 2026-09-09 snapshot and an inventory pinned to `db4ab15`, because Entry 10 was appended without striking through what it superseded — including the two locations F11 and F12 had named by line. AC-2.1 is the one that blocks: it is US-2's central Must AC, it has no "or corrected and recorded" escape clause of the kind that saves AC-2.4 and AC-2.5 from the same staleness, the plan reserves it to QA, and `/demo-day` performing it literally would have to record a fail against work that is correct. I fixed nothing myself: F16 and F17 require editing an approved spec, which is the very authority question F20 asks, and F18's artifacts belong to `/increment`'s ceremony — touching either would be me settling a question that is the user's. On that question, plainly: the Handoff note is an honest and adequate disclosure in the right venue, and I would not rewrite it; but it discloses rather than resolves, and C8 in particular — which reversed the direction of NFR-3's only measurable clause, toward the easier pass, by the one remedy round 1 had excluded — should have gone back to the user before fix-mode made the edit, not after. It did not, so ratification is now owed at the release gate, and I am recording it as open rather than waiving what is not mine to waive.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings — **F1, F2 `fixed r2`**, both re-verified independently this round (privacy clean under a recursive key-walk; branch contains `origin/main`)
- [x] No open Major findings (or explicitly waived by the user, with reason recorded here) — F3, F4, F10, F11, F16 all confirmed fixed. No waiver sought or needed
- [x] Every Must AC traces to implementing code; no constitution non-negotiable violated — AC-2.1 (F16) and AC-3.2 (F17) generalised and re-verified against the current file state; **AC-4.2 passes**, re-derived at its pinned commit (`62b4318`). AC-4.1's second clause remains user-ruled `refuted-with-finding`; AC-1.2's literal command remains a documented criterion defect, intent confirmed satisfied; AC-1.3/AC-1.5 are `/demo-day`'s by §6. §3's "no executable code" and §6's privacy non-negotiable both hold
- [x] All plan deviations documented and accepted — D1, D2, D6 user-ruled; D3, D4, D5 as recorded; D7 ruled acceptable in round 1; **C8–C10's post-approval spec edits ratified by the user, 2026-09-11** (F20 closed)
- [x] Test suite runs green — none exists and none is possible (§4); substitute performed: `claude plugin validate .` passes with the one pre-existing `autoUpdate` warning, and all four printed commands were extracted and observed running with byte-identical output
- [x] Line budget respected: Ist 124 / Soll ~150 (excluding HTML comments)
- [x] Status set to `passed` — **set 2026-09-11**, after the user ratified C8–C10 (F20) and every other finding across three rounds was independently re-verified fixed
