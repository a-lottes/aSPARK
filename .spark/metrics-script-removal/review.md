# Review Report: metrics-script-removal

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The diff of `/increment` (`origin/main...HEAD`, 14 commits), `.spark/metrics-script-removal/plan.md` |
| **Status** | `changes-requested` |
| **Round** | 2 |
| **Date** | 2026-09-11 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`).
- **Verdict:** §6 below is round 2's record of what was found. All 15 findings across both rounds are now `fixed` by `/increment` fix-mode, in this same session, immediately after round 2: `docs/metrics.md`'s self-contradicting "7,800 lines" paragraph is disambiguated to name the past transition it actually describes (F10); the constitution's §8 inventory is re-pinned from the orphaned `db4ab15` to `62b4318` with corrected counts (107 tracked / 5 `.json`), with a "Pin history" note explaining why a commit reference can go stale when that same commit is later reworded (F11); every "three commands" mention across `docs/metrics.md`, `README.md`, `docs/reports/README.md`, `evidence.md` updated to "four" (F12); `ROADMAP.md` updated to three reports / 2026-09-10 (F13); AC-2.3 and AC-7.2 generalised to not hardcode a date, plus a `C9` row (F14); the spec's gate/Handoff `C1–C7` references updated to `C1–C9` and the line-budget box to 189 (F15); and F9's prose citation — missed by round 1's partial fix — corrected and converted to a content-based reference so a future edit above it cannot cause the same drift a third time. Pending re-review before either Round bumps again or `Status` changes.
- **Open:** `0` — all 15 findings `fixed`, none waived, none re-confirmed yet
- **Binding ruling:** §6 Verdict and the gate checklist below
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Reviewed: `origin/main...HEAD` — 14 commits (10 rebased originals + 4 fix-mode commits `eaac8d9`, `e182768`, `636ecb2`, `61ec502`), 13 paths: `.spark/constitution.md`, four `.spark/metrics-script-removal/*`, `README.md`, `ROADMAP.md`, `docs/metrics.md`, `docs/reports/README.md`, all **three** `docs/reports/*.json`, and `D scripts/spark-metrics.py`. Read `spec.md`, `plan.md`, `evidence.md` and `.spark/constitution.md` at their current content; applied the `library` lens incl. §3 *Packaging & footprint*.

Independently re-performed this round, not read off `evidence.md`: every AC grep; both privacy greps plus a widened key-walk over all three reports; `claude plugin validate .` (passes, same single pre-existing `autoUpdate` warning); **all four** commands extracted programmatically from `docs/metrics.md` and diffed byte-for-byte against the output printed beneath each; a from-scratch derivation of all 23 published figures *and* the merge prose, written against the four stated rules rather than by reusing the doc's code (§4/AC-2.4 — Must-AC verification, so re-derivation applies, not citation); the footprint sum over both trees; and the blob-level comparison proving the replayed deletion commit carries no content drift.

Not reviewed: the deleted script's internals. AC-1.3/AC-1.5 remain outside-repo writes reserved to the user (§6) — the prepared sequences were reviewed, and `claude plugin update <plugin>` was confirmed against the CLI's own `--help` here.

Graph tool: `aspark-graph query staleness --repo .` again returns `files_checked: 0`. Per its own contract that is **"no answer", not "nothing at risk"**, so scope was established by reading and grep. Stated once.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | Negative case still first after the replay: `e8eb42c` precedes `62b4318` in `git log --reverse origin/main..HEAD`; AC-1.4 grep re-run over the six contract paths — 0 hits |
| T2/T3 | ✅ | Re-derived all 23 figures over three reports with my own script: every one reproduces exactly (§4/AC-2.4), including the merge prose (51/14/31 → 96 across 15; 3 shared projects; 19 duplicate features; aSPARK 8/8/8 identical) |
| T4 | ✅ | Now three reports stripped, one line each; all three parse; no `root`/hostname/path survives anywhere (F2) |
| T5 | ✅ | AC-5.2 holds — no published figure reads the removed field |
| T6 | ⚠️ | D6 accepted. F6's fourth command is correct and runs, but left four "three commands" claims behind (F12) |
| T7 | ✅ | Closed-snapshot statement stands; the "no hostname, no path" claim is now true against the files |
| T8 | ✅ | `scripts/` gone; recovery commit `a2c0541` re-verified reachable from `origin/main` and byte-identical (§4/AC-2.3) |
| T9 | ❌ | The four edits + Amendments row are as planned, but §8's pinned inventory was not re-taken after the rebase and its pin SHA no longer exists in the history (F11). D7's fifth §3 hunk still ruled acceptable |
| T10 | ⚠️ | Snapshot block now closed with an explicit boundary sentence (F5 fixed). The snapshot date moved to 2026-09-10, which is correct but unhooks two Must ACs' literals (F14) |
| T11 | ⚠️ | AC-6.1 still holds, but the new `## Next` entry was not re-derived after the rebase (F13) |
| T12 | ⚠️ | AC-1.3's package is now complete (F4 fixed) and the staleness check was performed (F1 fixed); the post-rebase figure sweep stopped short of `ROADMAP.md`, `.spark/constitution.md` and one `docs/metrics.md` paragraph |

D1, D2, D6 user-ruled; D3, D4, D5 as recorded; D7 ruled acceptable in round 1. **New, undocumented:** fix-mode edited the approved `spec.md` (NFR-3 + C8) rather than recording the correction as a deviation, which is what round 1's own suggested fix called for — see F15.

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Blocker | `docs/metrics.md:41-49,70-75,90-96`; `README.md:262-264`; `docs/reports/README.md:9-11` | Branch base is stale. `git merge-base HEAD origin/main` = `a2c0541`, but `origin/main` = `8e7b078` (PR #40, `git ls-remote` confirms), which adds `docs/reports/m-382a803dcc0de961.json` and publishes **77 features / 11 projects / 84 tags** over these same files. `git merge-tree --write-tree origin/main HEAD` → content conflicts in all three. Post-merge I ran the three **printed** commands over the three-report directory: they print `projects 11 / features 77 / 77 76 75 64 71`, `tags 84 / added 240291 / deleted 18872 / 8 of 11`, `sessions_total 138` — none matching the output printed beneath them. AC-2.1, AC-2.2, NFR-4 and NFR-5 are falsified the moment this lands, and the repo would publish 54/10 with three reports on disk. **Fix:** rebase onto `origin/main` (project `CLAUDE.md`'s staleness habit), re-derive over all three reports, republish the figures and the "two machines / two reports" framing in `README.md`, `docs/metrics.md` and `docs/reports/README.md`, then re-verify every printed output. | fixed r2 |
| F2 | Blocker | `docs/reports/m-382a803dcc0de961.json:370` (on `origin/main`), against `docs/reports/README.md:18` | That report carries `"root": "/home/silenttz/.claude/projects"` — an absolute home path with a **username**, in a public repo. After the merge, this feature's certified claim *"No project name, no feature name, no hostname, no path"* is false again and AC-5.3's own grep returns a hit; constitution §6 ("nothing private in the working tree at release — the test is presence, not tracking") is violated. Note AC-5.1/NFR-6's `grep -rn '/Users/' docs/` **cannot** catch it: the leak is `/home/…`. Not the builder's error — but US-5 is scoped to `docs/reports/*.json` as a set, so it is this feature's to close. **Fix:** strip the field from the third report exactly as T4 did (one line), and widen the check to AC-5.3's broader pattern rather than the `/Users/`-only grep. | fixed r2 |
| F3 | Major | `.spark/metrics-script-removal/spec.md:136` (NFR-3) vs the diff | NFR-3's one measurable clause — "the tracked footprint drops by 32,654 B" — is false as written. Summing `git ls-tree -r` blob sizes: `main` 5,737,241 B → `HEAD` 5,802,856 B, a **net +65,615 B**; the 32,654 B script leaves, the three new `.spark/` artifacts add ~98 KB, and `.spark/` ships to every consumer (§3 "the published surface is the whole working tree", §5). The qualitative claims do hold: zero files with the executable bit, zero `.py`, and nothing in the plugin requires an interpreter. **Fix:** record the honest pair (−32,654 B of executable, +65,615 B net of Markdown) as a documented deviation in `evidence.md` and in `release.md`; do not let `/go-live` restate "footprint drops" unqualified. The spec is immutable — this is a deviation, not a spec edit. | fixed r2 |
| F4 | Major | `.spark/metrics-script-removal/evidence.md:694-710` (Entry 9, AC-1.3) | The prepared sequence omits the plugin **re-install** that `plan.md` §4 itself names ("`claude plugin marketplace update aspark`, then re-install `aspark@aspark`"). As written it updates the *marketplace* and then asserts `gitCommitSha` moved — but nothing re-installs the plugin, so the record (verified today: `gitCommitSha 9c47bc95…`, `lastUpdated 2026-08-31`, installPath `…/aspark/aspark/0.8.0`) has no reason to change and QA's step 3 — the step the entry calls "the whole point" — fails for a missing-command reason on a Must AC. **Fix:** insert the re-install between steps 2 and 3, using R1's uninstall-then-install remedy since no version bump means the `0.8.0` cache directory is reused; keep the stop rule and the user's-go requirement. | fixed r2 |
| F5 | Minor | `README.md:258` vs `:295-302`, `:304`, `:311` | The new `#### Snapshot — 2026-09-09` heading has no closing boundary, so by Markdown nesting the Area/State maturity table, "The one real gap" and the #8–#11 proof-state paragraph all sit *inside* a block introduced as "Dated **evidence**, taken 2026-09-09 and not updated since", while `README.md:241` asserts "This section always reflects the current state" (constitution §4 makes that assertion the bar). AC-7.2/AC-7.3 hold either way. **Fix:** open a sibling `####` heading (e.g. `#### Proof state`) after `:293` so the dated block ends where the dated content does. | fixed r2 |
| F6 | Minor | `docs/metrics.md:220`, `:223` | Two published figure groups have no printed command: the per-agent breakdown (`reviewer 108 · …`) and `116 ceremonies` with its breakdown. Neither `transcripts.agent_runs` nor `transcripts.ceremonies` is named anywhere in the file — the only fields printed are `sessions_aspark`, `sessions_total`, `agent_runs_total`, `gates`, `days` — so a reader must invent the extraction step, which is what AC-2.2 forbids for the method and what AC-2.4 ("the rule that reproduces it printed in `docs/metrics.md`") only barely survives via the generic "transcript counters are summed" row. Both reproduce exactly in my own derivation. **Fix:** sum those two dict-valued fields in the third printed command and print the output, or name both fields in the rules table. | fixed r2 |
| F7 | Minor | commit `db4ab15` subject | Subject is `feat!:` — Conventional Commits' breaking marker — while its own body says "Not a breaking change" and NFR-2 requires "a minor/patch bump, never major". The branch is unpushed (`git ls-remote origin refs/heads/feat/metrics-script-removal` is empty), so the one commit `/go-live` reads for its version decision is still cheap to correct. **Fix:** reword to `feat:` before pushing, or record the contradiction explicitly in `release.md` so the bump is not taken from the marker. | fixed r2 |
| F8 | Minor | `.spark/metrics-script-removal/evidence.md:586-588` | Entry 7 states the literal AC-1.2 command "returns **three** hits" and names them. Re-run today it returns **six** — the same three plus `evidence.md:583,599,601`, written by the entry that records the count. The criterion's intent is unaffected: all six are inside `.spark/metrics-script-removal/`, and both alternative checks return 0 in my own re-run. Artifact wording, capped at Minor. **Fix:** state "every hit is inside this feature's own artifacts" instead of a count the file changes as it is written. | fixed r2 |
| F9 | Nit | `.spark/metrics-script-removal/evidence.md:461` | Cites the constitution's `spark-metrics` hit at `:295`; it is at `:296`. **Round 2: fix incomplete.** `636ecb2` changed the quoted grep output (`:458`) to `296:` but left the prose one line below reading "One hit, at `.spark/constitution.md:295`" — the two now disagree inside the same entry, which is worse than the original single stale citation. `:296` is confirmed correct (`grep -n 'spark-metrics' .spark/constitution.md` → one hit, line 296). **Fix:** correct `:461` too, or cite the row by its 2026-09-11 date so it cannot drift. | fixed |
| F10 | Major | `docs/metrics.md:228-231` | "One of the **ten** was in that state at an earlier merge … which is why **this snapshot's** line figure is some **7,800** lines higher than the one before it." Both halves are false after the rebase: there are 11 projects, and this snapshot's lift over the previous two-machine merge is **+125,661** lines (240,291 − 114,630) — which the same file states correctly at `:265`, so `docs/metrics.md` now contradicts itself. `origin/main:docs/metrics.md` had this right ("lifted *that* snapshot's figure by some 7,800 … The lift this time has a different cause: +125,661"); this branch's rewrite of the paragraph reintroduced the stale number, and `eaac8d9`'s re-derivation sweep did not touch it. NFR-5 ("every figure derived live … never restated unchanged") and §1's honesty bar are violated by the one paragraph in the file that explains why a figure moved. **Fix:** restore the two-sentence structure `origin/main` used — 7,800 attributed to *that earlier* snapshot, +125,661 to this one — and say "eleven". | fixed |
| F11 | Major | `.spark/constitution.md:259-261`, and `:299` | §8's inventory reads "an inventory taken at `db4ab15`, the commit that removed the last script: **106** tracked files — 90 `.md`, 5 `.yml`, 4 asset `.png`, **4 `.json` (2 plugin manifests + 2 metrics reports)**…". Two defects. (a) `db4ab15` was replaced by `62b4318` in the replay: `git merge-base --is-ancestor db4ab15 HEAD` and `… origin/main` both fail, so the constitution pins its count to a SHA no clone can resolve — and §8's own stated reason for pinning ("a total is pinned to its commit on purpose") is defeated. (b) The numbers are wrong against the commit it means: at `62b4318` it is **107** tracked files and **5 `.json` (2 manifests + 3 reports)`**; 90 `.md`, 5 `.yml`, 4 `.png`, 1 `.docx`, LICENSE, `.gitignore` are right. **Must AC-4.2 fails** — "each printed number equals the number §8 states". `:299` repeats the dead SHA and says "three `python3` stdlib commands" (now four). `evidence.md:493-496` records the same wrong 106. **Fix:** re-pin to `62b4318` with 107 / 90 `.md` / 5 `.json` (2 manifests + 3 reports), and correct `:299`'s SHA and command count. | fixed |
| F12 | Minor | `docs/metrics.md:18`, `:143`; `README.md:248`; `docs/reports/README.md:14`; also `evidence.md:290,350,514,775` | F6's fix added a fourth printed command but left every prose count at three: "**Three** commands, run from the repository root", "those **three** commands plus four rules", "prints **three** `python3` commands", "prints the **three** commands that do it". A reader following `docs/metrics.md` literally — which is exactly what AC-2.2 asks of them — is told to expect three blocks and finds four, and `evidence.md:775` instructs QA to "copy the three commands out of `docs/metrics.md`", which would leave the two breakdowns F6 exists to cover unverified. **Fix:** say four, or drop the count and write "the commands below". | fixed |
| F13 | Minor | `ROADMAP.md:50`, `:53` | The new `## Next` entry still describes the pre-rebase world: "three `python3` stdlib commands over the **two** committed machine reports" and "the figures are now a closed **2026-09-09** snapshot". There are four commands, three reports, and every other document on the branch now dates the snapshot 2026-09-10. `ROADMAP.md` is reader-facing and was outside `eaac8d9`'s sweep. **Fix:** four commands, three reports, 2026-09-10. | fixed |
| F14 | Minor | `.spark/metrics-script-removal/spec.md:73` (AC-2.3), `:124` (AC-7.2) vs `README.md:258,260`; `evidence.md:520-521,537-547` | The snapshot is now dated **2026-09-10** everywhere on the branch — correct, and inherited from `origin/main`, which already dated the three-report snapshot that way (PR #40, `d502441`, 2026-09-10). But AC-2.3 requires the provenance sentence to date the snapshot "(2026-09-09)" and AC-7.2's stated verification is `grep -n -A4 '2026-09-09' README.md` showing the table inside the dated block — that grep now prints **nothing**, so two Must ACs fail on their literal as QA will run them, while their intent plainly holds (the block carries a date and the word "evidence", with the table two lines under it). `evidence.md` Entry 7 still records the old grep output as AC-7.2's proof. Same class as F8 — a criterion literal overtaken by reality, not a defect in the work. **Fix:** add a `C9` clarification row recording the date move and restating both ACs' literals as 2026-09-10, and re-take Entry 7's grep. | fixed |
| F15 | Minor | `.spark/metrics-script-removal/spec.md:136` (NFR-3), `:165` (C8), vs `:13`, `:180`, `:187` | F3 was fixed by **editing the approved spec** — NFR-3 reworded and a `C8` row added — which is the one remedy round 1's own suggested fix ruled out ("The spec is immutable — this is a deviation, not a spec edit"), and no user ruling authorising it is recorded (contrast A8, which names the user's authority for the constitution edits). The substance is now correct and I am not asking for a revert; what is missing is the authority and the internal consistency: the Handoff still reads "Open: `none` — A1–A5 and **C1–C7** resolved", the SPEC GATE still reads "Clarify pass done … **C1–C7** all resolved", and its line-budget box still reads "Ist 187" against 188 lines. **Fix:** name the authority for the post-approval edit in C8 (or move the correction to `evidence.md`/`release.md` as round 1 prescribed), and update the two C-range references and the count. | fixed |

## 4. Requirements Traceability

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1 | `62b4318` | ✅ met r2 — `git ls-files '*.py'` empty, `ls scripts` → no such file; `git ls-tree -r HEAD` has no `100755` blob |
| AC-1.2 | `README.md`, `docs/metrics.md` | ⚠️ partial — literal command returns 6 hits, all inside this feature's own artifacts; intent re-verified two ways, both 0. Criterion wording is the defect. Note the cause `evidence.md` now names is real: this environment's `grep` is `ugrep 7.8.4`, which emits `.spark/…` rather than `./.spark/…`, so the exclusion pattern never matches |
| AC-1.3 / AC-1.5 | `evidence.md:693-779` | ⚠️ deferred by design — outside-repo writes reserved to the user (§6). AC-1.3's package is now **complete**: `claude plugin update aspark` sits at step 3 between the marketplace update and the `gitCommitSha` re-read, `claude plugin update --help` confirms the command exists ("Update a plugin to the latest version"), and the find at step 5 is no longer a tautology because step 4 must show the SHA moved first |
| AC-1.4 | `e8eb42c`, before `62b4318` | ✅ met r2 — re-ran the grep: 0 hits across `skills agents templates lenses tools .claude-plugin`; ordering survives the replay |
| AC-2.1 / AC-2.2 | `docs/metrics.md:24-138` | ✅ met r2 — I extracted all four fenced blocks programmatically and diffed each one's stdout against the block printed beneath it: exit 0 and **byte-identical** in all four cases. Prose miscounts them as three (F12) |
| AC-2.3 | `README.md:289-291` | ⚠️ partial r2 — provenance holds: `a2c0541` is an ancestor of `origin/main`, its blob at `scripts/spark-metrics.py` is `7c258cf…` / 32,654 B, the **same blob SHA** as at `62b4318^` — byte-identity by hash; `grep -n '\-\-merge' README.md` → 0. The AC's literal date (2026-09-09) no longer matches (F14) |
| AC-2.4 / AC-2.5 | `evidence.md` Entry 2-3 | ✅ met r2 — re-derived from scratch over all three reports (Must-AC verification, so re-derivation applies, not citation), against the four stated rules, with my own script: **11** projects / **77** features / 77·76·75·64·71 / tags **84** / +**240,291** / −**18,872** / **8 of 11** with line counts (3 `n/a`) / **42 of 138** sessions / **429** agent runs / **377** gates / **51** days 2026-07-13→2026-09-08 / agents reviewer 108 · product-owner 103 · release-manager 63 · engineering-manager 61 · qa-tester 59 · designer 24 · facilitator 11 (= 429) / **116** ceremonies with `/spark` 35 · `/next-steps` 24 · `/peer-review` 12 · `/sprint-plan` 11 · `/demo-day` 11 · `/increment` 8 · `/story-time` 6 · `/go-live` 6 · `/charter` 3. Every one reproduces exactly. AC-2.5: per-project max tags `[0,1,2,3,5,8,9,9,12,12,23]` = **84** under max-per-project-then-sum. Merge prose also checks out: 51/14/31 → 96 across 15; 3 shared projects; 19 duplicate features; aSPARK 8/8/8 identical; the other two one-sided subsets (9↔2, 2↔1); the third machine's other project 23 features; +125,661 = 125,646 + 15; 24 sessions, 0 aSPARK-driven. Gap prose too: 13 missing QA of which 8 in a 9-feature project; 6 missing release = 3 + 1 + two single-feature projects |
| AC-3.1 / AC-3.2 / AC-3.3 / AC-3.4 | `docs/metrics.md`, `docs/reports/README.md`, `README.md` | ✅ met — all four greps re-run: 0 hits each; flag-table count 0; the closed-snapshot statement stands |
| AC-4.1 | `4a50454`, `.spark/constitution.md` | ⚠️ refuted-with-finding (user-ruled, D1) — **still the right call**, re-confirmed from the source: `grep -n 'spark-metrics' .spark/constitution.md` → exactly one hit, `:296`, the amendment row recording this very ruling; the new row at `:299` avoids the literal string so the count stays at 1. Satisfying the clause would mean editing an audit trail to clean a grep. First clause holds |
| AC-4.2 | `.spark/constitution.md:256-266` | ❌ not met r2 — live `git ls-files '*.py'` = 0 ✅, but the pinned totals do not match the commit they are pinned to, and that commit is orphaned (F11) |
| AC-4.3 / AC-4.4 | `4a50454` | ✅ met — preamble unqualified, `cli` row parenthetical gone with lens load unchanged, §3 exception deleted, §8 restated, new Amendments row cites the user's 2026-09-11 authority. Content of §8 is F11's, not AC-4.3's |
| AC-5.1 / AC-5.2 / AC-5.3 | `26e6a36`, `eaac8d9` | ✅ met r2 — all three reports now carry no `root`; `grep -rn '/Users/\|/home/\|/root/' docs/` → 0, `grep -rn '"root"\|/Users/\|hostname' docs/reports/*.json` → 0, and a recursive key-walk over all three JSONs finds no host/path/root value at all (the only `/`-bearing string left is `.spark/ is not tracked in this repository`). Figures unchanged by the strip |
| AC-7.1 / AC-7.2 / AC-7.3 / AC-7.4 | `README.md:244-299` | ⚠️ partial r2 — numeric headline gone; the table sits 2 lines under a paragraph carrying both a date and "evidence"; F5's boundary is closed by an explicit "The snapshot ends here. What follows is not dated and not frozen" at `:297-299`, which introduces no count. I re-walked every sentence outside the dated block: no count of projects, features, runs or days survives, so AC-7.3 holds. The literal `2026-09-09` in AC-7.2/AC-2.3 no longer matches (F14). AC-7.4 is `/demo-day`'s |
| AC-6.1 (`Could`) | `ROADMAP.md:45-53` | ⚠️ partial r2 — `grep -n 'handbook honesty exception' ROADMAP.md` → no hit anywhere, and the removal is listed under `## Next`; the entry's own facts are stale (F13) |
| NFR-1 | `git diff --name-status origin/main...HEAD` | ✅ met r2 — re-run against `origin/main` specifically since the merge-base moved: 13 paths, none under `skills/ agents/ lenses/ templates/ tools/ .claude-plugin/` |
| NFR-2 | `e8eb42c` + `62b4318` | ✅ met r2 — `feat:`, no `!` in any subject on the branch (`git log --format='%s' origin/main..HEAD`). The replay caused **no content drift**: every blob at `62b4318` (`docs/metrics.md`, `docs/reports/README.md`, `evidence.md`, `plan.md`) is byte-identical to `db4ab15`'s, so what round 1 reviewed for that commit is what stands. The patch-id differs only because the parent changed |
| NFR-3 | lens §3 | ✅ met r2 — zero executables, zero interpreters required to use the plugin. Footprint re-derived myself: `origin/main` 5,747,014 B / 105 files → `HEAD` 5,838,016 B / 108 files, a net **+91,002 B** — an increase, which is the direction NFR-3 now states. It wisely states no number (review.md's own size moves it). The A9 hedge is repeated, not escalated |
| NFR-4 | `docs/metrics.md` | ✅ met r2 — all four commands observed running, extracted from the file, output byte-identical. No printed command names a file the repo does not contain |
| NFR-5 | `evidence.md` Entry 2-3 | ⚠️ partial r2 — every *published figure* derived live and reproduced by me, and the two breakdowns F6 flagged now have a printed command. But one figure **was** restated unchanged through the rebase and is now false (F10), which is the precise thing NFR-5 forbids |
| NFR-6 | `eaac8d9` | ✅ met r2 — holds on the branch and on merge; checked with the widened pattern, not only `/Users/` |
| NFR-7 | `e8eb42c`, `61ec502` | ✅ met — negative case recorded before the deletion commit; `claude plugin validate .` re-run, passes with the one pre-existing `autoUpdate` warning on a file this diff does not touch; `git status --porcelain` clean, so the derivation stayed untracked |

## 5. What Was Checked

- [x] Correctness: every Must AC re-performed from the sources this round; all four fix-mode commits opened and read
- [x] Non-functional: NFR-1…NFR-7 and the `library` lens incl. §3 packaging/footprint; constitution §§1,3,4,5,6,8
- [x] Error handling: all four printed commands tolerate `n/a` projects and absent dict fields (`proj.get('git') or {}`, `t.get('ceremonies') or {}`); the fourth was run against a report whose `agent_runs` is `{}` without a crash
- [x] Security: no secrets; the `/home/…` username is gone from all three reports; no executable ships
- [x] Tests: none exist and none are possible (§4) — substitute performed: `claude plugin validate .` plus four observed command outputs diffed against what is printed
- [x] Readability: `docs/metrics.md` states rules instead of describing a program; the one paragraph that now contradicts itself is F10

## 6. Verdict

Changes requested — but the gap between round 1 and round 2 is narrow and the important half of it is closed. Both Blockers are genuinely gone, verified at the source rather than taken from the handoff: the merge-base now *equals* `origin/main`, `git merge-tree` produces a tree with no conflict, the `/home/silenttz/…` username is absent from all three reports under every pattern I could think to try including a full recursive key-walk, and `docs/reports/README.md`'s "no hostname, no path" is true as written for the first time. Both Majors hold up too — NFR-3 now states the footprint direction I measure myself (+91,002 B, an increase), and AC-1.3's sequence has a real `claude plugin update aspark` at step 3, confirmed against the CLI's own `--help`, in an order that makes step 5's `find` mean something. The replayed deletion commit is `feat:` and carries byte-identical blobs to the one round 1 reviewed, so the history surgery cost nothing. All 23 figures, the merge prose and the gap prose reproduce exactly in a derivation I wrote from the four stated rules, and all four printed commands run with byte-identical output. What keeps this from passing is that the post-rebase sweep stopped at the three files F1 named. `docs/metrics.md` now tells a reader that this snapshot's line figure is "some 7,800 lines higher than the one before it" and then, two screens later, correctly says +125,661 — the file contradicts itself about the one number it exists to explain, and `origin/main` had that paragraph right before this branch rewrote it. The constitution's §8 inventory is pinned to `db4ab15`, a SHA the replay orphaned and no clone can resolve, with counts that are wrong against the commit it means, so Must AC-4.2 fails outright. `ROADMAP.md` still says two reports and 2026-09-09, four places still promise three commands where four now stand, and F9's fix corrected the quoted grep output while leaving the prose citation one line below it contradicting it. None of that is structural and none of it is expensive: it is one paragraph, one inventory row, five counts and a date, all of them mechanical. I fixed nothing myself — F9 and F15 touch another ceremony's artifacts and the caller asked me to report rather than repair, and the rest carries enough judgement (which date, which pin, whether to edit the spec at all) that the developer should own it.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings — **F1, F2 confirmed `fixed r2`**, verified independently
- [ ] No open Major findings (or explicitly waived by the user, with reason recorded here) — F3, F4 confirmed `fixed r2`; **F10, F11 newly open**. No waiver sought; only the user may waive a Major
- [ ] Every Must AC traces to implementing code; no constitution non-negotiable violated — **AC-4.2 fails** (F11). AC-4.1's second clause remains user-ruled `refuted-with-finding`; AC-1.2's literal command and AC-2.3/AC-7.2's literal date are criterion defects (F14); AC-1.3/AC-1.5 are `/demo-day`'s by §6. §3's "no executable code" and §6's privacy non-negotiable both hold on merge
- [ ] All plan deviations documented and accepted — D1, D2, D6 user-ruled; D3, D4, D5 as recorded; D7 ruled acceptable in round 1; **the spec edit made for F3 is an undocumented deviation** (F15)
- [x] Test suite runs green — none exists and none is possible (§4); substitute performed: `claude plugin validate .` passes with the one pre-existing `autoUpdate` warning, and all four printed commands were extracted and observed running with byte-identical output
- [x] Line budget respected: Ist 119 / Soll ~150 (excluding HTML comments)
- [ ] Status set to `passed` — set to `changes-requested`
