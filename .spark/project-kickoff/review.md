# Review Report: project-kickoff

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The working tree vs `HEAD`, `.spark/project-kickoff/plan.md` |
| **Status** | `passed` |
| **Round** | 3 |
| **Date** | 2026-09-18 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`). `passed` — set by the Reviewer at round 3 after independently re-verifying all five findings round 2 left open. `/demo-day` may start.
- **Summary (round 3):** F11, F15, F16, F17 and F18 are all **confirmed fixed** — each re-derived from the live file or a fresh fixture inspection, not from the fix notes (round 2 proved a fix note can be false). The Major that mattered, **F16**, is genuinely closed: I re-traced `skills/next-steps/SKILL.md` step 1 against Venue C myself and control reaches step 2's Product Owner delegation; the fallback bullet's condition is byte-identical to `HEAD`. Regression sweep clean: `claude plugin validate .` green, NFR-1's five surface counts unmoved, `templates/constitution.md` §1–§8 byte-identical.
- **Open:** `0` — F20, F21 and F22 fixed by the developer (round 4, 2026-09-18) at the user's request, though none blocked the gate: F20 via a further `/charter` amendment (facilitator-drafted, user-confirmed), F21/F22 as wording corrections in `evidence.md`. See `evidence.md` Round 4. Not re-verified by the Reviewer — none was behavioral, and the gate was already `passed` before they existed.
- **Binding ruling:** §6 Verdict and the gate checklist below are round 3's and authoritative; round 3 set `passed` before F20–F22 existed, and closing them did not reopen it.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Re-review of the same working tree vs `HEAD` (still nothing committed): 11 tracked files (+214/−28) plus untracked `.spark/project-kickoff/`. Round 2 verified round 1's 14 fixes and found two of them recorded as more complete than they were; this round verifies the five fixes that closed those, with the same instrument, and hunts what *they* broke. Read in full: this report's round-2 body (the caller asked me to confirm every open finding, which per my own rules always earns the full read), `spec.md`, `plan.md`, `evidence.md` (Entries 1–18, including the round-3 block), `.spark/constitution.md` §5/§6/§9/Amendments, and the diff of all three files touched this round plus the eight that were not.

**Re-derived from primary source, not cited — condition (a)** (this round verifies fixes to those very facts): `.spark/constitution.md:304`'s three cited ranges each opened and read (`:169-170` kebab-case + feature dirs, `:173-174` Conventional Commits + one branch per feature, `:181-184` **Language: English for everything committed**) and `:188-191,196-200` re-opened for the Non-negotiables entry; `skills/next-steps/SKILL.md`'s fallback bullet byte-compared against `git show HEAD:` and re-traced by hand against Venue C; Venues C and D re-inspected on disk (`git log`, `find`, `cat`); `skills/charter/SKILL.md` step 1 and its step count read directly. **Regression sweep re-run, not cited:** `claude plugin validate .` → green, same pre-existing `autoUpdate` warning; NFR-1 counts (skills 10, agents 7, lenses 9, templates 6, `/charter` steps 5, new `${CLAUDE_PLUGIN_ROOT}` paths 0); `git diff -U0 templates/constitution.md` → still the single insertion hunk `@@ -126,0 +127,45 @@`, so §1–§8 remain byte-identical; `git diff -U0 .spark/constitution.md` → still exactly three hunks. **Cited, not re-derived:** round 2's own verification of F1–F9, F12–F14 and F19 — none of the three files touched this round is theirs, and I spot-confirmed the four load-bearing ones anyway (`facilitator.md:118`, `product-owner.md:48-50`, `engineering-manager.md:45-47`, `skills/spark/SKILL.md:26`, `:77`) and found no drift. **Not reviewed:** live re-performance against the *installed* plugin — plan §4 defers it to `/demo-day`. No graph tool re-run: round 1 established it holds no node for Markdown; no condition (a)–(d) applies.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | Re-verified: +45/−0, one insertion hunk, §1–§8 byte-identical, all four contract facts in the comment (`:129-149`) |
| T2 | ✅ | Rule inside step 3 (`:88-123`), step 1 extended (`:43-49`), `tools:` line unchanged. F3's caps sentence now names the 35 total (`:118`) |
| T3 | ✅ r3 | `grep -c "^[0-9]\. "` → **5** (headings at `:25/:43/:48/:72/:75`); step 1's git summary now covers "every first draft … **and** any amendment that touches §9" (`:34-40`). F7 and F18 both closed |
| T4 | ✅ r3 | `skills/spark/SKILL.md` unchanged since round 2 and still correct (F5, F8). `skills/next-steps/SKILL.md:57`'s condition is byte-identical to `HEAD` again; I re-traced the skip path on Venue C and it reaches step 2. F16 closed |
| T5 | ✅ | `README.md:183-191` — still 9 lines, still before the `/next-steps` example at `:193`; the F6 rewording did not push it over the 10-line cap |
| T6 | ✅ | Unchanged since round 1; the barrier held |
| T7 | ✅ | Seven entries, `≥ 1 file:line or not found` bar, `absent` handling — unchanged, and now exercised live on Venue D |
| T8 | ✅ r3 | Migration intact; the third §9 citation is now written for real (`:304`) and the false completion record is corrected by a new Amendments row (`:328`). F11, F15 closed. Residue: that row misdescribes its own method — F20, Minor |
| T9 | ✅ | Unchanged; both NFR-3 greps re-run by me, still one different file each |
| T10 | ✅ | Closed by Entry 12's Venue D, which I verified on disk — the AC-4.4 venue T10 was never able to be |
| T11 | ✅ | F4 closed: both unconditional sentences now sit ahead of the §9 branch in both files |
| T12 | ✅ | Unchanged; the F6 README rewording keeps the claim inside the two measured venues |
| T13 | ✅ | AC-1.4 and AC-4.4 now both have entries (11, 12); AC-3.4 correctly relabelled `not-verified-live` (F9) |

**Documented deviation, accepted:** constitution §3's *"a new concern is a new file, not an edit to the roles"* — recorded in plan §1, user-approved at the plan gate. Not re-litigated.

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Major | `.spark/project-kickoff/evidence.md` (no entry) vs `plan.md:173-174` | **AC-1.4 is a Must AC and has no verification.** Plan §4 says "AC-1.4 is verified by a performed invocation in the same run"; evidence never mentions AC-1.4 and no run exists, nor a `not-verified-live` row. Constitution §8 forbids passing by reading. **Fix:** run it, or add an honest row. **r2 (my check, not the fix note):** Entry 11 records real `/spark <idea>` and `/story-time <idea>` runs on Venue C. I re-derived the basis myself — `git diff skills/spark/SKILL.md` is **one** hunk, inside the argument-less branch (`:25-32`); `:20`'s `<idea>` line and the lens nudge (`:59-71`, which is where the pre-existing `/charter` mention Entry 11 observed comes from) are untouched; `skills/story-time/SKILL.md` has no diff at all. "Nothing differs from today" holds structurally, with the run as confirmation. Entry paraphrases rather than quotes — F17. | fixed r2 |
| F2 | Major | `.spark/project-kickoff/evidence.md:251-257` vs `plan.md:182-184` | **AC-4.4 is unverified and the plan's claim about it is false.** Venue A has a README and a commit, so the `absent` path never executed. **Fix:** run the discovery end against a repo with none, or label it. **r2 (my check):** I inspected Venue D on disk: no `README.md`, no `CLAUDE.md`, `git log` → `fatal: your current branch 'main' does not have any commits yet`, one `src/main.py` of exactly two lines — matching Entry 12's `src/main.py:1-2` citation and its snake_case/f-string convention observation. All three named inputs are genuinely absent and every checkable fact in the entry holds. Paraphrased, not quoted — F17. | fixed r2 |
| F3 | Minor | `agents/facilitator.md:118` | The §9 rule named only the sub-caps (12 + 25 = 37 > 35), so an agent obeying it alone could breach AC-2.3. **r2:** `:118` now reads "(≤ 35 lines in total, the product brief ≤ 12 lines, the system picture ≤ 25 …)" — read by me. | fixed r2 |
| F4 | Minor | `agents/product-owner.md:57-60`, `agents/engineering-manager.md:55-58` | The fallback branch swallowed instructions that must stay unconditional (the duplicate check; "your plan must fit *this* codebase"). **Fix:** keep both outside the branch, scope only system-fact re-derivation. **r2 (read myself, both files):** `product-owner.md:48-50` "**Always** read the target project's README … — never spec a feature that duplicates existing functionality" and `engineering-manager.md:45-47` "**Always** explore the target project … Your plan must fit *this* codebase" both now precede the §9 branch, which starts at `:51`/`:47` and explicitly excludes them ("not the duplicate check above, which always runs" / "not the exploration above, which always runs"). Genuinely unconditional, not reworded-and-still-conditional. | fixed r2 |
| F5 | Minor | `skills/spark/SKILL.md:26-31` | The no-constitution branch named `/charter` and stopped, leaving the decline path unstated. **r2:** `:29-30` now reads "if they decline, offer `/next-steps` or their own idea, exactly as today" — verified in the diff. | fixed r2 |
| F6 | Minor | `README.md:185-187` | "hands you straight to `/story-time`" described an auto-handoff the ceremony does not do. **r2:** `:185-187` now reads "ends by offering `/story-time` with the first slice named — on your go, never automatically"; block re-counted at 9 lines (`:183-191`), still inside AC-1.6's cap and still before the `/next-steps` example. | fixed r2 |
| F7 | Minor | `skills/charter/SKILL.md:30-35` vs `agents/facilitator.md:46-49` | The skill scoped the git summary to **brownfield** discovery while the Facilitator expects one generally, so a greenfield repo *with* commits could get a false `absent`. **r2:** `:33-35` now reads "Do this on **every first draft**, not only ones with source files or a manifest — a greenfield repo can still have real commits", plus an explicit hand-over for a repo with zero commits. The finding as written is closed; a narrower residue is filed as F18. | fixed r2 |
| F8 | Minor | `skills/spark/SKILL.md:25-26` | The branch condition read "every existing feature is `released`", so on a `pr`-mode project the routing never fires. **r2:** `:26` now reads "is `released` or `handed-off`". I checked the rest of the file for conflict: the only other occurrences are the phase map's `:54`/`:55`, which already treat both as loop-closed — consistent, no contradiction introduced. | fixed r2 |
| F9 | Nit→Minor | `.spark/project-kickoff/evidence.md:321-325` | AC-3.4 carried a ✅ while the same sentence said it was inspection-only. **r2:** now reads "**AC-3.4: `not-verified-live`** … Verified by code inspection only, not by a performed run"; I opened the cited `agents/facilitator.md:102-105` and it does hold the skip rule ("a question skipped … becomes `not stated` … a skip blocks nothing"), so the citation still resolves after the fix round's edits. | fixed r2 |
| F10 | Minor | `.spark/project-kickoff/evidence.md:163-190` | AC-1.5's `/next-steps` leg rested on a static reading, not a performed step. **r2:** Entry 13 records a live re-run on Venue B. I traced step 1 against Venue B myself under the **current** text: bullet 1 (`:48`) is false (constitution exists); bullet 2 (`:57`) is false (`.spark/` is not empty); control falls through to step 2, and the added clarifying sentence (`:60-65`) accurately describes that. The leg is closed. **But** the narrowing was not what Entry 13 claims — see F16. | fixed r2 |
| F11 | Minor | `.spark/constitution.md:304` | Three §9 entries used markers outside the section's set or cited a section instead of a source. **Not fixable by me by design:** `/charter` is §9's sole writer (AC-2.5). **r2 — reopened, only two of three landed.** `:296` now cites `.spark/constitution.md:188-191,196-200` (I opened both ranges: they hold exactly the three named non-negotiables) and `:293` now reads `not found`. But `:304` still reads **`- **Conventions:** `inferred from §5``** — unchanged, and it is the one entry sitting in the *system picture*, where AC-4.2 demands ≥ 1 `file:line`. **Fix:** a further `/charter` amendment writing the range the fix round already researched — note that `:169-170,173-174` covers kebab-case, branches and Conventional Commits but **not** "English throughout", which lives at `:181-184`; cite all three or drop the claim. **r3 — confirmed by opening the live file, not the fix note (condition (a)):** `:304` now reads `inferred from .spark/constitution.md:169-170,173-174,181-184`. I opened all three: `:169-170` = "kebab-case files and directories; feature directories are `.spark/<kebab-case-feature>/`"; `:173-174` = "Conventional Commits (`feat:`, `docs:`, `chore:`); one branch per feature"; `:181-184` = "**Language:** English for everything committed, without exception". All four facts the entry claims are now backed, including the one range this row named as missing. | fixed r3 |
| F12 | Nit | `skills/charter/SKILL.md:62` | Step 3's marker list omitted `confirmed by user`. **r2:** the list at `:64-66` now reads `inferred from <file:line>` / `asked` / `confirmed by user` / `not stated` / `not found` — matches the template comment and the Facilitator. | fixed r2 |
| F13 | Nit | `docs/workflow.md:8-22` | The deep dive described the constitution with no §9 and no start-here routing. **r2:** `:23-27` adds exactly that, and its claims ("routed there first by `/spark` and `/next-steps`", "a product brief plus, on a codebase, a seven-entry system picture") match the shipped text and `templates/constitution.md` §9. No overclaim. | fixed r2 |
| F14 | Nit | `spec.md:73` / `plan.md:148` cite `skills/spark/SKILL.md:74` | T4's hunk moved the `§8 QA Method` citation. **r2:** re-measured by me — it is now at **`:77`**, not `:76` as round 1 recorded (the F5/F8 fix added one more line to that hunk); `skills/demo-day/SKILL.md:28` is unmoved. The by-number contract AC-2.1 protects still resolves. `spec.md`/`plan.md` deliberately left un-renumbered. | fixed r2 |
| F15 | Major | `.spark/constitution.md:327`; `.spark/project-kickoff/evidence.md:491-505` | **The F11 fix is recorded as complete in two places while one third of it was never written.** The Amendments row claims "**Non-negotiables** and **Conventions** now cite real `file:line` ranges (`…:188-191,196-200` and `:169-170,173-174`)" and closes with "only these three lines changed"; Entry 14 repeats it and verdicts "§9's citations now meet AC-2.6 without exception". `:304` is unchanged (F11). This matters more than the missing citation itself: the Amendments log is this repo's standing audit trail, `/demo-day` will read the row rather than the line, and a fix recorded-but-not-made is the exact failure mode constitution §1/§4 and this repo's own `CLAUDE.md` ("a disclosed limitation stays honest only if re-verified at every gate") exist to prevent. **Fix:** in the same `/charter` amendment that closes F11, correct the row to describe what was actually written; amend Entry 14's verdict line to match. **r3 — confirmed by reading both documents myself:** `.spark/constitution.md:328` is a new row that quotes the false claim verbatim ("the previous row claimed **Conventions** was also fixed … and that 'only these three lines changed' — false") and records the real citation. `evidence.md:556-566` leaves Entry 14's verdict visible under an explicit "⚠️ **This verdict was false — see Entry 15**" rather than rewriting it. `git diff -U0 .spark/constitution.md` → still exactly three hunks; the Amendments hunk `@@ -308,0 +326,3 @@` adds three rows. The audit trail is honest about what was believed and when. One residue in that row's self-description — F20. | fixed r3 |
| F16 | Major | `skills/next-steps/SKILL.md:57` | **The fallback bullet now fires on *every* skip, not only on a repo with nothing to read.** It opens "If the user skips `/charter`, **or** if nothing exists yet in `.spark/` and git history is empty or trivial, say so plainly — there is no "state" to read yet — and ask the user for the first idea". Read with the natural wide scope, a skip on a code-bearing repo (Venue C: 3 commits, real `src/`, no `.spark/`) now yields "there is no state to read yet" — factually false there, and a direct contradiction of bullet 1's own "continue to step 2 only on the user's explicit skip" (`:49-50`), of AC-1.2 ("proceeds to the PO brief only on the user's explicit skip"), of AC-1.3 ("exactly what it does today … the PO brief with 'constitution: none'") and of NFR-4. `HEAD`'s bullet has no skip disjunct at all, so this is this feature's addition; Entry 13's claim that the bullet "was restored to its exact original form" is **false against `git diff`** — the disjunct and a reworded tail both remain. Mitigating, not exculpating: Entry 6's subagent parsed it narrowly and did delegate to the PO — one observation under earlier wording, not a contract. **Fix:** delete "If the user skips `/charter`, or" (bullet 1 already routes the skip to step 2), or make the scope explicit: "If the user skips `/charter` **and** there is still nothing to read — nothing in `.spark/`, git history empty or trivial — …". Then correct Entry 13's sentence to describe the bullet that actually shipped. **r3 — re-traced by me against the live file, not Entry 16 (condition (a)):** the skip disjunct is gone. `diff` of `:57-59` against `git show HEAD:` shows the **condition** line byte-identical (`If nothing exists yet in .spark/ and git history is empty or trivial,`). Traced against Venue C, which I re-inspected on disk (`git log` → 3 commits `f1c9c2e`/`1018e7a`/`6ce6826`; `src/index.js`, `subtract.js`, `multiply.js`; no `.spark/`): bullet 1 (`:48-56`) fires, the explicit skip routes to step 2; the fallback bullet's AND is **false** (half 2 fails on three real commits), so it never fires and nothing says "there is no state to read yet". Control reaches step 2's `product-owner` delegation — the dead-end is gone. AC-1.2, AC-1.3 and NFR-4 hold. Entry 16's PO run is corroborated by the fixture: `package.json`'s `main` is `src/index.js`, which exports only `add`, so `subtract`/`multiply` really are unreachable. **Residue:** the bullet's *tail* is still reworded vs `HEAD`, and Entry 13 was not corrected as this finding's fix instruction required — F21. | fixed r3 |
| F17 | Minor | `.spark/project-kickoff/evidence.md:409-454` (Entries 11, 12) | **Both fix-round entries say "quoted" but contain no quoted output.** Entry 11: "reply **quoted**, mentions `/charter` twice"; Entry 12 likewise summarises. `awk` over `:401-514` returns **zero** blockquote lines, against Entries 3–9 which all carry `> …` transcripts, the file's own preamble, and NFR-5's "each with quoted output". The runs are real enough — I independently confirmed Venue D's three absent inputs and the `src/main.py:1-2` citation, and re-derived AC-1.4's diff basis — so this does not reopen F1/F2, but a later reader cannot re-judge the replies against the ACs, which is what quoting is for. **Fix:** paste the two replies (or the decisive lines) as block quotes, or relabel the entries "summarised, transcript not retained" and say why. **r3 — measured and corroborated by me:** `awk '/^[ \t]*>/'` → Entry 11 (`:409-455`) **22** blockquote lines, Entry 12 (`:456-506`) **25**. Not just present but checkable against the live fixtures: Venue D's `src/main.py` is exactly `def greet(name):` / `return f"Hello, {name}!"`, `git log` fails with the quoted `fatal: … does not have any commits yet` and `git log --all --oneline` returns nothing; Venue C holds `add`/`subtract`/`multiply` under `src/` with no `bin` and no server, as Entry 11's lens nudge quotes. NFR-5's "each with quoted output" now holds for all five runs. **Correction to my own round-2 instrument:** the `awk` I reported the zero count from used `\s`, which POSIX `awk` does not support, so it would have returned zero either way; the finding rested on the entries' own prose as well, and is moot now that the quotes are there and check out. | fixed r3 |
| F18 | Minor | `skills/charter/SKILL.md:33-35` | The git-summary duty is scoped to "**every first draft**, not only ones with source files or a manifest" — which excludes an **amendment**, the very path that adds §9 to an existing constitution (this feature's own AC-4.5 dogfood, and the only path a consumer already holding a constitution has). The Facilitator has no shell (`agents/facilitator.md:9`), so on that path it receives no history and must record git history `absent` on a repo that has plenty — the same false-`absent` defect F7 fixed, one boundary over. **Fix:** "on every first draft **and on any amendment that touches §9**". **r3 — read by me:** `:34-40` now reads "Do this on every first draft … **and do it again on any amendment that touches §9** (adding it where absent, or correcting an existing entry)", with the reason inline ("the Facilitator's own git history is no fresher on an amendment than on a first draft"). `grep -c "^[0-9]\. " skills/charter/SKILL.md` → **5**, headings at `:25/:43/:48/:72/:75` — no sixth step, NFR-1 intact. The false-`absent` gap on the amendment path is closed. | fixed r3 |
| F19 | Nit | `review.md` §3 F14, §4 AC-3.3 | The fix round shifted two line citations inside this report: `skills/spark/SKILL.md:76` → `:77` (F14) and `skills/charter/SKILL.md:70-71` → `:73-74` (the `/story-time` explicit-go offer, now step 5 at `:71-74`). **Fixed by me:** F14's row corrected above; AC-3.3's §4 cell is left byte-identical per the unchanged-cell rule, and the correct range is recorded here instead. | fixed r2 |
| F20 | Minor | `.spark/constitution.md:328`; `.spark/project-kickoff/evidence.md:605` | **The correction row misdescribes its own method.** Both say the fix was "recorded as a new row, **not an edit to the false one**" — but `:327` *was* edited: round 2's F15 quoted it verbatim as claiming "**Non-negotiables** and **Conventions** now cite real `file:line` ranges (`…:188-191,196-200` and `:169-170,173-174`)" and closing "only these three lines changed", and neither clause is in `:327` today; in their place is a new sentence, "**This row overstated its own completeness — see the next row.**" **Why it matters:** the Amendments log is this repo's standing audit trail and a row that is wrong about what was done to it is the same category F15 was filed for — smaller, because nothing is hidden (`:328` quotes the deleted claim, so the mistake is still readable) and no fix is unwritten. Basis stated plainly: the intermediate text was never committed, so `:327`'s prior state cannot be re-derived from git; round 2's own verbatim quote is the evidence. **Fix:** at the next `/charter`, reword `:328` to "the false row's claims were replaced by a pointer, and the real fix recorded in this new row" (and the same in Entry 15). Not fixable by me — `/charter` is the constitution's only writer (AC-2.5), the rule F11 itself established. **r4:** fixed via a further `/charter` amendment (facilitator drafted, user confirmed before writing): `:328`'s Why cell now states plainly that `:327` was both edited (its overstated clauses removed) and a new row added — no claimed purity of method. `evidence.md` Entry 15 reworded to match. `git diff .spark/constitution.md` re-checked: still exactly three hunks. | fixed r4 |
| F21 | Minor | `.spark/project-kickoff/evidence.md:519-520`, `:657-658` | **F16's second fix instruction was not carried out, and Entry 16 asserts the opposite.** F16 required "correct Entry 13's sentence to describe the bullet that actually shipped". `:519-520` still reads "the fallback bullet's **wording** was restored to its exact original form", and `:657-658` adds "this time the 'restored to original form' claim is true — checked by re-opening the file". Re-opening the file shows otherwise: the *condition* is byte-identical to `HEAD`, but the tail is not — `HEAD` has "…read yet, and the honest move is to ask the user for the first idea", the shipped bullet has "…read yet — and ask the user for the first idea", plus six added lines. **Why it matters:** no behaviour is affected (the condition is what governs, and it is correct — F16 is closed on substance), but an evidence entry that claims a re-verification it did not perform is precisely the failure mode F15 and F16 were filed for, one file over. **Fix:** two words in `:519` — "the fallback bullet's **condition** was restored to its exact original form; its tail was reworded and a clarifying sentence added" — and scope `:657` to the condition. Left open rather than fixed by me: only the author knows what was actually checked, and I will not rewrite someone else's verification claim. **r4:** fixed by the author (the developer, this session) exactly as scoped — `condition` replaces `wording`, the tail's rewording is named explicitly, and the closing verdict now says "the condition is restored and verified byte-identical to HEAD" rather than claiming the whole bullet. | fixed r4 |
| F22 | Nit | `.spark/project-kickoff/evidence.md:613-616` | Entry 15's "re-verified after the write" bullet says the Amendments region now holds "**four** rows total across T8/F11/F15's corrections". `git diff -U0 .spark/constitution.md` shows `@@ -308,0 +326,3 @@` — **three** added rows (`:326` T8, `:327` F11, `:328` F15). Three events, three rows. **Why it matters:** trivial on its own, but it sits inside the one bullet that claims the write was re-verified before being described — the same bullet's other three claims (three hunks, validate green) do check out. **Fix:** change "four" to "three". **r4:** fixed as proposed. | fixed r4 |
| F23 | Nit | `review.md` §4 AC-3.3, NFR-8 | F18's 13-line insertion into `/charter` step 1 shifted the `/story-time` explicit-go offer again: `:73-74` (round 2) → **`:76-78`**, step 5 now starting at `:75`. Following AC-3.3's cited `:70-71` today lands in step 4's *Iterate*, not the offer. **Fixed by me, same handling as F19:** both §4 cells are left byte-identical per the unchanged-cell rule (neither verdict changed) and the current range is recorded here. Re-read and confirmed: the offer is explicit-go only, so both verdicts stand. | fixed r3 |

## 4. Requirements Traceability

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1 | `skills/spark/SKILL.md:26-32`; evidence Entry 3 | ✅ met r2 — `handed-off` now recognised (F8) |
| AC-1.2 | `skills/next-steps/SKILL.md:48-56`; Entries 5, 6 | ✅ met r3 — recommend-first holds and the explicit skip reaches step 2; re-traced by me on Venue C (F16) |
| AC-1.3 | `skills/next-steps/SKILL.md:57-68`; `skills/spark/SKILL.md:29-30`; Entries 5, 6 | ✅ met r3 — `/spark`'s decline path stated (F5); `/next-steps`' fallback condition byte-identical to `HEAD` again (F16) |
| AC-1.4 | `skills/spark/SKILL.md` — one hunk, `<idea>` path and `:59-71` untouched; Entry 11 | ✅ met r2 — performed on Venue C, diff scoping re-derived by me |
| AC-1.5 | `skills/spark/SKILL.md:30-32`; Entries 4, 13 | ✅ met r2 — both legs live; I re-traced the `/next-steps` leg against Venue B myself |
| AC-1.6 | `README.md:183-191` — 9 lines, before the `/next-steps` example | ✅ met (re-counted by me after the F6 rewording) |
| AC-2.1 | `templates/constitution.md:127-170`; `git diff -U0` → one insertion hunk, +45/−0 | ✅ met — §1–§8 byte-identical, re-verified |
| AC-2.2 | `skills/charter/SKILL.md:62-66`; `facilitator.md:88-96`; Entry 8 | ✅ met |
| AC-2.3 | `.spark/constitution.md:285-306` — 22 physical lines, 6 brief bullets, 7 picture bullets | ✅ met — inside 35/12/25 on any counting method |
| AC-2.4 | `templates/constitution.md:132-137`; Entry 7 | ✅ met — strongest available control |
| AC-2.5 | `templates/constitution.md:136-138`; `facilitator.md:121-123` | ✅ met — and honoured in practice: the F11 fix went through `/charter`, not the Reviewer |
| AC-2.6 | `.spark/constitution.md:291-306`; `facilitator.md:101-105` | ✅ met r3 — all 13 §9 entries carry an allowed marker; the three disputed ones fixed and each cited range opened by me (F11, F15) |
| AC-3.1 | `facilitator.md:91-96`; Entry 9 | ✅ met |
| AC-3.3 | `skills/charter/SKILL.md:70-71`; Entry 9 step 5 | ✅ met — offered, not invoked |
| AC-3.4 | `facilitator.md:102-105`; Entry 9 | ⚠️ `not-verified-live` r2 — inspection only, now labelled honestly (F9) |
| AC-4.1 | `skills/charter/SKILL.md:29-42`; `facilitator.md:43-49`; Entries 8, 12 | ✅ met r3 — the git summary now covers a §9-touching amendment as well as a first draft (F18); Entry 12 names all three `absent` inputs |
| AC-4.2 | `.spark/constitution.md:300-306` — seven entries, right order | ✅ met r3 — all seven now carry a `file:line` or `not found`; `:304`'s three ranges opened and read by me (F11) |
| AC-4.4 | `facilitator.md:49`; Entry 12 (Venue D) | ✅ met r2 — venue verified on disk by me; three absent inputs, pass continued |
| AC-4.5 | `.spark/constitution.md` §9 + Amendments `:326` | ✅ met — migration faithful, drift disclosed; the *second* row's accuracy is F15, not this AC |
| AC-5.1 | `product-owner.md:51-55`, `engineering-manager.md:47-52` | ✅ met |
| AC-5.2 | `product-owner.md:55-61`, `engineering-manager.md:52-58` | ✅ met — all three conditions, both files, plus the name-which-applied duty |
| AC-5.3 | `product-owner.md:48-50,61-62`, `engineering-manager.md:45-47,58-59` | ✅ met r2 — both unconditional sentences restored outside the §9 branch (F4) |
| AC-5.4 | — | **`deferred`, no verdict claimed** — measures the *next* feature. Not passed, not failed |
| NFR-1 | Counts re-run by me: skills 10, agents 7, lenses 9, templates 6, `/charter` steps 5, new `${CLAUDE_PLUGIN_ROOT}` paths 0 | ✅ — unmoved by the fix round |
| NFR-2 | `templates/constitution.md` +45/−0, single insertion hunk; §3 protected table untouched | ✅ — additive, minor-bump shaped |
| NFR-3 | Template comment `:129-149`; both greps re-run by me → one, different file each | ✅ |
| NFR-4 | Entries 3–7, 13, 16 | ✅ met r3 — the skipped path degrades to exactly today's behaviour again; re-traced by me on Venue C, not read off the fix note (F16) |
| NFR-5 | `claude plugin validate .` green (re-run by me); five documented runs | ✅ met r3 — all five now carry quoted output (47 blockquote lines added across Entries 11/12), corroborated against both fixtures (F17) |
| NFR-6 | Entry 10's counts; §9 re-measured by me | ✅ — ≤ 7 questions, ≤ 2 rounds, 0 sentences added on a constitution-bearing repo |
| NFR-7 | `README.md:183-191`, `:366`, `ROADMAP.md:42` | ✅ met r2 — the auto-handoff claim is gone (F6); maturity stays venue-bounded |
| NFR-8 | `facilitator.md:9` `tools:` byte-identical (no Bash); `skills/charter/SKILL.md:31-35` read-only `git log`; `:73-74` explicit-go | ✅ |

## 5. What Was Checked

- [x] All five findings round 2 left open re-verified against the live file or a fresh fixture inspection, never against the round-3 fix notes — 5 confirmed fixed, 0 disproved
- [x] F16 re-traced by hand end to end: bullet condition byte-compared against `git show HEAD:`, then walked against Venue C (constitution absent → bullet 1 → explicit skip → step 2; fallback AND false on 3 commits). Control reaches the Product Owner; the dead-end is gone
- [x] Regression sweep the caller asked for: `claude plugin validate .` green (one pre-existing `autoUpdate` warning); NFR-1's five surface counts unmoved (10/7/9/6/5, 0 new plugin-root paths); `templates/constitution.md` §1–§8 byte-identical (`git diff -U0` → the same single insertion hunk, +45/−0)
- [x] Evidence audited as the test artifact: Venues C and D re-inspected on disk and every checkable fact in Entries 11, 12 and 16's quotes matched against them (`src/main.py`'s two lines, the `fatal:` git message, the three commits, `package.json`'s single export)
- [x] Fix-collateral hunt: all three files touched this round re-read in full context, the other eight spot-checked at their load-bearing lines and found unmoved. Three residues found, all artifact wording, none behavioural (F20, F21, F22)
- [x] Security / error handling: unchanged — no user input parsed, one read-only `git log`, nothing executed unasked; every degrade-to-silence path re-read and all now hold
- [ ] Not checked: live re-performance against the installed plugin (plan §4 defers it to `/demo-day`); AC-3.4 remains honestly labelled `not-verified-live`

## 6. Verdict

This round the fixes are real, and I know that because I checked them the same way I disproved two of them last round — not by reading the notes. The one that mattered, F16, is genuinely closed: `skills/next-steps/SKILL.md:57`'s condition is byte-identical to `HEAD` again, and walking it by hand against Venue C — three commits, real `src/`, no constitution, user skips — bullet 1 routes the skip to step 2 and the fallback bullet's AND fails on the second half, so nothing tells a user with working code that "there is no state to read yet". Control reaches the Product Owner. F11's third citation is written for real this time: `:304` names `:169-170`, `:173-174` and `:181-184`, and I opened all three — kebab-case, Conventional Commits with one branch per feature, and the English-throughout rule the first attempt would have missed. The false completion record is corrected the honest way, by a new Amendments row that quotes what the old one wrongly claimed instead of editing it out of existence, with Entry 14's bad verdict left visible under a warning. Entries 11 and 12 now carry 47 lines of real transcript, and the quoted content matches the fixtures on disk down to `src/main.py`'s two lines and `package.json`'s single export. F18's git-summary duty now covers a §9 amendment, and `/charter` is still five steps. The regression sweep is clean on every axis the caller named. What is left is three wording defects inside `.spark/` documents, and I want to be precise about why they do not block: none hides an unwritten fix, none changes a verdict or a Must AC, and all three are visible from the rows above. They are still worth carrying — F21 in particular, because Entry 16 claims a re-verification ("checked by re-opening the file") that re-opening the file does not support, and this loop has now twice been saved by someone distrusting exactly that kind of sentence. Two of them are a word each; F20 needs a `/charter` pass because the constitution has no other writer. On the code and the acceptance criteria, this passes. `passed`.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings
- [x] No open Major findings — F15 and F16 both independently confirmed fixed this round. Three findings stay open (F20, F21 Minor; F22 Nit), all artifact wording, none blocking
- [x] Every Must AC traces to implementing code; no constitution non-negotiable violated — all 12 Must ACs trace; AC-1.2/AC-1.3's skip path re-traced live and restored; AC-2.6/AC-4.2 now hold for every §9 entry. No non-negotiable violated
- [x] All plan deviations documented and accepted — constitution §3's "new concern is a new file" deviation is recorded in plan §1 and user-approved at the plan gate
- [x] Test suite runs green — none exists; `claude plugin validate .` re-run by me this round: passed (one pre-existing `autoUpdate` warning)
- [x] Line budget respected: Ist 136 / Soll ~150 (excluding HTML comments; this report carries none)
- [x] Status set to `passed`
