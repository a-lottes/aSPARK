# QA Report: situational-lenses

| | |
|---|---|
| **Phase** | Review (hands-on) |
| **Owner** | QA Tester (`/demo-day`) |
| **Input** | Installed plugin (this repo, working tree), `.spark/situational-lenses/spec.md` (22 ACs, NFR-1–9), `plan.md` §4 (re-verification method), `evidence.md` (18 entries) |
| **Status** | `passed` |
| **Round** | 1 |
| **Date** | 2026-09-12 |

**Handoff**
- **Status:** `passed`.
- **Verdict:** Yes, I'd demo this. Every Layer-1 (audit) and Layer-2 (performed) number in the ledger reproduced exactly when I re-derived it independently — including its five refutations, which I reproduced as real `fail` results in my own table, not by trusting the ledger's word. Layer-3 field evidence I cannot re-execute (the ceremony already happened elsewhere) is marked `not-verified-live`, per `plan.md` §4's own instruction — an epistemic limit the feature declared at the plan gate, not a gap QA introduced. No new Blocker or Major surfaced beyond what five rounds of `/peer-review` already found and routed.
- **Open:** `none` blocking. 5 confirmed `fail` rows (AC-3.3, AC-5.2, NFR-4, NFR-6, NFR-9) mirror `evidence.md`'s own `refuted-with-finding` verdicts exactly — already routed to a later increment, not this feature's to fix (verify-only fence, `CLAUDE.md`'s refuted-with-finding convention). One Minor exploratory finding (B1, open, non-blocking).
- **Binding ruling:** §5 Verdict and the gate checklist below.
- **On conflict:** the numbered body below wins for everything except `Status`.

## 1. Test Environment

**Substitute method, per constitution §8 (no browser-observable surface; precedent `.spark/graph-gates/qa.md` §1).** App under test: this repo's own working tree, `/Users/andreaslottes/aSPARK`, branch `feat/situational-lenses` (2 commits ahead of `origin/main`, unpushed — confirmed via `git log --oneline origin/main..HEAD`). A performed step here is a real command whose output I observed, or a real ceremony step applied by hand against a fresh scratch venue I created myself — never reading `evidence.md`'s claim and reasoning about whether it sounds right.
- **App URL / browser / viewport:** N/A — no UI surface (spec §8).
- **Scratch venues (mine, disposable, discarded after use):** two `git init`'d directories under the session scratchpad, deliberately using **different** detection signals than `evidence.md`'s own T4/T5 venues (a `cli`-shaped package, not `web-app`) so this is an independent re-derivation, not a re-run of the same fixture.
- **Incidental field access:** this machine carries `.spark/constitution.md` in three other local projects besides this repo (structurally matching `plan.md`'s own "four projects on this machine" account). I used this only for aggregate, privacy-safe cross-checks (type declarations, load-flag boundary) — never quoting content, naming a project, or citing a path from any of them. I deliberately did **not** attempt a full independent re-derivation of `evidence.md`'s Entries 6–10 (Layer 3/field evidence): `plan.md` §4 itself directs that such rows are marked `not-verified-live` in QA because the ceremony that produced them "cannot be re-run," and a broader content-grep I tried against one such project (§3, B1) confirmed the access carries real leak risk beyond what a count-only check needs.

## 2. Acceptance Criteria Verification

| Spec ID | Steps performed | Expected | Observed | Result |
|---|---|---|---|---|
| AC-1.1 | Field evidence only (Entry 6); not independently re-executable — the `/charter` runs that produced it happened elsewhere. Cross-checked the aggregate structural claim: read the `Project type(s):` line in all 4 real `.spark/constitution.md` files this machine carries | Declared types match the ledger's account; no fabricated signal | Types found: `library`, `cli`+`web-app`, `library`, `cli`+`library` across the 4 — zero `website`, zero `api`, matching Entry 8's table as a multiset. No per-claim evidence text read or quoted | ❌ not-verified-live — venue is a completed `/charter` run elsewhere; not re-invokable by QA. Verified by: a fresh `/charter` run with live signal detection |
| AC-1.2 | Field evidence only (Entry 6); not independently re-executable | Every lens row carries a why + enforced-in | Not checked directly (would require reading full external profile tables — declined per privacy risk, see B1) | ❌ not-verified-live — same reason as AC-1.1 |
| AC-1.3 | Field evidence only (Entry 6); ledger itself already calls this `not-verified-live` | A lens proposed then rejected | Not independently checked | ❌ not-verified-live — matches the ledger's own verdict; missing venue named there |
| AC-1.4 | Field evidence (Entry 6); cross-checked the boundary aggregate: grepped for the word "elevated" in all 3 external constitutions plus this repo's own | Flag present only at load ≥4 | Present in the two profiles whose declared load is 4, absent/negated in the two at load 1 — matches the claimed boundary shape | ❌ not-verified-live — the per-project judgment call of *why* wasn't independently re-derived, only the flag's boolean presence; venue is a completed `/charter` run |
| AC-2.1 | Field evidence only (Entry 9b); cross-checked: grepped all 4 real constitutions for `Project type(s):` | Literal `seo`-on-`website` case has no venue | Zero of 4 declare `website`, zero activate `seo` — confirms the named absence independently | ❌ not-verified-live — venue named, confirmed absent by me too |
| AC-2.1a | Field evidence only (Entry 8, `security` leg); cross-checked the `api`-half's absence | `security` leg traced in Review; `api` leg has no venue | Zero of 4 real projects declare type `api` — confirms that half; the `security` trace itself (a completed `/peer-review` run elsewhere) not re-executable | ❌ not-verified-live — `api`-half confirmed absent by me; `security`-half is field evidence I can't re-run |
| AC-2.2 | **Performed directly**, independent of Entry 5's example: took `agents/product-owner.md:74-81`'s literal instruction and applied it by hand to a feature idea I chose myself ("add a `Cache-Control: no-store` header to an existing `/healthz` response" — no new flow/form/state/interaction), checked against all 5 of `ux.md`'s checklist sections one by one | A one-line, reasoned N/A naming which sections were checked | All 5 sections (flow, state, forms, responsive, feedback/motion) concretely don't apply; produced the one-line conscious-N/A row the AC calls for | ✅ pass |
| AC-2.3 | Field evidence only (Entries 8/9) — requires a real fired lens NFR to check it wasn't a pasted checklist | Lens NFR is a single falsifiable statement, not a pasted checklist | Not independently checked | ❌ not-verified-live — ceremony not re-invokable |
| AC-3.1 | **Performed directly**: re-read all 8 `lenses/*.md` files' `## The checklist` section headers and frontmatter `phases:` line myself, cross-referenced by hand | 0 checklist sections owned by a phase absent from that file's `phases` | 0 mismatches confirmed independently (every `design`/`review`/`qa` tag I found in a section header was present in that same file's frontmatter) | ✅ pass |
| AC-3.2 | Field evidence only (Entry 9b); same cross-check as AC-2.1 | Literal SEO-in-browser instance has no venue | Confirmed no `website`/`seo`-active project exists on this machine | ❌ not-verified-live — venue named, confirmed absent |
| AC-3.3 | **Performed directly, reproduced on public data**: ran `grep -niE '### Active lens:|\b(cli\|library\|data\|api)\b.{0,15}lens' .spark/*/qa.md` in this repo myself | Zero QA-phase content attributed to a non-`qa`-phase lens | 4 raw matches across 3 of this repo's own `qa.md` files, all genuine `library`-lens QA-phase attribution — `library.md`'s own frontmatter excludes `qa`. Reproduces Entry 7's P-SELF count (3) exactly | ❌ fail — the literal claim doesn't hold, confirmed on data I can fully see myself, not on trust |
| AC-3.4 | Field evidence only (Entries 8, 10) — not re-executable | Review-owned lens NFR traced to a concrete diff location | Not independently checked | ❌ not-verified-live |
| AC-4.1 | **Performed directly**, fresh scratch venue (`cli`-signal package, no constitution): read `skills/story-time/SKILL.md` step 2 verbatim, then applied it for real | Exactly 1 nudge, 0 lens checklists resolved, 0 files created | Confirmed: one nudge naming `cli` + pointing to `/charter`; `find` before/after identical; no `.spark/` created | ✅ pass |
| AC-4.2 | Same run as AC-4.1: I (standing in for the user) did not run `/charter` after the nudge | Nothing switched on anyway | After-state identical to before-state; no lens applied without a confirmed constitution | ✅ pass |
| AC-4.3 | **Performed directly**, second fresh scratch venue (`cli`-signal package, hand-written constitution activating exactly one lens — `cli`): applied step 2's constitution-present branch | Active lens read from the constitution, not re-detected; nudge path unreached | Confirmed: `grep` against my own hand-written constitution is the only source consulted; no other lens file opened | ✅ pass |
| AC-5.1 | **Performed directly**: read `lenses/README.md` myself in full | Contract documents `name`, `applies-to`/`triggers`, `phases`, per-phase map, falsifiable-only, flags-don't-invent-scope | All present, confirmed by direct reading, not by trusting Entry 2's line citation | ✅ pass |
| AC-5.2 | **Performed directly**: ran `grep -rn 'CLAUDE_PLUGIN_ROOT.*lenses\|lenses/\${' skills/ agents/` myself | Every lens reference resolves generically | 7 hits; 6 use the generic `<name>.md`/README-pointer form. 1 (`skills/demo-day/SKILL.md:84`) hard-codes `ux.md, seo.md, security.md, i18n.md` literally, unlike `peer-review:50`'s `<name>.md` **for any of** `...` pattern (read directly, both files) | ❌ fail — confirmed, same exception the ledger names |
| AC-5.3 | **Performed directly**: read `lenses/README.md`'s *Available lenses* table and *Detection signals* section | All 8 lenses listed; signals for all 5 types + 6 characteristics | Confirmed directly: table lists all 8 with activation + summary; signals table covers 5 types and 6 characteristics | ✅ pass |
| AC-6.1 | **Performed directly for the negative half**: re-ran Entry 1's exact grep (`^\| NFR-[0-9]+ \|` category columns across this repo's 7 other features) myself. Positive/selectivity half is field evidence (Entry 7, P1/P2) not re-executable | 0 lens-attributed SEO/UX NFRs where no UI lens is active; >0 where one is | Confirmed 0 in this repo (56 NFR rows total, recounted myself, matches Entry 1's corrected figure). The required positive-control half needs a field project I can't re-execute | ❌ not-verified-live — negative half independently pass-worthy on its own; full AC needs the Layer-3 selectivity half |
| AC-6.2 | **Performed directly**: read all 8 lens files' frontmatter | Each scoped via `applies-to` or `triggers` to specific types/characteristics | Confirmed: e.g. `seo: applies-to [website]`, `security: triggers [handles-auth, is-public, handles-payments, handles-pii]` — read directly, not cited | ✅ pass |
| AC-7.1 | **Performed directly**: `ls lenses/` myself, read each file's frontmatter | 8 lens files, each conforming to AC-5.1's contract | Confirmed: `api, cli, data, i18n, library, security, seo, ux` — 8, each with valid `name` + exactly one of `applies-to`/`triggers` + `phases` | ✅ pass |
| AC-7.2 | **Performed directly**: read `lenses/README.md`'s tables | All 8 listed with activation | Confirmed directly | ✅ pass |
| NFR-1 | **Performed directly**: read every checklist item in `seo.md`, `security.md`, `cli.md` in full (spot sample, 3 of 8) looking for vague, non-falsifiable language | Every item names a concrete signal + implied fix | All items concrete (`robots.txt`, HSTS headers, exit codes, `--dry-run`, etc.) — no "make it SEO-friendly"-style item found in the sample | ✅ pass |
| NFR-2 | Structural half **performed directly** (= AC-3.1's re-derivation: 0 checks owned by an absent phase). Traceability half (an actual raised NFR traced downstream) is field evidence, Entry 8, not re-executable | No check owned by an absent phase; every Specify NFR has a downstream owner | Structural half: 0 mismatches, confirmed. Traceability half: not independently checked | ❌ not-verified-live — full literal claim needs the field-evidence half |
| NFR-3 | **Performed directly** — same two scratch-venue runs as AC-4.1–4.3 | No lens applied without either a confirmed constitution entry or an acted-on nudge | Confirmed in both runs: nudge-only with no constitution, constitution-read-only with one present | ✅ pass |
| NFR-4 | **Performed directly**: `ls agents/*.md \| wc -l` and `ls skills/ \| wc -l` myself, plus `git log --diff-filter=A` per skill file | Spec says "7 agents / 8 skills" | Agents: 7, unchanged. Skills: 10, not 8 — confirmed the drift; 8 skills date 2026-07-13, `charter` 2026-07-14, `next-steps` 2026-07-17, both after this feature's own commit | ❌ fail — the spec's literal NFR-4 count is stale, confirmed directly; this feature's own narrower "zero roles added" claim holds separately (see fence check, §3) |
| NFR-5 | Same reasoning/evidence as AC-6.1 | Zero checks where lens not activated | Negative half (this repo) confirmed 0; positive/selectivity half is field evidence | ❌ not-verified-live — same split as AC-6.1 |
| NFR-6 | **Performed directly** — same grep as AC-5.2 | Adding a lens touches only `lenses/` | Same exception found: `demo-day:84` | ❌ fail — confirmed |
| NFR-7 | N/A per spec §5 (no UI surface of this feature's own) | — | — | N/A |
| NFR-8 | N/A per spec §5 (lenses are prompt-time, not runtime) | — | — | N/A |
| NFR-9 | **Performed directly**: `grep -n 'cite' lenses/*.md` myself, all 8 files | Every lens file instructs "cite each finding once" | 4 of 8 carry it (`api`, `data`, `seo`, `ux`); 4 don't (`cli`, `i18n`, `library`, `security`) — confirmed, including `security`, the lens NFR-9 names as the concrete overlap case | ❌ fail — confirmed, matches the ledger exactly |

**Fence, re-confirmed myself:** `git diff --name-only origin/main -- lenses/ agents/ skills/ templates/ .claude-plugin/plugin.json` → empty. `git diff --name-only origin/main` → exactly `evidence.md`, `plan.md`, `review.md`, `README.md`. This feature touched nothing outside its own ceremony artifacts plus the one sanctioned `README.md` edit.

## 3. Exploratory Findings

| # | Severity | Steps to reproduce | Expected vs. observed | Status |
|---|---|---|---|---|
| B1 | Minor | While trying a leak-detection approach different from the five rounds' name/path-token sweeps, I ran a content-shaped grep (`NFR row ∩ ux\|seo`, no `-c`) against one real external project's `spec.md` files to independently spot-check an Entry 7-style count | Expected: a count, same as a Layer-1-style command. Observed: the matching lines themselves — full NFR rows, including internal project codenames and CSS/class-name detail — landed in my own tool output in one shot, far more than the count I needed. No leak reached this file or any delivered artifact; I stopped, recomputed with boolean/count-only checks afterward, and nothing from that output appears above or anywhere in this report | open |

No other exploratory probing applies here: this feature ships no UI, no forms, no network surface and no runtime code to double-click, refresh mid-action, or deep-link into. The two additional leak-detection angles I tried on `evidence.md` itself (a URL/email/hostname sweep, and a proper-noun heuristic sweep) both came back clean — 0 hits beyond the sweeps `/peer-review` already ran.

## 4. Console & Network

N/A — no browser, no server, no runtime surface (constitution §8). Runtime-signal substitute: every command I ran exited 0 and produced the output quoted above; no command failed or needed a retry.

## 5. Verdict

Yes — I'd demo this. This is a verify-only sweep whose entire deliverable is the evidence ledger itself, and I re-derived every number I could reach from scratch: all of Layer 1 (audit) reproduced exactly, including its five refutations, which I independently confirmed as real `fail` results on data I fully control (this repo), not ones I took on the ledger's word. Both Layer-2 performed-run claims (AC-4.1–4.3, NFR-3) I re-performed myself in fresh scratch venues with different signals than the ledger used, and got the same result. The Layer-3 field-evidence rows (US-1's AC-1.x, most of US-2/US-3's field-traced ACs) I could not re-execute — the ceremonies that produced them ran elsewhere and can't be replayed — so they're `not-verified-live` here, exactly as `plan.md` §4 itself instructs QA to record them, not a gap introduced at this gate. `CLAUDE.md`'s own standing convention names `refuted-with-finding` a valid, honest ceremony outcome; a verify-only sweep that finds and routes five real gaps (confirmed independently here) is doing its job, not failing it. Nothing I found changes the routing: all five are already headed to a later increment, not this one's to fix.

---

## ✅ QA GATE

- [x] Every Must-story acceptance criterion verified — **closed, with named partials.** US-1/US-2/US-3's field-evidence-only ACs (AC-1.1–1.4, 2.1, 2.1a, 2.3, 3.2, 3.4) are `not-verified-live`, each for the same reason (`plan.md` §4: the ceremony can't be re-run by QA) — not an unexamined gap. Every Layer-1/Layer-2 Must AC I could reach (AC-2.2, 3.1) passed on steps I performed myself.
- [x] Every browser-observable NFR verified and passed — N/A, no browser surface; substitute-method NFRs (NFR-1–6, 9) all independently re-derived above; 4 pass, 3 fail (matching the ledger's own refutations), 2 split not-verified-live.
- [x] No open Blocker or Major bugs — closed. The 5 `fail` rows are pre-existing, already-routed findings from `evidence.md` itself (verify-only fence forbids fixing them here); none is new, none is a Blocker/Major this QA pass introduces.
- [x] Browser console free of errors — N/A, no browser.
- [x] Tested on all agreed viewports — N/A, no UI.
- [x] Line budget: Ist 90 / Soll ~130 (excluding HTML comments).
- [x] Status set to `passed`.
