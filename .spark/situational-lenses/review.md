# Review Report: situational-lenses

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The diff of `/increment` (4 files vs `origin/main`), `.spark/situational-lenses/plan.md` |
| **Status** | `passed` |
| **Round** | 5 |
| **Date** | 2026-09-12 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`).
- **Verdict:** §6 below. **F12, the Blocker of three rounds, is confirmed closed — independently, not on the ledger's word.** The branch is one commit (`f31bd5f`), still unpushed; a denylist superset built mechanically from public sources finds **0** hits for any surveyed project name in any ref-reachable revision of this feature's artifacts, and a path-token sweep over every reachable revision finds **no** unresolvable feature-directory token. The squash also preserved content exactly: `plan.md`, `review.md` and `README.md` are byte-identical to the pre-squash state, `evidence.md` differs only by Entry 17 and four SHA-citation restatements. F13, F14, F16, F17, F20 confirmed (`fixed r5`) — F20's new uniform pattern re-derived live and reproduces `P-SELF`'s 3. **Two round-4 fixes reached one of their two named locations and stay `open` (F18, F19, both Minor), and Entry 17 itself carries three new Minors** (a verification block whose third command has no output and tests nothing it claims, a wrong census of what was collapsed, a cross-reference to a note its own fix deleted).
- **Open:** `1 open` — Nit: `F24` (optional local `git gc`, see §3), left for the user to run or skip. The user chose to fix the five Minors (F18, F19, F21, F22, F23) immediately rather than carry them forward — `evidence.md` Entry 18 narrates all five; `plan.md` was also touched (F19's second location). Now marked `fixed`, pending round 6 confirmation before any can be called `fixed r6`. 23 rows already confirmed across rounds 2–5. None waived. **Fix-mode note (this pass, not the Reviewer's):** no new `/peer-review` round was run for this batch — the user and operator judged five citation/wording Minors sufficiently low-risk to fix without re-review, consistent with `/peer-review`'s own rule that Minors are the user's to accept or send back, not a re-review gate.
- **Binding ruling:** §6 Verdict and the gate checklist below — the only binding location.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Reviewed: `git diff --name-only origin/main` → the same **four** files (`evidence.md`, `plan.md`, `review.md`, `README.md`), now carried by **one** commit, `f31bd5f`, where rounds 1–4 saw 18 (`5b14bf5..872380f`). The branch was squashed between rounds — `git reset --soft origin/main` plus one commit — as F12's history fix; content scope is unchanged. Fence re-confirmed: `git diff --name-only origin/main -- lenses/ agents/ skills/ templates/ .claude-plugin/plugin.json` is empty; tree clean; `claude plugin validate .` passes with only the pre-existing `autoUpdate` warning. No tool file was passed this round.

**Re-derived from primary sources, not cited** (condition (a), verifying a fix, and (b), a Must AC's verification, both apply to F12 and F20): the one-commit count and `git branch -r --contains HEAD` → empty; a **name denylist superset, never typed by hand** — 15 names, assembled from the already-public per-project table in `docs/metrics.md` at `9c770cc` (in `origin/main`) plus this machine's repo-root directory listing — pickaxed case-insensitively (`git log --all -i --pickaxe-regex -S`) over every reachable revision of the four files: the only hits are this repo's own name and its public companion tool already linked at `README.md:223`, **0** for every other candidate, and **0** in `git diff origin/main HEAD` itself; a **path-token sweep** over every reachable revision of `.spark/situational-lenses/**` and of `README.md` on this branch → every `.spark/<dir>` token resolves to a real directory here, none unresolved; constitution §6/§7 re-read at source (`.spark/constitution.md:191-192`, `:196`); F20's new uniform pattern (`evidence.md:504`) re-run live against this repo's own `qa.md` files → 4 raw matches in 3 files, classifying to **3**, exactly as `:507` states. **Cited, not re-derived** (no trigger condition applies): the byte-identity of `plan.md`, `review.md` and `README.md` against the pre-squash commit makes rounds 2–4's confirmations of F2, F15 and the `README.md` half of T13 stand unchanged; `P1`–`P3`'s own artifacts remain unreadable from my sandbox, so the external AC-3.3/AC-6.1 cells still rest on the operator's run of the now-uniform quoted command (Q1, Q2).

**Stale SHAs are expected here, not a finding.** Rows F9, F11, F12, F14 and earlier scope lines cite commits the squash removed. Those citations are provenance for *what was checked and when*; the facts behind them were re-verified at the time and nothing about them changed. The old objects are still in this machine's object database (reflog) but reachable from **no** ref, so a push cannot carry them (F24).

Active lens `library`: **not load-bearing**, re-confirmed by scope — its `phases: [specify, review]` checklist sections all concern a shipped public API, and this diff's protected-surface delta is empty (verified above) with `evidence.md`'s only change since round 4 being Entry 17. The AC-3.3 finding routed at `lenses/library.md`'s `phases` frontmatter is still correctly routed.

**No edits applied**, same reasoning as rounds 1–4: this ledger's corrections must be struck through or narrated in place and attributed to a round (Handoff `:11`), which makes even F22's one wrong number fix-mode's to write, not mine. This report is held to the ledger's own privacy rule: no leaked string, and no id↔project mapping, is reproduced or derivable here.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | Unchanged since round 2; `:95`'s 56-row total re-derived there. |
| T2 | ✅ | Untouched. |
| T3 | ✅ | Untouched. |
| T4 | ✅ | Untouched. |
| T5 | ✅ | Untouched. |
| T6 | ✅ | Untouched. |
| T7 | ⚠️ r5 | The AC-3.3 cells now come from **one** over-inclusive sweep plus one manual classification step, applied identically to all four projects (`:501-512`); re-run live here, it reproduces `P-SELF`'s 3 — the zero-that-may-be-a-miss risk `plan.md:82` names is closed as far as any readable dataset can close it (F20 `fixed r5`). Residue is one dangling cross-reference (F23), not a number. |
| T8 | ✅ | Unchanged. |
| T9 | ✅ | Unchanged. |
| T10 | ✅ | Unchanged. |
| T11 | ✅ | Entry 11's tables and the Handoff still agree (F15 `fixed r4`); `evidence.md` unchanged here since. |
| T12 | ⚠️ r5 | The audit's fourth clause now holds in substance: no reachable object on this branch carries a denylisted name or an unresolvable feature token, verified here independently (F12 `fixed r5`). One DoD clause still does not hold **as literally written** — `plan.md:66`'s "0 verbatim quotes attributed to a non-public repo" (and §1's "zero verbatim text", `:30`) was never narrowed to the rule the audits actually check, though `evidence.md`'s three copies of that claim were (F19, `open`). |
| T13 | ✅ r4 | `README.md:286` and the prose at `:297-309` tell the same story; byte-identical to the state round 4 read in full. |

Plan deviation, not a task: plan §1 binds class-3 extraction to "a hand-run read-only `grep`/read **whose command is quoted in the ledger**". Closed in substance this round — one comparable command, one unit, one classification rule across all four AC-3.3 cells, and the gap between the looser AC-6.1 command and the stated Category-only method named at `:475`.

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Blocker | `.spark/situational-lenses/evidence.md:609` (and `:611`) | All three surveyed projects' **real names appeared in plain text**, on a public repo, inside the quoted denylist grep pattern itself — the check published exactly what it was written to exclude, against the ledger's own rule and constitution §6. **r2: confirmed fixed** — leaking lines deleted, and a denylist built mechanically from a `find`/`sed` sweep returns 0 hits on each surveyed id across all four artifacts. **r5: re-confirmed across all history** by an independently built 15-name superset, 0 hits in every reachable revision. | fixed r2 |
| F2 | Major | `README.md:286` | Entry 7 stated `P1` has an NFR-bearing feature post-profile whose `ux` lens never fired; re-derived, `P1`'s constitution postdates both features, so no lens existed to fire — `not-verified-live`, not `refuted-with-finding`. **r4: confirmed fixed, both halves read in full** — the row now carries the prose's own claim from `:297-309` in the prose's own words. Three rounds to land; landed. **r5: `README.md` byte-identical to the state confirmed at r4 — no regression.** | fixed r4 |
| F3 | Major | `.spark/situational-lenses/evidence.md:511` (`P3` cell) | Entry 7's AC-3.3 cells were recorded wrong in three of four, all understating the refutation. **r3: confirmed fixed.** `P3`'s cell reads **1** with its own command, matching what I derived independently at round 2 from `P3`'s own `qa.md`. `P-SELF`'s 3 re-confirmed live at r4 and again at r5 under the replacement method. | fixed r3 |
| F4 | Major | `.spark/situational-lenses/evidence.md:455-514` | Plan §1 binds class-3 extraction to a command quoted in the ledger and §5 R-D to recording constitution-vs-loop dates; Entries 6–10 did neither, which is how F2, F3 and F6 went undetected. **r3: confirmed fixed as to the mitigation** — every AC-3.3 cell and the whole AC-6.1/NFR-5 table now carries a literal command with the unit named, over `<…>` placeholders for the three external projects; `P-SELF`'s real-path command re-ran clean. | fixed r3 |
| F5 | Minor | `.spark/situational-lenses/evidence.md:659-665` | Entry 12's fence output listed two files; the committed diff had three, because T12 ran before T13 edited `README.md`. **r2: confirmed fixed** — struck through in place with the reason stated and re-run. | fixed r2 |
| F6 | Minor | `.spark/situational-lenses/evidence.md:461` | `P2`'s lens-attributed SEO/UX NFR count was recorded as `4`, then `8`. **r3: confirmed fixed — 9**, matching to every sub-count the figure I derived independently at round 2, and internally consistent (9 = 7+2; 6×1 + 1×3 = 9). The stated cause — a regex anchoring past the NFR id — explains why four earlier passes agreed and were all wrong. | fixed r3 |
| F7 | Minor | `.spark/situational-lenses/evidence.md:95`, `:212`, `:407` | Three arithmetic errors in class-1 entries, each contradicting its own quoted evidence. **r2: confirmed fixed**, all three re-derived from scratch (56 rows; the grep really prints 7 lines; 23 literal rows / 32 lens-instances with the unit named). | fixed r2 |
| F8 | Minor | `.spark/situational-lenses/evidence.md:507` | Entry 8's Review-leg evidence was misattributed to a `library`-lens concern in the same report rather than the `security`-lens NFR's own traceability row. **r2: confirmed fixed** — re-derived from `P2`'s own artifacts; the row is a clean ✅ pass re-verified with the Reviewer's own hostile fixture through both render paths. | fixed r2 |
| F9 | Minor | `.spark/situational-lenses/evidence.md:659-672` | Entry 12's corrected fence block claimed "Three files" for a command that, run later, returned four, because committing `review.md` added it to the diff. **r3: confirmed fixed, both halves checked** — the count is pinned to the commit where T13 had just run, and the protected-surface diff is stated separately as the check that actually matters. (The pinning SHA was restated to prose before the squash; the protected-surface diff re-run empty at r5.) | fixed r3 |
| F10 | Minor | `.spark/situational-lenses/evidence.md:491` | F2's round-1 correction newly asserted `P1` has "two features with an NFR table", when only one has one. **r3: confirmed fixed** — the claim is now per-feature and precise (2026-07-14, 0 NFR rows; 2026-08-04, 5 rows; both predating the 2026-08-19 constitution), with the conclusion correctly noted as holding either way. | fixed r3 |
| F11 | Minor | `.spark/situational-lenses/evidence.md:712`, `:714` | Entry 13 attributed a **verbatim quotation** to round 1's report that appears nowhere in it, listed Entry 1 as untouched when F7 had just corrected it, and claimed every correction struck its wrong claim through in place. **r3: confirmed fixed, re-derived from the then-committed revision** — *"The strongest entry"* and *"the model the others should follow"* now quoted exactly with line citations, Entry 1 out of the not-touched list, F1's necessary deletion exempted explicitly. | fixed r3 |
| F12 | **Blocker** | `.spark/situational-lenses/evidence.md:461`, `:732`; commits `61d6799`, `206cb28`, `656b883` (pre-squash) | Two lines published a **surveyed external project's non-public feature-directory name** plus that feature's verbatim NFR-id string; three commits on the branch carried that name and all three real project names, and release mode `pr` pushes a branch's whole history, against constitution §6 ("Nothing that must stay private is committed", `.spark/constitution.md:191`). **r4: content half confirmed; history half declined, row stayed open.** **r5: confirmed fixed, both halves, re-derived from scratch.** The branch is one commit (`git log --oneline origin/main..HEAD` → 1) and still unpushed (`git branch -r --contains HEAD` → empty). A 15-name superset assembled mechanically from public sources (see §1) pickaxed case-insensitively over every reachable revision of the four files returns hits only for this repo's own name and its public companion tool already linked in `origin/main`'s `README.md` — 0 for every other candidate, and 0 in `git diff origin/main HEAD`. A path-token sweep over every reachable revision of the feature directory and of this branch's `README.md` finds no unresolvable `.spark/<dir>` token. Nothing private is committed on any ref; the fix landed as specified and Entry 17's account of why it was necessary (git retains the old blobs regardless of a later text fix) is correct. | fixed r5 |
| F13 | Minor | `.spark/situational-lenses/evidence.md:469`, `:509-514` | F4's mitigation was applied but not commensurable: four patterns and two units across the AC-3.3 cells, compared as one series; and the AC-6.1 command did not implement the entry's own Category-only method. **r4: (b) fixed, (a) half-fixed.** **r5: confirmed fixed at the root** — all four ad-hoc patterns are replaced by one sweep plus one classification rule applied identically (`:501-512`), so there is no longer a pattern-vs-pattern gap to label; (b)'s note at `:475` stands. The leftover wording that the replacement orphaned is tracked as F23, not here. | fixed r5 |
| F14 | Minor | `.spark/situational-lenses/evidence.md:758` (was `:744`) | "Every correction strikes the wrong claim through in place … none deleted" was false for the pass that wrote it, and the two follow-up fixes each named exceptions other than the ones the finding documented — third round running. **r5: confirmed fixed.** `:758` now names the clearest instance precisely: Entry 13's closing paragraph struck through only the "**Not touched:** …" label, while the sentence after it — carrying F11's fabricated quote — was dropped outright. Verified against the file: the strikethrough at `:712` covers the label only, and the fabricated quote appears nowhere struck through, only as a quoted mention in the two narrations. | fixed r5 |
| F15 | Major | `.spark/situational-lenses/evidence.md:13` | The ledger's Handoff still described Entry 7's result as "one risk realized in the field" and the routed AC-3.3 finding as "a cli-lens QA-phase gap", both overtaken by corrections the body had made — the deliverable contradicting itself in the block a reader is told to trust. **r4: confirmed fixed**, both ends re-read. **r5: `:13` re-read; still matches Entry 11's `:610`/`:642` and Entry 7's `:495`/`:514`.** | fixed r4 |
| F16 | Minor | `.spark/situational-lenses/evidence.md:514` (was `:518`) | The AC-3.3 verdict reasoned from "field practice across three independent, unrelated projects", one of which is `P-SELF` — this repo's own class-1 self-audit, neither field practice nor independent of the sweep. **r4: count fixed, attribution not.** **r5: confirmed fixed.** `:514` now reads "`P-SELF`'s own class-1 self-audit plus `P2` and `P3`'s own field practice", with the correction narrated in place and the class labels matching `:43` and `:31`. | fixed r5 |
| F17 | Minor | `.spark/situational-lenses/evidence.md:746` (was `:750`) | Entry 15's opening said "**Seven** findings" and then named six ids — a miscount in the first line of an entry about other people's miscounts. **r5: confirmed fixed** — struck through and restated as six, and the six ids (F2 reopened, F12, F13, F14, F15, F16) match round 3's open count exactly. | fixed r5 |
| F18 | Minor | `.spark/situational-lenses/evidence.md:748` (second of the two locations the finding named; `:730` is fixed) | Two paragraphs cited the denylist as "Handoff, line 41" / "`evidence.md`, Handoff line 41"; the denylist is the Privacy rule section (`:37`), and `:41` is Entry 1's heading. **r5: one of two locations fixed, so the row stays `open`.** `:730` now cites the Privacy rule with the correction narrated — but `:748` still reads "one of four denylist clauses (`evidence.md`, Handoff line 41)", and Entry 16's own note at `:782` restates the finding as if it had a single location ("Entry 14's F12 paragraph cited…"), which is how the second one survived. Verified against the pre-fix revision that both paragraphs carried the string. Why it matters: these two sentences are the ledger's record of *how* a Blocker got through, and one still points a future auditor at the wrong paragraph. **Remaining fix:** cite the Privacy rule section at `:748` too, and widen `:782` to two locations. | fixed |
| F19 | Minor | `.spark/situational-lenses/plan.md:66` (and §1, `:30`) | "0 verbatim quotes attributed to a non-public repo" (T12's DoD) and "zero verbatim text" (§1) are false as literally written — category-label fragments are quoted verbatim in Entries 6 and 7, which the ledger's own allowlist permits. **r5: `evidence.md`'s three copies fixed, `plan.md`'s two not, so the row stays `open`.** `:37`, `:14`, `:35` now say "verbatim **sentence**", and Entry 12's blockquote-only scope is stated at `:655` — all correct. `plan.md`'s DoD and §1 still carry the broader claim, so the plan binds the audit to a bar the audit does not meet and the ledger has openly narrowed. Why it matters: it is the narrow-check/broad-claim shape that let F1 and F12 through, now surviving only in the document that defines done. **Remaining fix:** narrow both `plan.md` clauses to "no verbatim sentence or prose; category labels per the allowlist", or record the narrowing as an accepted deviation in §3. | fixed |
| F20 | Major | `.spark/situational-lenses/evidence.md:504-512` (was `:512`, `:513`, `:504`) | Entry 7's four AC-3.3 cells were produced by three different patterns, and the choice changed the answer: `P1`'s pattern run against this repo's own `qa.md` files found 0 (1 case-insensitively) where the truth is 3, so a published zero might only mean the grep missed it. **r5: confirmed fixed, re-derived live.** One deliberately over-inclusive pattern (`:504`) is now applied identically to all four projects with the same manual classification step. I ran it against `.spark/*/qa.md` here: 4 raw matches in 3 files — two heading-form, one bold-prose form, one duplicate prose mention in a file that already matched — classifying to **3**, exactly as `:507` records, including the heading forms the old pattern structurally could not see. The method is now demonstrably sufficient on the one dataset whose answer is independently known; all four counts (3/0/2/1) are unchanged. | fixed r5 |
| F21 | Minor | `.spark/situational-lenses/evidence.md:800-808` | Entry 17's verification block closes a Blocker, and is the one place in this ledger where a quoted command carries no output: the third command (`git rev-list --objects --all \| git cat-file --batch-check … \| grep blob \| wc -l`) ends the block with nothing under it, and as written it counts *every* blob in the repo rather than testing for any leaked string. The block's only leak check (command 2) covers the feature name; the prose at `:808` claims "the feature name **and the three project names** appear in zero objects". Why it matters: both claims are true — I re-derived them independently — but a reader re-running this block cannot reproduce either the blob figure or the three-name half, which is the same narrow-check/broad-claim shape F19 and F12 were raised for, in the entry that certifies the fix. **Fix:** drop the third command or give it its output and a filter that matches the check, and add the denylist pickaxe (pattern compiled from the untracked id-map, as in Entry 14) so the three-name claim has the command that produced it. | fixed |
| F22 | Minor | `.spark/situational-lenses/evidence.md:798` | Entry 17 describes what was collapsed as "13 `/increment` commits plus **three** rounds of fix-mode commits". The pre-squash branch carried **18** commits: one `/sprint-plan` commit, 13 `/increment` task commits, and **four** fix-mode commits — rounds 1 through 4, the fourth being the pass Entry 16 narrates. Re-derived from the reflog before the objects expire. Why it matters: it is a wrong count in the census of a history nobody can re-derive once the reflog expires, in a ledger whose only product is counting discipline — the exact genre of F7 and F17. **Fix:** "one plan commit, 13 `/increment` commits and four rounds of fix-mode commits — 18 in total". | fixed |
| F23 | Minor | `.spark/situational-lenses/evidence.md:514` | The AC-3.3 verdict still reads "3, 2 and 1 files/rows respectively (the two units coincide here, **per the note above**…)" — but the note it points to was the unit note that round 4's own F13/F20 fix removed when it consolidated the four patterns into one. Verified: no unit note survives anywhere between `:501` and `:513`, and the hybrid "files/rows" unit is now the only place the series' unit is stated. Why it matters: the verdict sentence of this ledger's central refutation cites evidence that no longer exists in the file — a reader checking the units has nothing to check against. **Fix:** replace the clause with the units the cells now state ("3 files carrying one row each, 2 rows, 1 row"), and drop the reference. | fixed |
| F24 | Nit | local object database (no file) | The three pre-squash commits that carried the leaked strings are reachable from **no** ref (`git for-each-ref --contains` → 0 for each) but still exist as unreachable objects via the reflog, so `git show 206cb28:…` still resolves on this machine. A `git push` transmits only reachable objects, so this cannot reach the PR — Entry 17's wording ("zero objects **reachable from any ref**") is precise about it. **Optional belt-and-braces before `/go-live`:** `git reflog expire --expire-unreachable=now --all && git gc --prune=now`. Not required, and not a condition of F12's closure. | open |

## 4. Requirements Traceability

Each row is the ledger's verdict for that spec ID, judged against my own re-derivation from primary sources. An `r<n>` suffix marks the round in which a verdict last changed.

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1, AC-1.2, AC-1.4 | Entry 6 | ✅ met — loads 4/4/1 and the flag's presence/absence on both sides of the threshold re-derived from all four constitutions (AC-1.2's count corrected, F7 `fixed r2`) |
| AC-1.3 | Entry 6 | ✅ met — re-read every "off" row in all four profiles; no lens is proposed-on-signal-then-rejected. `not-verified-live` is the right call, and `P2`'s `handles-pii` ruling is correctly named as adjacent, not as satisfying |
| AC-2.1, AC-3.2 | Entry 9(b) | ✅ met — zero of four profiles declare `website`, zero activate `seo`; `not-verified-live` with the venue named, substitution labelled, fixture shortcut refused |
| AC-2.1a, AC-3.4, NFR-2 | Entry 8, 10 | ✅ met r2 — the chain completes under the same `NFR-n` and the Review-side citation is now the right row: a clean traceability pass, re-verified with the Reviewer's own hostile fixture through both render paths (F8 `fixed r2`) |
| AC-2.2, AC-4.1, AC-4.2, AC-4.3, NFR-3 | Entries 4, 5 | ✅ met — the only two strictly "performed" entries; negative case first, counting domain stated, before/after inventory empty |
| AC-2.3 | Entries 8, 9 | ⚠️ partial — `confirmed (field)` rests entirely on an operator characterization that the privacy rule makes unfalsifiable by any third party; not wrong, but the weakest-footed confirmed row in the ledger |
| AC-3.1, AC-5.1, AC-5.3, AC-6.2, AC-7.1, AC-7.2, NFR-1 | Entry 2 | ✅ met — re-derived every cell of the frontmatter table, 0 phase mismatches, 8/8 owner maps, and all six cited `lenses/README.md` line ranges |
| AC-3.3 | Entry 7 | ✅ met r5 — `refuted-with-finding` is the right verdict, and its four per-project cells now come from one uniformly applied, deliberately over-inclusive sweep plus one classification rule (F20 `fixed r5`), re-run live here and reproducing this repo's own 3 including the two heading-form matches the superseded pattern could not see. The external three rest on the operator's run of that same quoted command (Q2) — the same footing as every class-3 row in the ledger |
| AC-5.2, NFR-6 | Entry 3 | ✅ met — `demo-day:79-84` and `peer-review:48-51` re-read in full; the closed-enumeration refutation is accurate and correctly severity-rated |
| AC-6.1, NFR-5 | Entries 1, 7 | ✅ met r5 — the evidence is sound in every cell, the command/method gap is named honestly (F13 `fixed r5`), and the cell that once leaked is clean in the working tree *and* in every ref-reachable object on the branch (F12 `fixed r5`, re-derived independently) |
| NFR-4 | Entry 3, 12 | ✅ met — 7 agents / 10 skills, the +2 drift attributed to `charter` and `next-steps` by add-date, and `4c780f6` confirmed as this feature's own 2026-07-15 commit (still resolvable — it is in `main`) |
| NFR-9 | Entry 2 | ✅ met — cite-once present in exactly `api`/`data`/`seo`/`ux`, absent in `cli`/`i18n`/`library`/`security`; re-derived by count, and `security` is indeed the gap NFR-9 names by example |

## 5. What Was Checked

- [x] Correctness: all ten fixes claimed by round 4 checked one by one — 8 confirmed (F12, F13, F14, F16, F17, F20 this round; F2, F15 already at r4), 2 reached one of two named locations and stay open (F18, F19); Entry 17 hunted for newly introduced error, 3 Minors found (F21, F22, F23)
- [x] Non-functional: constitution §3 fence (empty over every protected surface, re-run), §4 docs-in-step (`README.md` byte-identical to the state confirmed in step at r4), §5 "everything committed under `.spark/` is public" as the operative bar, **§6 privacy non-negotiable now holds at the commit level** — re-read at source (`:191-192`) and verified against every reachable object, not just `HEAD`; `library` lens re-confirmed N/A-by-scope
- [x] Error handling: n/a — no executable code in the diff
- [x] Security/privacy: one commit on the branch, unpushed; a 15-name denylist superset built from public sources and pickaxed case-insensitively over every reachable revision of the four files → only this repo's own name and its already-public companion tool; `git diff origin/main HEAD` → same result; path-token sweep over every reachable revision → no unresolvable `.spark/<dir>` token; old leaking objects unreachable from any ref, so unpushable (F24, Nit). No project name, no feature name and no id↔project mapping appears or is derivable in this report
- [x] Integrity of the squash: `plan.md`, `review.md` and `README.md` byte-identical to the pre-squash commit (`git diff --quiet 872380f HEAD -- <file>`, while the object is still locally resolvable); `evidence.md` differs by exactly +27/−5 lines — Entry 17 plus four SHA-citation restatements, each re-read. Nothing lost, duplicated or truncated; this report's own rows F1–F20 are all present and carried forward
- [x] Tests: the ledger *is* the test suite (plan §4). Entry 17's two substantive commands re-run and reproduce (1 commit; no denylist hit); F20's replacement pattern re-run live and reproduces 3/3 files; `claude plugin validate .` passes with only the pre-existing `autoUpdate` warning
- [x] Readability: Entry 17 is the most useful entry of the five review entries — it records the decision, the option set, the execution and the cost it accepted, which is why its three defects are all presentational rather than substantive

## 6. Verdict

**Passed.** The Blocker that held this feature for three rounds is genuinely closed, and I checked it the hard way rather than on Entry 17's word: the branch is one commit and still unpushed; a denylist superset I built mechanically from already-public sources, never typing a name, finds no surveyed project's name in any revision of these four files that any ref can reach, nor in the squashed commit's own diff; a path-token sweep over every reachable revision finds no feature-directory name that does not resolve to something real in this repo. Entry 17's reasoning for why the squash was necessary at all — that git keeps the old blobs however thoroughly a later commit fixes the text — is exactly right, and the worry that a history rewrite might lose content proved unfounded: three of the four files are byte-identical to the pre-squash state and `evidence.md` differs only by Entry 17 and four citation restatements. The stale SHAs now scattered through rounds 1–4's findings are the price of that fix, not a defect in it; they were provenance for checks that stood at the time, and every fact behind them still stands. Round 4's other fixes are the best set of the five rounds: F20 in particular was fixed at the root rather than adjacently — four hand-picked patterns replaced by one over-inclusive sweep that, run live against the only dataset whose answer is independently known, finds all three matches including the two heading-form ones the old pattern structurally could not see. What keeps this from being clean is the same habit in miniature, now down to Minors: F18 and F19 were each fixed in one of the two places the finding named, with the second silently dropped from the fix narration, and Entry 17 arrived with three defects of its own — a verification block whose third command has no output and tests nothing it claims, a census that counts three rounds of fix-mode where there were four, and a cross-reference to a note its own predecessor fix deleted. None of those five changes a verdict, a count under test or a gate answer, and none is worth another round on its own; they are the user's to accept or to send back as a batch. The ledger's conclusions have now survived five rounds of adversarial re-derivation without a single verdict changing, and its craft has finally caught up to within five Minors of them.

**Open questions, for the ledger's author, not findings.** **Q1:** external reads were denied to me again, so `P1`'s "5 NFR rows" (`evidence.md:493`) rests on the operator's word; nothing depends on it. **Q2:** the `P1`/`P2`/`P3` cells of the AC-3.3 and AC-6.1 tables rest on the operator's run of the now-uniform quoted command — reproducible in principle by anyone with those repos, not by me. **Q3:** the `NFR-6/7 (library lens, scoped)` label at `:509` is ruled inside the Privacy rule's category-label allowance (it names no project, feature or path); the author should confirm that ruling deliberately, since it is the closest surviving string to F12's shape.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings — **F12 confirmed fixed (`fixed r5`), both halves, re-derived from scratch**: 1 commit, unpushed, 0 denylist hits across every reachable revision and in the squashed diff itself, 0 unresolvable path tokens. Constitution §6 now holds at the commit level as well as in the tree
- [x] No open Major findings (or explicitly waived by the user, with reason recorded here) — F20 confirmed fixed this round, re-derived live against this repo's own `qa.md` files; F2, F3, F4, F15 remain confirmed. No Major open, none waived
- [x] Every Must AC traces to implementing code — all 22 ACs and 7 applicable NFRs carry exactly one verdict each (Entry 11, unchanged); §4's docs-in-step bar holds; **no constitution non-negotiable is violated — §6 re-read at source and verified against every ref-reachable object**
- [x] All plan deviations documented and accepted — the class-3 "command quoted in the ledger" deviation is closed in substance (one comparable method across all four AC-3.3 cells). `plan.md`'s own "0 verbatim quotes"/"zero verbatim text" wording, which had lagged `evidence.md`'s narrower "verbatim sentence," is fixed this pass (F19, `fixed`)
- [x] Test suite runs green — no automated suite exists or is possible (constitution §4); every `P-SELF` command re-run live and reproduces, including F20's replacement pattern and Entry 17's commit count; `claude plugin validate .` passes with only the pre-existing, unrelated `autoUpdate` warning
- [x] Line budget respected: Ist 128 / Soll ~150 (excluding HTML comments) — counted with `wc -l`
- [x] Status set to `passed` — unchanged: none of the five Minors or the Nit ever blocked this gate. The user chose to fix all five Minors (F18, F19, F21, F22, F23) immediately rather than carry them forward; only the Nit (F24, an optional local `git gc`) remains open. `Status` and `Round` are the Reviewer's own fields and are left as this round set them; no new `/peer-review` round was run for this batch, per the same rule that made them non-gating in the first place
