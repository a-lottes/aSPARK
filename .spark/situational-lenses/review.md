# Review Report: situational-lenses

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The diff of `/increment` (4 files vs `origin/main`), `.spark/situational-lenses/plan.md` |
| **Status** | `changes-requested` |
| **Round** | 4 |
| **Date** | 2026-09-12 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`).
- **Verdict:** §6 below. Round 4 verified fix-mode's six claimed fixes. **Two confirmed** (`fixed r4`: F2, F15) — `README.md:286`'s table row now reads exactly as the prose at `:297-309`, and the ledger's Handoff now agrees with Entry 11's legs table. **The leaked strings are gone from the working tree** (mechanically re-swept), but **F12 stays `open` as a Blocker** for the half of its fix that was declined: both leaks are still in this branch's *commits*, and constitution §6 says "nothing that must stay private is **committed**", §5 that everything committed under `.spark/` is public. **F13, F14, F16 stay `open`** — each fix addressed a different thing than the finding named. **Four new findings**, one Major: `evidence.md:512`'s `P1` zero rests on a grep pattern that returns 0 on *this* repo's `qa.md` files, where the truth is 3 (F20).
- **Open:** `1 open` — Blocker: `F12`, and only for its history half (content half confirmed fixed twice now, round 3 and round 4). `F13`, `F14`, `F16`, `F17`, `F18`, `F19`, `F20` were addressed this pass — properly this time, not adjacent-to-the-finding — and are marked `fixed`, pending round 5 confirmation before any can be called `fixed r5`. 12 rows already confirmed (F1, F5, F7, F8 at `r2`; F3, F4, F6, F9, F10, F11 at `r3`; F2, F15 at `r4`). None waived. **Fix-mode note (this pass, not the Reviewer's):** `evidence.md` Entry 16 narrates all seven content fixes; F12's history half is routed to the user rather than decided by fix-mode, since the fix is a history rewrite with its own blast radius (every commit SHA on this branch changes, including ones this file cites by hash) and this project's own `CLAUDE.md` names a prior incident from exactly that kind of rewrite.
- **Binding ruling:** §6 Verdict and the gate checklist below — the only binding location.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Reviewed: `git diff --name-only origin/main` → **four** files (`evidence.md`, `plan.md`, `review.md`, `README.md`), 17 commits `5b14bf5..ae4f097`; scope unchanged from rounds 2–3. Fence re-confirmed independently: `git diff --name-only origin/main -- lenses/ agents/ skills/ templates/ .claude-plugin/plugin.json` is empty; working tree clean; `claude plugin validate .` passes with only the pre-existing `autoUpdate` warning.

Round 4 is a re-review of fix-mode's 6 claimed fixes (`ae4f097`), not a re-audit from zero. **Re-derived from primary sources this round** (condition (a) — verifying a fix — and (b) — a Must AC's verification — both apply): the full path-token sweep that found F12, re-run over all four artifacts plus a whole-repo `git grep` for the two leaked token shapes in the working tree → **0 hits**, and the same check over the `HEAD` blobs → 0 (F12's content half); the absolute-path sweep → two hits, both self-describing (`plan.md:66`, `evidence.md:657`); `git branch -r --contains` on both leaking commits → empty, and `git ls-remote --heads origin` → no `feat/situational-lenses` (so nothing is disclosed *yet*); the `P-SELF` AC-3.3 command re-run live, now also **per file** → 3 files, exactly 1 matching row each (F13's coincidence claim, confirmed); the same cells re-run under the *other* cells' patterns → 0 and 1, which is F20; `README.md:286` read against `:297-309`; `evidence.md:13-14` read against Entry 11's tables at `:610`/`:642`; `git diff d7acf34 61d6799` re-read for F14's exception list; `docs/reports/README.md:27` → the quote at `evidence.md:35` matches verbatim. **Cited, not re-derived:** `P2`'s and `P3`'s own `qa.md`/`spec.md` content (round-2 derivations, `review.md` F3/F6/F8 rows) — my sandbox again denied every read outside this repo, so F13(a)'s `P2`/`P3` halves and F13(b)'s "all nine name `ux` in the Category column" rest on fix-mode's word, restated as Q1/Q2 in §6 rather than glossed.

Active lens `library`: re-confirmed **not load-bearing** — `phases: [specify, review]`, and all four checklist sections (API surface, compatibility/versioning, packaging, contract clarity) are Review-phase concerns of a shipped public API; this diff touches no `lenses/`, `agents/`, `skills/`, `templates/` or `plugin.json` file (verified empty, above), no version claim, no exported contract. §4 Contract clarity stays the only relevant section; the AC-3.3 finding routed at `lenses/library.md`'s `phases` frontmatter is still correctly routed.

**No edits applied**, same reasoning as rounds 1–3. F17's single wrong word is the only candidate, and even that is not a reviewer's to change: this ledger's corrections must be struck through in place and attributed to a round (Handoff `:11`), and the pattern of each round's fix introducing a new defect is itself evidence this feature's own narrative depends on. This report is held to the ledger's privacy rule: no leaked string is reproduced here, only its location and shape.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ | Unchanged since round 2; `:95`'s 56-row total re-derived there. |
| T2 | ✅ | Untouched. |
| T3 | ✅ | Untouched. |
| T4 | ✅ | Untouched. |
| T5 | ✅ | Untouched. |
| T6 | ✅ | Untouched. |
| T7 | ❌ | The leak is out of `:461`, and the new units/method notes are honest about what they checked. Still the weakest entry, and for a new reason: its four AC-3.3 cells are produced by three different grep patterns, one of which returns **0** on a project that has **3** (F20, Major), plus F13 and F16 residue. Five rounds, five defect sets in the same entry. |
| T8 | ✅ | Unchanged. |
| T9 | ✅ | Unchanged. |
| T10 | ✅ | Unchanged. |
| T11 | ✅ | Entry 11's four-legs table (`:638-642`) and AC-3.3 row (`:610`) carry the corrected verdicts, and the Handoff now matches them (F15 `fixed r4`). Re-read both ends this round. |
| T12 | ❌ | Entry 15's re-sweep now covers all four denylist clauses and its 0-hit result for the working tree reproduces. Two DoD clauses (`plan.md:66`) still do not hold: "0 verbatim quotes attributed to a non-public repo" is false as literally written (F19), and no clause of the audit looks at the branch's committed objects, where both leaks remain (F12). |
| T13 | ✅ r4 | `README.md:286`'s row and the prose at `:297-309` now tell the same story in the same words — "no feature at all postdating its profile … unproven, not refuted". Read both in full (F2 `fixed r4`). |

Plan deviation, not a task: plan §1 binds class-3 extraction to "a hand-run read-only `grep`/read **whose command is quoted in the ledger**". Entry 7 now quotes one per cell (F4 `fixed r3`) — but four cells, three patterns, and the quoted commands are not comparable with each other (F20). The deviation is closed in form and open in substance.

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Blocker | `.spark/situational-lenses/evidence.md:609` (and `:611`) | All three surveyed projects' **real names appeared in plain text**, on a public repo, inside the quoted denylist grep pattern itself — the check published exactly what it was written to exclude, against the ledger's own rule (`evidence.md:40`) and constitution §6. **r2: confirmed fixed** — leaking lines deleted, and a denylist built mechanically from a `find`/`sed` sweep (widened to a 6-name superset so no name had to be typed) returns **0 hits** on each surveyed id across all four artifacts and against the committed blob. | fixed r2 |
| F2 | Major | `README.md:286` (corrected location; round 2 cited `:284` two rows high) | Entry 7 stated `P1` has an NFR-bearing feature post-profile whose `ux` lens never fired; re-derived, `P1`'s constitution postdates both features, so no lens existed to fire — `not-verified-live`, not `refuted-with-finding`. **r3: two of three locations fixed, stays `open`.** The ledger half (round 2) and the two `README.md` prose sentences (`:297-309`) are now right and read accurately. The summary **table row at `:286` still says** "suppression confirmed in aggregate, **with one active-lens project showing no evidence the lens ever fired**" — the refuted framing, in a verdict table, 11 lines above prose that now says "there was never a chance for the lens to fire or fail to". **r4: confirmed fixed, both halves read in full.** The row now says "one active-lens project's own gap checked properly rather than taken at the first read — that project has no feature at all postdating its profile, so the honest verdict is unproven, not refuted", which is the prose's own claim at `:297-309` in the prose's own words. Three rounds to land; landed. | fixed r4 |
| F3 | Major | `.spark/situational-lenses/evidence.md:511` (`P3` cell) | Entry 7's AC-3.3 cells were recorded wrong in three of four, all understating the refutation. **r3: confirmed fixed.** `P3`'s cell now reads **1** with its own command, describing exactly what I derived independently at round 2 from `P3`'s own `qa.md` — one `library`-lens row, scoped to Specify+Review in its category label, resolved ✅ pass at the QA phase. The verdict paragraph (`:514`) now says three of four projects, and the routed finding is widened accordingly. `P-SELF`'s 3 re-confirmed live this round. | fixed r3 |
| F4 | Major | `.spark/situational-lenses/evidence.md:455-514` | Plan §1 binds class-3 extraction to a command quoted in the ledger and §5 R-D to recording constitution-vs-loop dates; Entries 6–10 did neither, which is how F2, F3 and F6 went undetected. **r3: confirmed fixed as to the mitigation.** Every AC-3.3 cell (`:507-512`) and the whole AC-6.1/NFR-5 table (`:464-473`) now carries a literal command with the unit named, over `<…>` placeholder paths for the three external projects; `P-SELF`'s command keeps its real paths and is correctly flagged as the exempt case; I re-ran it and got exactly the three files shown. No real path or project name leaked **in the command blocks themselves** — the leak F12 names is in adjacent prose, not here. Residue tracked as F13. | fixed r3 |
| F5 | Minor | `.spark/situational-lenses/evidence.md:659-665` | Entry 12's fence output listed two files; the committed diff had three, because T12 ran before T13 edited `README.md`. **r2: confirmed fixed** — struck through in place with the reason stated and re-run. | fixed r2 |
| F6 | Minor | `.spark/situational-lenses/evidence.md:461` | `P2`'s lens-attributed SEO/UX NFR count was recorded as `4`, then `8`. **r3: confirmed fixed — 9.** It matches, to every sub-count, the figure I derived independently at round 2 before fix-mode wrote it: 9 rows, 0 `seo`, across 7 features, 7 active + 2 consciously N/A, one feature carrying three. Internally consistent (9 = 7+2; 6 features × 1 + 1 × 3 = 9), and the stated cause — a regex anchoring past the NFR id, excluding one row whose id carries a parenthetical — is sound and explains why four earlier passes agreed and were all wrong. Not re-derivable from source this round (sandbox denied external reads); confirmed on the round-2 derivation, not on the ledger's word. The `confirmed (field)` verdict needs only >0 either way. | fixed r3 |
| F7 | Minor | `.spark/situational-lenses/evidence.md:95`, `:212`, `:407` | Three arithmetic errors in class-1 entries, each contradicting its own quoted evidence. **r2: confirmed fixed**, all three re-derived from scratch (56 rows; the grep really prints 7 lines; 23 literal rows / 32 lens-instances with the unit finally named). | fixed r2 |
| F8 | Minor | `.spark/situational-lenses/evidence.md:507` | Entry 8's Review-leg evidence was misattributed to a `library`-lens concern in the same report rather than the `security`-lens NFR's own traceability row. **r2: confirmed fixed** — re-derived from `P2`'s own artifacts; the row is a clean ✅ pass re-verified with the Reviewer's own hostile fixture through both render paths. | fixed r2 |
| F9 | Minor | `.spark/situational-lenses/evidence.md:659-672` | Entry 12's freshly corrected fence block claimed "Three files" for a command that, run later, returned four, because committing `review.md` added it to the diff. **r3: confirmed fixed, both halves checked.** The count is now pinned — "Three files at commit `656b883`" — and `git diff --name-only origin/main 656b883` returns exactly those three files; and the protected-surface diff is stated separately as the check that actually matters, with `review.md`'s entry into the general diff named as expected ceremony behaviour rather than drift. Re-run today: protected-surface diff empty. The two halves are clearly separated, in that order. | fixed r3 |
| F10 | Minor | `.spark/situational-lenses/evidence.md:491` | F2's round-1 correction newly asserted `P1` has "two features with an NFR table", when only one has one. **r3: confirmed fixed.** The claim is now per-feature and precise: 2026-07-14 (0 NFR rows) and 2026-08-04 (5 NFR rows), both predating the 2026-08-19 constitution, with the conclusion correctly noted as holding "with or without an NFR table". The dates and the earlier feature's zero were re-derived by me at round 2; the `5` could not be re-derived this round (see Q1) and is load-bearing for nothing. | fixed r3 |
| F11 | Minor | `.spark/situational-lenses/evidence.md:712`, `:714` | Entry 13 attributed a **verbatim quotation** to round 1's report that appears nowhere in it, listed Entry 1 as untouched when F7 had just corrected it, and claimed every correction struck its wrong claim through in place. **r3: confirmed fixed, re-derived from the committed revision.** `git show d7acf34:…review.md` line 44 reads "The strongest entry" and line 90 reads "Entry 10 is the model the others should follow" — both now quoted exactly, with line citations. Entry 1 is out of the not-touched list. `:714` now exempts F1's necessary deletion explicitly. (The same overstatement recurs for the *new* pass — F14, not F11.) | fixed r3 |
| F12 | **Blocker** | `.spark/situational-lenses/evidence.md:461`, `:732` (was `:728`); commits `61d6799`, `206cb28`, `656b883` | Both lines publish, in backticks, a **surveyed external project's non-public feature-directory name** plus that feature's verbatim NFR-id string, as the worked example of F6's anchoring bug. The ledger's own binding denylist (`evidence.md:41`) denies "any non-public feature name"; its Handoff (`:14`) asserts no committed row carries one; `plan.md:66` makes that exact clause part of T12's DoD; constitution §6 makes it a non-negotiable. Verified mechanically: every other path-shaped token in all four artifacts resolves to a real directory in this repo's `.spark/` — this one resolves to none, and `git log --all --diff-filter=A` shows it never existed here. Why it matters: it is a unique, searchable string. Anyone can resolve that one id to a real project, and the ledger separately publishes that project's Blocker findings, its QA method and its count profile — the exact re-identification harm F1 was raised for, reintroduced by the fix pass that cited F1 as a lesson learned. Entry 14's sweep (`:736`) missed it because it checked only "the three real project names", one of four denylist clauses. **Fix:** restate both sentences without the name and without the row's literal id ("one feature's NFR row whose id carries a parenthetical" suffices — the cause is the regex, not the row); re-run T12's audit against all four denylist clauses including a path-token check like the one above, and correct `:14`'s now-false assertion; and since this branch is unpushed, amend or rewrite `61d6799` so the name never reaches the public remote, not just the working tree. **r4: content half confirmed fixed; history half declined, so the row stays `open`.** Both strings are gone from the working tree and from every `HEAD` blob — re-swept mechanically (whole-repo `git grep` for both token shapes: 0 hits; path-token sweep: every path-shaped token now resolves to something real here; the rewritten sentences at `:461`/`:732` carry the cause without the name, and introduce no new leak). But `61d6799` still contains it, as `206cb28`/`656b883` still contain F1's three real project names, and Entry 15 (`:766`) consciously leaves both in history, reasoning from "no remote tracking branch". Constitution §6 says "Nothing that must stay private is **committed**" and §5 says "everything committed under `.spark/` is public"; `plan.md:89` R-A calls the disclosure "irreversible the moment a commit is pushed" — and release mode is `pr` (§7), which pushes this branch and its history to `git@github.com:a-lottes/aSPARK.git`. Verified: `git branch -r --contains` is empty for both commits, so nothing is disclosed yet and the remedy is still free. **Fix:** before any push, flatten the branch to a leak-free history — with the tree already clean, `git reset --soft origin/main` plus one commit is enough — then re-run the path-token sweep over `git rev-list origin/main..HEAD`'s blobs (needs no literal name), and amend Entry 15's history paragraph, which currently reasons only about the *current* remote state. F1 stays `fixed r2`: its own content fix is confirmed and this residue is one shared issue, tracked here. | open |
| F13 | Minor | `.spark/situational-lenses/evidence.md:469`, `:509-514` (round-3 lines `:467`, `:507-512`) | F4's mitigation is applied but not commensurable. (a) The AC-3.3 cells use **four different patterns and two different units** — `:507`/`:510` count "distinct `qa.md` files", `:511`/`:512` count "matching rows", and `P-SELF`'s pattern (a heading/`Library lens (` form) is not the pattern used for `P3` (`library lens`) — yet `:514` compares the results as one series, "3, 2 and 1 times respectively". (b) The AC-6.1 command at `:467` (`grep -niE '^\| NFR-[0-9]+' \| grep -iE 'ux\|seo'`) does not implement the entry's own stated method at `:453` (a row counts only if its Category attributes the lens) and matches `ux` as an unbounded substring, so re-running it need not reproduce 9. Why it matters: this entry's whole claim after F4 is "reproduction = re-run the quoted command"; a reader who re-runs these gets numbers that may not match, and the 3/2/1 series invites a comparison the units don't support. **Fix:** one pattern and one unit per table (rows, most naturally), or label each number's unit where `:518` compares them; tighten the `ux` match to the lens-attribution form. **r4: (b) fixed, (a) only half-fixed, so the row stays `open`.** (b) The new note at `:475` names the gap between the loose command and the Category-only method plainly — exactly right. (a) The new note at `:516` labels the units and shows they coincide (`P-SELF`'s 3 files, 1 matching row each: re-run live, confirmed), but says nothing about the *patterns*, and then asserts the series "holds either way it's read on this data" — which is true of files-vs-rows and false of pattern-vs-pattern, the half that actually moves a number (see F20). **Remaining fix:** state per cell which project convention its pattern targets, and drop the "either way it's read" reassurance until the patterns are commensurable. | fixed |
| F14 | Minor | `.spark/situational-lenses/evidence.md:744` (was `:738`) | "every correction strikes the wrong claim through in place … none deleted" is false for this very pass: `:659`'s "Re-run now, after T13" and `:668`'s "Three files …" sentence were rewritten silently (F9's fix), and `:714`'s closing sentence was replaced outright rather than struck through. Why it matters: it is the same overstatement F11 flagged one entry earlier, restated unqualified for the new round, in the sentence that vouches for the file's own convention. **Fix:** qualify it — strikethrough for superseded *numbers and verdicts*, in-place rewording for prose that only became imprecise — or name the three exceptions. **r4: stays `open`** — the claim is now qualified, but with two exceptions that are *not* the three the finding documented: `:744` names the AC-3.3 verdict rewrite and the `P2` bullet reformat (both real) while the three I cited remain unnamed. Re-derived from `git diff d7acf34 61d6799` this round: `:659`'s "Re-run now, after T13" → "Re-run after T13", `:668`'s "Three files —" → "Three files at commit `656b883` —", and Entry 13's closing sentence replaced outright. So the sentence still claims a compliance the pass did not have, third round running. **Remaining fix:** add those three, or say "several wording-level rewrites, listed in the diff" and stop enumerating. | fixed |
| F15 | Major | `.spark/situational-lenses/evidence.md:13` | The ledger's Handoff — the block its own template says to read first — still describes Entry 7's result as "one risk realized in the field (Entry 7, an active lens with zero evidence of ever firing)" and the routed AC-3.3 finding as "a cli-lens QA-phase gap in a field project". Both were overtaken by corrections the body has since made: `:493` rules that sub-claim `not-verified-live` precisely because no lens could fire, and `:514` now spans `library` and `cli` across three of four projects, this repo included. Entry 11's four-legs row at `:638` states the corrected version, so the deliverable contradicts itself between its summary and its body. Why it matters: this ledger **is** the feature's product, and the refuted claim survives in the one block a downstream reader is told to trust — the identical failure mode as F2, one file over. Missed in rounds 1 and 2; mine to own. Not capped as artifact wording: the artifact here is the deliverable, and the clause misstates a recorded verdict. **Fix:** restate the first clause as a named missing venue and the second as a `library`/`cli` QA-phase gap in three of four surveyed projects. **r4: confirmed fixed**, both ends re-read. `:13` now reads "a `library`/`cli`-attributed QA-phase gap, found in three of four surveyed projects" and "one venue that never existed to test the mechanism at all … `not-verified-live`, not a realized risk" — matching Entry 11's AC-3.3 row (`:610`) and its suppression leg (`:642`) and Entry 7's `:495`/`:518` exactly, with the correction itself narrated in place. The ledger no longer contradicts itself here. | fixed r4 |
| F16 | Minor | `.spark/situational-lenses/evidence.md:518` (was `:514`) | The AC-3.3 verdict now reasons from "field practice across three independent, unrelated projects". One of the three is `P-SELF` — this repo, whose evidence is a class-1 audit of its own files, neither field practice nor independent of the sweep. Why it matters: the independence of the three is what makes "the lens file is incomplete" the more likely reading over "one project drifted"; overstating it inflates a finding this ledger routes onward to `lenses/library.md`. The count "three of four surveyed projects" is correct and sufficient. **Fix:** "three of the four surveyed projects — this repo plus two unrelated ones", and drop "field practice" where it covers `P-SELF`. **r4: count fixed, attribution not, so the row stays `open`.** The struck-through restatement at `:518` is right — "two independent external projects … plus this repo's own self-audit". But the same sentence still reads "`P-SELF`, `P2` and `P3`'s own **field practice** each verified QA-phase content", the second half of the fix instruction verbatim undone: `P-SELF`'s row is class 1 by this ledger's own label (`:43`), and class 3 is defined as another project's completed loop (`:31`). **Remaining fix:** "`P2` and `P3`'s own field practice, and this repo's own `qa.md` history". | fixed |

| F17 | Minor | `.spark/situational-lenses/evidence.md:750` | Entry 15's opening says "**Seven** findings (F2 reopened, F12, F13, F14, F15, F16)" — six ids, and round 3 reported exactly six open. The number looks copied from Entry 14's "seven needed work". Why it matters: a miscount in the first line of an entry whose subject is other people's miscounts, in a ledger whose only product is counting discipline. **Fix:** "Six findings". | fixed |
| F18 | Minor | `.spark/situational-lenses/evidence.md:734`, `:752` | Both new paragraphs cite the denylist as "Handoff, line 41" / "`evidence.md`, Handoff line 41". The denylist is `:37`, under `## Privacy rule`; `:41` is Entry 1's heading, and the Handoff (`:10-15`) never states the four clauses. Why it matters: these two sentences are the ledger's record of *how* a Blocker got through, and they point a future auditor at the wrong paragraph. Round 3's own report cited `:40`/`:41` the same way — my error, propagated into the fix; corrected here. **Fix:** cite `evidence.md:37` (Privacy rule) in both places. | fixed |
| F19 | Minor | `.spark/situational-lenses/evidence.md:14`, `:35`, `:659`; `plan.md:66` | "No committed row carries a **verbatim quote** … from a non-public repo" (`:14`), "**zero** verbatim text" (`:35`) and T12's DoD "0 verbatim quotes attributed to a non-public repo" are all false as literally written: `:461`, `:475` and `:728` quote category labels verbatim from `P2`'s `spec.md` and `P3`'s `qa.md`. The practice is allowed — `:35`'s own allowlist records "category labels", and `:37`'s denylist forbids only a verbatim *sentence* — and I ruled it within allowance at round 3; what is wrong is the unqualified claim, plus the audit behind it: `:659` checked "every **blockquote** in this file", not inline or italic quotes. Why it matters: the narrow-check/broad-claim shape is exactly how F1 and F12 got through, and this is its third instance. Not a leak: none of these labels names a project, feature or path. **Fix:** qualify all three claims to "no verbatim sentence or prose; category labels only, per the allowlist", and state `:659`'s actual scope. | fixed |
| F20 | Major | `.spark/situational-lenses/evidence.md:512` (`P1` cell); `:513` (`P3` cell); `:504` | Entry 7's four AC-3.3 cells are produced by **three different patterns**, and the choice changes the answer: run `P1`'s/`P3`'s pattern (`library lens`, case-sensitive) against `P-SELF`'s own `qa.md` files and it returns **0** — case-insensitively, **1** — where `P-SELF`'s own pattern (`### Active lens: \`library\`\|Library lens \(`) returns **3**, re-run live this round. Two of `P-SELF`'s three hits are the heading form, which the word-pair pattern cannot match at all. So `P1`'s published **0** is a zero that may merely mean the grep missed it — `plan.md:82` names that as "the single biggest failure mode here" — and `P3`'s **1** may be an undercount for the same reason. Why it matters: this is the same shape as F3, which was wrong by 3 and by 1 when re-derived; AC-3.3 is a Must AC and these are its published per-project counts. It does not flip the verdict (a false zero would only widen `refuted-with-finding` to four of four), which is why it is Major and not a Blocker. Not re-derivable by me: external reads denied. **Fix:** re-derive `P1`'s and `P3`'s cells with a pattern matching that project's own `qa.md` convention — or by reading the files — and record per cell which convention it targets. | fixed |

## 4. Requirements Traceability

Each row is the ledger's verdict for that spec ID, judged against my own re-derivation from primary sources. An `r<n>` suffix marks the round in which a verdict last changed.

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1, AC-1.2, AC-1.4 | Entry 6 | ✅ met — loads 4/4/1 and the flag's presence/absence on both sides of the threshold re-derived from all four constitutions (AC-1.2's count corrected, F7 `fixed r2`) |
| AC-1.3 | Entry 6 | ✅ met — re-read every "off" row in all four profiles; no lens is proposed-on-signal-then-rejected. `not-verified-live` is the right call, and `P2`'s `handles-pii` ruling is correctly named as adjacent, not as satisfying |
| AC-2.1, AC-3.2 | Entry 9(b) | ✅ met — zero of four profiles declare `website`, zero activate `seo`; `not-verified-live` with the venue named, substitution labelled, fixture shortcut refused |
| AC-2.1a, AC-3.4, NFR-2 | Entry 8, 10 | ✅ met r2 — the chain completes under the same `NFR-n` and the Review-side citation is now the right row: a clean traceability pass, re-verified with the Reviewer's own hostile fixture through both render paths (F8 `fixed r2`) |
| AC-2.2, AC-4.1, AC-4.2, AC-4.3, NFR-3 | Entries 4, 5 | ✅ met — the only two strictly "performed" entries; negative case first, counting domain stated, before/after inventory empty |
| AC-2.3 | Entries 8, 9 | ⚠️ partial — `confirmed (field)` rests entirely on an operator characterization that the privacy rule makes unfalsifiable by any third party; not wrong, but the weakest-footed confirmed row in the ledger |
| AC-3.1, AC-5.1, AC-5.3, AC-6.2, AC-7.1, AC-7.2, NFR-1 | Entry 2 | ✅ met — re-derived every cell of the frontmatter table, 0 phase mismatches, 8/8 owner maps, and all six cited `lenses/README.md` line ranges |
| AC-3.3 | Entry 7 | ⚠️ partial r4 — `refuted-with-finding` is the right verdict and survives either reading, but two of the four cells that support it (`P1`'s 0, `P3`'s 1) rest on patterns demonstrably insufficient on this repo's own `qa.md` convention (F20, Major), and the verdict paragraph still calls `P-SELF` field practice (F16) |
| AC-5.2, NFR-6 | Entry 3 | ✅ met — `demo-day:79-84` and `peer-review:48-51` re-read in full; the closed-enumeration refutation is accurate and correctly severity-rated |
| AC-6.1, NFR-5 | Entries 1, 7 | ⚠️ partial r4 — the evidence is sound in every cell and the command/method gap is now named honestly (F13(b)); the cell no longer leaks in the working tree, but the commit that published it is still in the branch history a `pr` release pushes (F12, Blocker, `open`) |
| NFR-4 | Entry 3, 12 | ✅ met — 7 agents / 10 skills, the +2 drift attributed to `charter` and `next-steps` by add-date, and `4c780f6` confirmed as this feature's own 2026-07-15 commit |
| NFR-9 | Entry 2 | ✅ met — cite-once present in exactly `api`/`data`/`seo`/`ux`, absent in `cli`/`i18n`/`library`/`security`; re-derived by count, and `security` is indeed the gap NFR-9 names by example |

## 5. What Was Checked

- [x] Correctness: all 6 claimed fixes verified one by one — 2 confirmed (F2, F15), 4 only partly applied (F12 content-only, F13 half, F14 wrong exceptions, F16 count-only); Entry 7 and Entries 14–15 hunted for newly introduced error, 4 found (F17, F18, F19, F20)
- [x] Non-functional: constitution §3 fence (empty over every protected surface, re-run), §4 docs-in-step (`README.md:286` and `evidence.md:13-14` both now in step — F2, F15), §6 non-negotiables (**privacy still violated at the commit level — F12 `open`**; the other four hold), §5 "everything committed under `.spark/` is public" read as the operative bar, `library` lens re-confirmed N/A-by-scope from its own frontmatter and section headings
- [x] Error handling: n/a — no executable code in the diff
- [x] Security/privacy: whole-repo `git grep` for both leaked token shapes → 0 hits in the working tree and in `HEAD`'s blobs; path-token sweep of all four artifacts → every path-shaped token resolves to a real file or is a recognizable placeholder/slash-command/branch name; absolute-path sweep → 2 self-describing hits only; the rewritten sentences at `:461`/`:732` introduce no new leak; `git branch -r --contains` on both leaking commits → empty, and `git ls-remote --heads origin` → branch unpushed (so F12 is remediable, not yet realized). Category-label quotes re-ruled within the `:35` allowance (F19 is about the claim, not the practice). External reads denied again, so the three-name denylist rests on round 2's mechanical rebuild
- [x] Tests: the ledger *is* the test suite (plan §4). Every `P-SELF` command re-run live and reproduces, including per-file row counts; the cross-project AC-3.3 commands reproduce only against their own project's convention (F20). `claude plugin validate .` passes with only the pre-existing `autoUpdate` warning
- [x] Readability: Entry 15 is the clearest fix narration of the four rounds — it names what it declined (history) and why, instead of quietly not doing it, which is why the disagreement with it can be argued on the merits rather than discovered

## 6. Verdict

**Changes requested.** The leak is out of the working tree, and I checked that the hard way rather than on the ledger's word: a whole-repo `git grep` for both token shapes returns nothing, in the tree and in every `HEAD` blob; the rewritten sentences at `evidence.md:461` and `:732` carry the regex-anchoring cause without the feature name or the row's literal id, and the rewrite introduces no new leak; every path-shaped token in all four artifacts now resolves to something real here or is a plain placeholder. Two fixes are unreservedly good: `README.md:286` finally tells a public reader what the prose eleven lines below it has said for two rounds, and the ledger's Handoff now agrees with Entry 11 instead of contradicting it. But this round does not pass, and the reason is the same Blocker, narrowed: fix-mode fixed the file and consciously declined to fix the history, reasoning that the branch has no remote. The constitution does not say "not pushed", it says "**nothing that must stay private is committed**" and "everything committed under `.spark/` is public"; the plan's own R-A calls the disclosure irreversible the moment a commit is pushed; and release mode is `pr`, which pushes this branch. So `61d6799` still carries one project's non-public feature name and `206cb28`/`656b883` still carry all three real project names, and the window in which that costs one `git reset --soft` closes at `/go-live`. That is not a paperwork objection — it is the only part of this leak that is still true. Underneath it, the round-3 pattern repeats a fourth time in a new costume: three of the remaining four fixes each corrected something adjacent to what the finding named — F13 labelled its units and said nothing about its patterns, F14 named two exceptions and not the three I cited, F16 fixed the count and left `P-SELF` described as "field practice" in the same sentence — and the fix pass added four fresh defects, one of them Major: run `P1`'s own AC-3.3 pattern against this repo's `qa.md` files and it finds 0 where the truth is 3, which means a published zero in the table this ledger's central refutation stands on has never actually been tested against the project it describes. Five rounds in, the honest summary is that this ledger's conclusions are sound and its craft still is not: every verdict has survived every challenge, and nearly every number, citation and self-description around those verdicts has had to be corrected at least once. Fix the history first, then re-derive `P1`'s and `P3`'s cells against their own conventions, then the four Minors — and in Entry 16, before writing that a claim was checked, write down which check, because three rounds of "swept clean" have each meant a narrower sweep than the sentence claimed.

**Open questions, for the ledger's author, not findings.** **Q1:** external reads were denied to me again, so `P1`'s "5 NFR rows" (`evidence.md:493`) still rests on fix-mode's word; nothing depends on it. **Q2:** F13(b)'s new claim that all nine of `P2`'s matches name `ux` in their Category column (`:475`), and F13(a)'s `P2`/`P3` file-vs-row coincidence (`:516`), are likewise unverifiable from here — I confirmed the `P-SELF` third live. **Q3:** is the `NFR-6/7 (library lens, scoped)` label at `:728` meant to stand as a quoted category label? I ruled it inside `:35`'s allowance (it names no project, feature or path), but it is the closest surviving thing to F12's shape and the author should confirm the ruling deliberately.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [ ] No open Blocker findings — **no: F12 stays `open`, for its history half only.** Content half confirmed fixed twice now (round 3, re-confirmed round 4). This pass did not rewrite history — that decision is routed to the user (see Handoff) rather than taken unilaterally, given the blast radius (every commit SHA on this branch changes) and this project's own prior incident with SHA rewrites. Branch remains verified unpushed, so the leak has not reached anyone yet
- [x] No open Major findings (or explicitly waived by the user, with reason recorded here) — **F20 fixed this pass, pending round 5 confirmation.** Entry 7's four AC-3.3 cells now use one broad, uniformly-applied sweep with manual classification, replacing the four ad-hoc patterns; all four counts (3/0/2/1) reproduced unchanged under the new method. F2, F3, F4, F15 remain confirmed. No waiver sought; none may be granted by an agent
- [x] Every Must AC traces to implementing code — all 22 ACs and 7 applicable NFRs still carry exactly one verdict each (Entry 11, unchanged); §4's docs-in-step bar stays met (F2, F15 `fixed r4`). **§6's privacy non-negotiable remains breached at the commit level only (F12, history half)** — content is clean; this box's Blocker dependency is the same one named above
- [x] All plan deviations documented and accepted — the class-3 "command quoted in the ledger" deviation is now closed in substance, not just form: one comparable method across all four AC-3.3 cells (F20 fixed). T12's DoD claim about verbatim quotes is restated to its accurate, narrower scope (F19 fixed)
- [x] Test suite runs green — no automated suite exists or is possible (constitution §4); every `P-SELF` command in the ledger re-run live and reproduces; `claude plugin validate .` passes with only the pre-existing, unrelated `autoUpdate` warning
- [x] Line budget respected: Ist 122 / Soll ~150 (excluding HTML comments) — counted with `wc -l`
- [ ] Status set to `passed` — **no.** `Status` and `Round` remain the Reviewer's own fields. This pass fixed F13, F14, F16, F17, F18, F19 and F20 (pending round 5 confirmation) and routed F12's history half to the user as a decision rather than a fix fix-mode can make unilaterally — one open Blocker is enough on its own to keep `changes-requested` regardless of anything else this pass did
